# かな漢字変換の品質評価

micro benchmarkは速度を、評価suiteは変換品質を測る。AJIMEE-Benchは難しい漢字誤変換へ意図的に偏ったデータなので、単独の総合品質指標にはせず、must-pass testや将来のbalanced corpusと併用する。

## ライブ変換の逐次回帰

最終候補だけの評価では、入力途中のpreeditが前の表層に固定される問題を検出できない。そのため、`raibuhenkannno`を1キーずつ入力したときに未確定`n`を越えて`ライブ変換の`を維持する回帰をcoreとmacOS adapterの両方に置く。

局所コーパスなしで初回から`差がつく`を意味判断する商品モデルはまだない。完全一致履歴を無条件にライブ変換へ昇格させると、古い`そうしま→総島`のような履歴が現在のconfidence gateを迂回して自動表示される。通常候補では学習順位を使うが、ライブ変換は「正しいと確信できなければかなのまま」という安全境界を維持する。

## AJIMEE-Bench

実行方法:

```sh
just evaluate-ajimee
just evaluate-ajimee --context none
just evaluate-ajimee --context present --json
```

直接実行する場合:

```sh
scripts/evaluate-ajimee.sh --top-k 10 --context all
```

初回だけ評価データを`target/evaluation`へ取得する。取得元はAJIMEE-Benchのcommit `401666cd56d1a570c2021798b64b6da4396bfd45`に固定し、SHA-256を検証する。評価データを製品bundleへ含めたり、通常のbuildやtestでネットワークへ接続したりしない。

出力する指標:

- `acc@1`: 第1候補がいずれかの許容解と完全一致した割合
- `acc@k`: 上位k候補に許容解が含まれる割合
- `mrr@k`: 最初の正解候補の逆順位の平均
- `mincer@1`: 第1候補と最も近い許容解の文字誤り率
- `mincer@k`: 上位k候補と許容解の組み合わせで最小の文字誤り率
- latency `p50/p95/p99/max`: 辞書初期化を除いた候補生成時間

`--context none`は左文脈なし100件、`--context present`は左文脈あり100件、`--context all`は全200件を評価する。初期baselineでは`context_used_by_engine`が`false`だったが、現在は辞書規則とローカルニューラル再順位付けが左文脈を使う。評価itemに任意の`right_context_text`があればニューラルpromptだけへ渡し、フィールドがない既存fixtureは空文字として後方互換に読み込む。

### 2026-08-09 入力長に応じたニューラル候補幅

製品のニューラル再順位付けは、8文字以下の短い読みを5候補まで採点する。9文字以上では、モデルの準備完了後に限って内部N-bestを遅延探索し、`balanced`は最大16候補、明示選択する`high-accuracy`は最大32候補を採点する。候補は従来どおり、先頭候補とのcost差で制限する。モデル未使用・準備中・ロード失敗時は追加探索せず、採点失敗時も深い候補を初回10候補へ混ぜない。次の組み合わせは`balanced`の製品境界を再現する。

```sh
--top-k 16 \
--search-k 16 \
--neural-max-candidates 5 \
--neural-long-input-min-characters 9 \
--neural-long-input-max-candidates 16 \
--neural-max-candidate-cost-gap 1500
```

まずJWTDとGSD devで短文5候補・長文8候補のしきい値を決め、固定8候補で起きた短文の回帰を除いた。その後、元devと重ならないJWTD train 800件でも長文N-best 16の効果を確認してから、held-outを採否確認に使用した。

| dataset | 適応5/8候補 acc@1 | scorer-ready 5/16候補 acc@1 | 差 |
| --- | ---: | ---: | ---: |
| JWTD dev (400) | 0.4325 | 0.4400 | +3件 |
| disjoint JWTD train (800) | 0.4563 | 0.4675 | +9件 |
| GSD dev (331) | 0.8640 | 0.8640 | 0件 |
| AJIMEE held-out (200) | 0.7150 | 0.7300 | +3件 |
| GSD test held-out (323) | 0.8700 | 0.8700 | 0件 |

固定8候補では短いGSD testが1件悪化したが、9文字境界では短文を従来の5候補に保つため回避できた。AJIMEE 200件の3回交互測定では、5/8候補の中央値がp50 9.44 ms / p95 24.93 ms / p99 38.35 ms、5/16候補がp50 11.41 ms / p95 38.72 ms / p99 50.22 msだった。長文だけtail latencyを許容して+1.5ptを採り、KVセル数1024のメモリ上限は変更しない。

### 2026-08-09 長い読みのニューラルcost窓

[ATOK 2026の公式説明](https://www.atok.com/features/)は、助詞が省かれる口語や、確定済みの語に続いて助詞から細切れ入力する場合の変換を強化している。[azooKeyの通常変換](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Core/FullInputProcessing.swift)も、辞書costと接続costからN-bestを探索したうえで後段の評価へ渡す。Slimeでは短い同音語の安全境界を変えず、9文字以上の読みだけ、ニューラル採点候補の先頭からのcost差を1,500から2,500へ広げた。探索幅16、短文5候補、長文16候補、base上位2件のcost差1,000、lambda 0.7、EOS除外scoreは変更しない。

cost窓はJWTD devだけで1,500 / 2,000 / 2,500 / 3,000を比較し、top-1とMRRが最良だった2,500に固定した。2,500では`誌上で`、`仕えることに`、`神学院`、`エイトキン`、`神託事件`、`行われていた`など7件を修正し、`たかじん → た歌人`を1件悪化させ、差し引き6件改善した。単一dev例を消す表記別filterは追加せず、別domainのheld-out非回帰を採用条件にした。

| dataset | cost差1,500 acc@1 / MRR | 長文のみ2,500 acc@1 / MRR | MinCER@1 | 平均採点候補 | p95 |
| --- | ---: | ---: | ---: | ---: | ---: |
| JWTD dev (400) | 0.4400 / 0.5372 | **0.4550 / 0.5574** | 0.0697 → **0.0688** | 6.97 → 10.30 | 29.70 → 30.26 ms |
| GSD dev (331) | 0.8852 / 0.9259 | 0.8852 / 0.9259 | 0.1052（不変） | 3.51（不変） | 5.73 → 5.81 ms |
| AJIMEE held-out (200) | 0.7300 / 0.7761 | 0.7300 / **0.7804** | 0.0376 → **0.0368** | 7.16 → 9.72 | 40.39 → 40.74 ms |
| GSD test held-out (323) | 0.8824 / 0.9221 | 0.8824 / 0.9221 | 0.0908（不変） | 3.57（不変） | 5.77 → 5.52 ms |

AJIMEEはtop-1を維持しながら正解候補の平均順位とMinCERが改善し、GSD testは候補数を含めて不変だった。モデル未使用・準備中・失敗時は従来どおり追加探索も順位変更も行わない。ATOKの口語例`大丈夫、自信持てって`はxsmallモデルではまだ正解しないため、今回の改善済み例には数えず、商用再配布可能な意味rankerの次期評価対象として残す。

### 2026-08-09 編集位置の右文脈

[ATOKの公式説明](https://atok.com/info/features/engine.html)は、直前の確定語を機械的に優先するのではなく、日本語として自然な文章を優先する方針を示す。[azooKey/Zenz v3のprompt実装](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/93766c46e31fa6a18b7ced49dab31337780f6f45/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Zenzai/Zenz/ZenzPromptBuilder.swift)は、左文脈とは別の`U+EE07`タグでカーソル後方の右文脈を最大40文字渡す。Slimeも文章途中の編集時だけ、macOS/Windowsアダプターがカーソル両側の文書を取得し、ローカルモデルへ左末尾40文字・右先頭40文字を渡す。文書内容は保存せず、private modeでは両側とも破棄する。通常の文末入力では右文脈が空なので従来promptと順位を変えない。

UD Japanese GSDの各曖昧語について、同じ候補集合・候補数・lambda 0.7・EOS除外scoreを固定し、元文の対象語より後ろを`right_context_text`として追加した。GSD devで右文脈ありの場合だけ最小score差0.1を採用する条件を凍結し、その後testを一度だけ確認した。

| dataset | 左文脈のみ acc@1 / MRR | 両側文脈 acc@1 / MRR | top-1差 |
| --- | ---: | ---: | ---: |
| GSD dev (331) | 0.8640 / 0.9132 | **0.8761 / 0.9190** | +4件 |
| GSD test held-out (323) | 0.8700 / 0.9139 | **0.8731 / 0.9154** | +1件 |

devでは7件改善・3件悪化だった。右側の助詞や後続名詞によって`熱々の料理`、`機体の改修`、`第二選択薬`などを回収した一方、固有名詞や数値表記には誤変更も残る。したがって右文脈なしへ0.1 marginを一般化せず、右文脈が実在する編集時だけ適用する。候補生成件数は左右で同一であり、GSD testの候補生成込みp95も5.78 msと5.39 msで同じ測定帯だった。

#### 辞書変換の丁寧助動詞境界

ニューラルモデルを使わない通常候補にも右文脈を適用できるかを別途検証した。最初にMozc接続行列の終端を右文脈先頭の推定品詞へ全面的につなぎ替えたが、GSD devのtop-1は`0.8157 → 0.8006`、recall@10は`0.9909 → 0.9849`へ悪化した。読みのない右表層から先頭品詞を一意に決められず、名詞・固有名詞・数値を誤昇格するため、この方式は不採用とした。

採用した規則は、右文脈が`ます`・`ました`・`まして`・`ません`・`ましょう`のいずれかで始まり、候補が漢字を含む活用形である場合だけ、既存候補を500 cost昇格する。候補生成幅や辞書を変えず、private modeでは右文脈自体を受け取らない。GSD devで規則を固定し、held-outは各1回だけ確認した。

| dataset | 従来 acc@1 / MRR@10 | 丁寧助動詞境界 acc@1 / MRR@10 | recall@10 |
| --- | ---: | ---: | ---: |
| GSD dev (331) | 0.8157 / 0.8810 | **0.8187 / 0.8825** | 0.9909（不変） |
| GSD test held-out (323) | 0.8204 / 0.8822 | **0.8235 / 0.8837** | 0.9907（不変） |
| AJIMEE held-out (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0.8350（不変） |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0.6725（不変） |

GSD devでは`の目 → 飲め`（直後`ました`）、testでは`機器 → 聞き`（直後`ません`）をそれぞれ1件修正し、悪化はなかった。100,000回の同一プロセスマイクロベンチは右文脈あり134 µs/op、同じ読み・左文脈だけ137 µs/opで同じ測定帯だった。

#### 辞書複合語の右接続

既存の左文脈規則は、`格納｜庫`のように左表層と候補を連結した完全な辞書語を昇格する。これを右側にも対称化し、`待ち｜時間`、`和｜楽器`、`機関｜銃`のように候補とカーソル直後の表層prefixが、候補読みをprefixに持つMozc辞書語になる場合だけ既存候補を昇格する。右側は読み境界がないため、次の抑制条件を追加した。

- 候補自身が漢字を含み、右側が漢字またはカタカナで始まる場合だけ調べる。
- 姓・名POSだけで成立する連結語を証拠にしない。
- 左側に辞書句の完全一致がある場合、または左側が数値の場合は、既存の強い境界を優先して右証拠を使わない。
- 右側1文字だけの一致は辞書cost 6,300以下、2文字以上は9,000以下に限定し、最大昇格量は既存の左規則と同じ3,500とする。

全面適用ではGSD train 1,940件で41件改善・4件悪化した。`県内`が既存の`都市圏`を上書きする、`感冒`が`第1巻｜冒頭`へ割り込む、`幕間`が`膜｜間腔`へ割り込むなど、右だけを見た短い複合語が原因だった。上記gateとcostをtrain・devで固定した後、held-outを各1回確認した。

| dataset | 従来 acc@1 / MRR@10 | 右複合語 acc@1 / MRR@10 | recall@10 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.6820 / 0.7878 | **0.7000 / 0.7997** | 0.9691 → **0.9701** | 35 / 0 |
| GSD dev (331) | 0.8187 / 0.8825 | **0.8369 / 0.8933** | 0.9909（不変） | 6 / 0 |
| GSD test held-out (323) | 0.8235 / 0.8837 | **0.8545 / 0.9042** | 0.9907（不変） | 10 / 0 |
| AJIMEE held-out (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0.8350（不変） | 0 / 0 |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0.6725（不変） | 0 / 0 |

GSD testでは`大規模`、`神学校`、`核分裂`、`最下位`、`反時計`、`歯周`、`相次いで`、`化学物質`、`仮処分`、`買い物`を追加回収した。100,000回の同一プロセスマイクロベンチは右複合語あり98.2 µs/op、同じ読み・左文脈だけ91.8 µs/opで、追加は約6.4 µs/opだった。

#### 付属語と一般名詞複合語の右接続

[ATOK 2026の公式一覧](https://www.atok.com/products/)はモバイル版で「付属語を含む語の変換を強化」と説明している。Slimeでも右文脈がひらがなで始まる場合を全面許可すると、GSD devでは`来 → 気`（`気に入り`）、`氏 → 死`（`死に至る`）、`友 → 共`（`共に`）を直す一方、`話 → 話し`、`願い → 願`、`寄与 → 清`の3件を壊した。GSD trainでも少なくとも7件の既存正解を壊したため、この方式は不採用とした。

採用した境界は次のとおり。

- 結合辞書語の読みが現在の入力読みを**厳密に延長する**場合だけ使う。`ねがい → 願い`の読みと`願｜い`の結合語読みが同一なら、右表層を重複して借りない。
- 右側がひらがなの場合、結合語の左右POSが同一であることを必須とする。1文字接続は`に`かつMozcの五段動詞連用形、2文字以上は自立動詞に限定する。
- `再帰｜的`は生産的な派生接尾辞として、1文字接続の辞書cost ceilingを7,000にする。
- 逆引き索引には、3〜8字の漢字だけからなる`名詞・一般 → 名詞・サ変接続`、cost 7,500以下の2,883語を追加する。これは候補語彙ではなく文脈照合専用で、逆引きartifactの増分は120,430 bytesだった。
- 候補表層をFSTで一度たどった後、右prefixを最大8文字まで増分探索する。prefixごとに根から引き直した試作の追加73 µs/opを、追加8.9 µs/opまで削減した。

条件はGSD train・devで固定し、held-outは各1回だけ採否確認した。

| dataset | 従来 acc@1 / MRR@10 | 付属語対応 acc@1 / MRR@10 | recall@10 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.7000 / 0.7997 | **0.7031 / 0.8016** | 0.9701（不変） | 6 / 0 |
| GSD dev (331) | 0.8369 / 0.8933 | **0.8489 / 0.9003** | 0.9909（不変） | 4 / 0 |
| GSD test held-out (323) | 0.8545 / 0.9042 | 0.8545 / 0.9042 | 0.9907（不変） | 0 / 0 |
| AJIMEE held-out (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0.8350（不変） | 0 / 0 |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0.6725（不変） | 0 / 0 |

GSD trainでは`身を寄せ`、`気がつか`、`真価が問われ`、`家宅捜索`、`鼻にかけ`、`化学反応`、devでは`再帰的`、`気に入り`、`下部組織`、`死に至る`を修正した。100,000回の同一プロセスマイクロベンチは右付属語あり160.4 µs/op、同じ読み・左文脈だけ151.5 µs/opだった。

#### 兄弟姉妹語の右接続

一般の1文字右接続は、低costな`県内`などが左側の正しい境界を上書きしたため辞書cost 6,300以下に限定している。その結果、`異母｜弟`は完全な辞書語`異母弟`があるのに、そのentry cost 6,771が文脈indexから外れていた。一般閾値は広げず、2〜8字の漢字だけで構成され、`兄`・`姉`・`弟`・`妹`で終わる既存辞書語だけをcost 7,500まで逆引きindexへ追加した。追加は43 entry・39表層、`mozc-reverse.bin`・FST・reading poolの合計1,514 bytesである。右側の兄弟姉妹字の直後が別の漢字またはカタカナなら、`弟子`の途中を境界と誤認しないため1文字規則を使わない。

| dataset | 従来 acc@1 / MRR@10 | 限定index後 acc@1 / MRR@10 | 候補順の変化 | 判断 |
| --- | ---: | ---: | ---: | --- |
| GSD train (1,940) | 0.7036 / 0.8019 | 0.7036 / 0.8019 | 0件 | 不変 |
| GSD dev (331) | 0.8550 / 0.9044 | **0.8580 / 0.9059** | `イボ → 異母`の1件 | +1 / -0 |
| GSD test (323) | 0.8576 / 0.9058 | 0.8576 / 0.9058 | 0件 | 不変 |

JWTDとAJIMEEの既存fixtureは右文脈を持たないため、この規則を通らない。GSD train・testも候補表層配列と順序が全件同一で、recall@10は3 splitとも不変だった。

#### 一般名詞の並列句

[ATOKのAI用例](https://atok.com/other/support/howtouse/mac/shrd/shrd_yougo.htm)は、辞書に登録された語の組み合わせや文節間のつながりを前後文脈の候補選択に使う。SlimeのMozc辞書にも`肩や背中`、`鼻や口`のような並列句があるが、従来の右文脈indexは、ひらがなで始まるsuffixを動詞活用へ限定していたため、この証拠を使えなかった。

一般名詞POSで始まり終わる3〜8文字の句のうち、漢字列を1個の`や`または`と`で結んだcost 7,500以下の句だけを逆引きindexへ追加する。右文脈は接続詞の後ろに1文字以上の漢字を必要とし、辞書句の直後が別の漢字またはカタカナなら長い語の途中として棄却する。数値左文脈や既存の左側辞書句がある場合は従来の強い境界を優先し、候補集合は増やさない。最大昇格量は既存の辞書句と同じ3,500 costに制限する。

規則はGSD dev/testの失敗分析後に固定したため、testを未参照held-outとは扱わない。全候補を変更前の`origin/main`と比較した結果は次のとおりだった。

| dataset | 従来 acc@1 | 並列句後 acc@1 | 候補順の変化 | 判断 |
| --- | ---: | ---: | ---: | --- |
| GSD train (1,940) | 0.7036 | 0.7036 | 0件 | 不変 |
| GSD dev (331) | 0.8610 | **0.8640** | `花 → 鼻`の1件 | +1 / -0 |
| GSD test (323、診断済み) | 0.8576 | **0.8607** | `方 → 肩`の1件 | +1 / -0 |
| AJIMEE (200) | 0.5300 | 0.5300 | 0件 | 不変 |
| JWTD dev (400) | 0.2950 | 0.2950 | 0件 | 不変 |
| PUD phrase (446) | 0.6547 | 0.6547 | 0件 | 不変 |

逆引きartifact 3ファイルの合計は7,944,234 bytesから7,954,251 bytesへ10,017 bytes増えた。同一プロセス50,000回を3回測定すると、`肩`の右並列句ありは0.1533〜0.1563 ms/op、同じ読み・左文脈だけは0.1460〜0.1506 ms/opだった。各系列の中央値差は約8.2 µs/opで、range差は約3〜9 µs/opだった。

#### 一般名詞の「AのB」句

並列句と同じ逆引き経路を、一般名詞どうしを`の`で結ぶ辞書句へ限定して拡張した。対象は一般名詞POSで始まり終わる3〜8文字、漢字列の間に`の`が1個だけある、辞書cost 8,000以下の句である。右文脈が辞書句と一致し、その直後が漢字・カタカナでない語境界の場合だけ、既存候補を最大3,500 cost昇格する。候補を新しく生成せず、`未知の世界観`の途中を`未知の世界`として扱う前方一致も棄却する。

規則と閾値はGSD trainの失敗分析から決めた。GSD dev/testは既に他規則の診断にも使っているため、未参照held-outとは扱わない。変更前の候補配列との全件比較は次のとおり。

| dataset | 従来 acc@1 | 「AのB」対応後 acc@1 | 候補配列の変化 | 判断 |
| --- | ---: | ---: | ---: | --- |
| GSD train (1,940) | 0.7036 | **0.7057** | 10件（top-1は4件） | +4 / -0 |
| GSD dev (331) | 0.8640 | **0.8671** | 1件 | +1 / -0 |
| GSD test (323、診断済み) | 0.8607 | 0.8607 | 0件 | 不変 |
| AJIMEE (200) | 0.5300 | 0.5300 | 0件 | 不変 |
| JWTD dev (400) | 0.2950 | 0.2950 | 0件 | 不変 |
| PUD phrase (446、cross-encoder判定に使用済み) | 0.6547 | 0.6547 | 0件 | 不変 |

GSD trainでは`胃の調子`、`素の状態`、`真の目的`、`未知の世界`、GSD devでは`毛の根元`をtop-1へ修正した。cost上限7,500と8,000を比較し、後者による追加変化はtrainの正解top-1 3件のcost低下とdevの純改善1件だけだった。逆引きartifact 3ファイルの合計は7,954,251 bytesから8,182,120 bytesへ227,869 bytes増えた。上限7,500版からの追加は157,989 bytesである。同一プロセス50,000回を3回測定すると、右文脈ありは0.1235〜0.1247 ms/op、同じ読み・左文脈だけは0.1172〜0.1183 ms/opで、中央値差は約6.1 µs/opだった。

残存誤りの再診断では、サ変名詞どうしを結ぶ完全辞書句`番組の司会`が一般名詞POS限定のため索引外だった。一般名詞→一般名詞という既存条件は維持し、左右POSがともにサ変名詞である621 entryだけを同じcost 8,000以下・3〜8文字・単一`の`境界へ加えた。一般名詞→サ変名詞など任意組合せ4,430 entryも比較したが、top-1改善が増えないため採用しなかった。

GSD train 1,940件はacc@1が0.7082から0.7088へ上がり、`番組の｜しかい → 司会`を1件修正、悪化0だった。GSD dev 331件は候補配列まで不変で、境界固定後のGSD test 323件、AJIMEE 200件、JWTD dev 400件、PUD phrase 446件も候補表層、cost、順序まで全件完全一致した。逆引き3 artifactは8,307,446 bytesから8,337,123 bytesへ29,677 bytes（約0.36%）増加した。Apple M3、Release、50,000回を旧新交互に3組測定した`verbal_noun_genitive_context_candidates`は中央値236.522→237.322 µs/op（+0.800 µs）、非一致経路は230.876→230.746 µs/opで実質不変だった。

#### 最上級接頭辞の一文字境界

[ATOKの変換エンジン説明](https://atok.com/info/features/engine.html)が重視する単語どうしのつながりを、Slimeでは完全なMozc辞書語だけで検証する。従来の左文脈規則は、普通名詞の末尾1文字を誤って語境界と見なさないよう2文字以上を必要としていた。このため`最｜かい → 最下位`は完全辞書語であっても証拠にできなかった。Mozcが専用の最上級接頭辞POSを持つ`最`だけは1文字境界を許可し、`最 + 候補表層`の完全な逆引き辞書語の読みが現在の読みに一致する場合に限って既存候補を昇格する。

一文字境界を全漢字へ広げる案は、GSD devで`大正｜き → 正気`、`精神｜か → 神化`、`東海｜せん → 海戦`、`第一｜じ → 一時`など7件の正解top-1を壊し、acc@1を0.8671から0.8459へ悪化させたため棄却した。`最`限定ではGSD train 1,940件の`最｜かい → 下位`だけが変化し、acc@1は0.7088から0.7093へ改善、悪化0だった。GSD dev/test、AJIMEE、JWTD、PUDは候補表層、cost、順序まで完全一致し、逆引きartifactも不変だった。

Apple M3、Release、50,000回を3回測定すると、`最`一致経路の中央値は225.795→244.912 µs/op（+19.117 µs）、通常の一文字末尾非一致経路は75.284→75.638 µs/op（+0.354 µs）だった。専用POSを毎回逆引きする案は通常経路も約9 µs以上遅くしたため採用せず、同じMozc品詞境界を表す`最`の末尾判定を1回だけ行う。

#### 右側に続く一文字接頭辞

[ATOKの品詞説明](https://atok.com/other/support/howtouse/mac/shrd/shrd_hinsi_other.htm)は、接頭語を「ほかのことばの前に付けて使うことば」として独立に扱う。[Mozcの`id.def`](https://github.com/google/mozc/blob/master/src/data/dictionary_oss/id.def)でも名詞接続の接頭詞がPOS ID 2600〜2637に分離され、`未`、`再`、`非`などは個別IDを持つ。Slimeはこの構造を使い、候補が一文字の名詞接頭辞で、`候補 + 右文脈prefix`が完全なMozc辞書語になる場合だけ最大4,500 cost昇格する。

従来は左側にも完全辞書句が一つ見つかると、右側の証拠を全て棄却していた。このため`2012年8月現在｜み｜解決`では偶然辞書にある`現在見`が`見`を強め、より構造が明確な`未解決`を使えなかった。左側の証拠と競合する場合は、Mozcの汎用接頭詞ID 2600を除き、語彙が明示された一文字接頭辞だけ右側を再確認する。一般語、複数文字候補、数字直後の助数詞境界は従来どおりである。3,500では対象を回収できず、5,000まで上げる必要はなかったため、最小の4,500に固定した。

| dataset | 変更前 acc@1 | 接頭辞対応後 acc@1 | 候補配列の変化 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.7093 | **0.7098** | 8件 | 1 / 0 |
| GSD dev (331) | 0.8671 | 0.8671 | 3件 | 0 / 0 |
| GSD test (323) | 0.8824 | 0.8824 | 6件 | 0 / 0 |
| AJIMEE (200) | 0.5300 | 0.5300 | 0件 | 0 / 0 |
| JWTD dev (400) | 0.2950 | 0.2950 | 0件 | 0 / 0 |
| PUD phrase (446) | 0.6547 | 0.6547 | 0件 | 0 / 0 |

top-1が変わったのはtrainの`見 → 未`だけだった。dev/testでcostが変わった候補は全て正解表層の追加昇格で、先頭順位は変わらない。AJIMEE、JWTD、PUDは候補表層、cost、順序まで完全一致し、逆引きartifactも変更しない。

Apple M3、Release、30,000回を旧新交互に3組測定すると、左句と`未解決`が競合する対象経路の中央値は64.759→68.065 µs/op（+3.306 µs）だった。接頭辞候補がない左句経路は252.313→251.698 µs/opで測定誤差内だった。汎用品詞を事前判定に含める案は非一致経路を約9 µs遅くしたため棄却し、個別接頭辞IDの有無を先に確認する。

#### 数字直後の一般名詞複合語

従来は左文脈が数字で終わると、助数詞・日付・基数表記を保護するため右側の辞書句を一律に無効化していた。この境界では`M9｜かんせん｜道路`の`幹線道路`や`県警2｜か｜長`の`課長`まで使えない。数字境界を全面解禁すると、`8月3｜か｜から`で動詞`架から`、`六｜だい｜目`で`題目`を誤って強めたため不採用とした。

採用した規則は、左末尾がASCIIまたは全角の10進数字であり、候補と右文脈prefixを結合したMozc辞書entryの左右品詞がともに一般名詞である場合だけ、既存候補を最大3,500 cost昇格する。日本語数詞は従来の数値境界を維持し、動詞活用、助数詞、固有名詞はこの例外を通さない。

| dataset | 従来 acc@1 | 対応後 acc@1 | 候補配列の変化 |
| --- | ---: | ---: | ---: |
| GSD train 1,940件 | 0.7057 | **0.7062** | 1件改善、悪化0 |
| GSD dev 331件 | **0.8671** | **0.8671** | 0件 |
| GSD test 323件 | 0.8607 | **0.8638** | 1件改善、悪化0 |
| AJIMEE 200件 | **0.5300** | **0.5300** | 0件 |
| JWTD dev 400件 | **0.2950** | **0.2950** | 0件 |
| PUD phrase 446件 | **0.6547** | **0.6547** | 0件 |

変化したのはtrainの`県警2課長`とtestの`M9幹線道路`だけで、いずれも誤ったtop-1を正解へ直した。Apple M3、Release、50,000回を旧新交互に3組測定した`numeric_right_compound_candidates`は、旧0.298〜0.492 ms/op、新0.329〜0.634 ms/opだった。組ごとの差の中央値は約0.143 ms/opで、候補初回表示20 ms予算内にある。

#### 「する・して」が後続するサ変名詞

`長い｜して`、`関心｜する`、`94｜する`、`1｜する`のように、単独costでは強い候補が後続文法と接続できない場合が残っていた。右文脈が`する`または`して`で始まる場合だけ、現在の読みと完全一致するMozc entryから左右POSがともにサ変名詞の表層を調べる。最安表層が次点より1,500 costを超えて優勢な場合に限り、その既存一語候補を最大2,500 cost昇格する。候補は新しく生成せず、右文脈がない通常入力、ライブ変換、private modeには適用しない。

GSD train・dev・testは既に右文脈規則の診断に使っているため、未参照held-outとは扱わない。変更前の候補配列との全件比較では、trainで`適し → 敵視`を1件、testで`長い → 長居`、`関心 → 感心`、`94 → 駆使`、`1 → 位置`の4件を修正し、top-1悪化はなかった。devのtop-1は不変だった。

| dataset | 変更前 acc@1 | サ変名詞対応後 acc@1 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: |
| GSD train 1,940件 | 0.7062 | **0.7067** | 1 / 0 |
| GSD dev 331件 | **0.8671** | **0.8671** | 0 / 0 |
| GSD test 323件 | 0.8638 | **0.8762** | 4 / 0 |
| AJIMEE 200件 | **0.5300** | **0.5300** | 0 / 0 |
| JWTD dev 400件 | **0.2950** | **0.2950** | 0 / 0 |
| PUD phrase 446件 | **0.6547** | **0.6547** | 0 / 0 |

AJIMEE・JWTD・PUDは候補表層、cost、順序まで全件完全一致した。Apple M3、Release、50,000回を旧新交互に3組測定した`unique_right_suru_candidates`は、旧0.352〜0.360 ms/op、新0.355〜0.375 ms/opだった。中央値差は約0.0058 ms/opで、候補初回表示20 ms予算内にある。

#### 「する」の連用・過去・否定形

[ATOKの品詞対応表](https://atok.com/other/support/howtouse/mac/ap/pgs/ap_hinsi_atok8.htm)は名詞サ変を独立品詞として扱う。[Mozcの`id.def`](https://github.com/google/mozc/blob/master/src/data/dictionary_oss/id.def)も名詞サ変接続をID 1841、サ変・スルの活用をID 627〜638に分けている。[azooKeyの辞書形式](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Docs/dicdata_format.md)と[Viterbi更新](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Core/FullInputProcessing.swift)も、各語の左右接続IDと接続重みを変換経路へ加える。Slimeでは既存の「最安のサ変名詞表層が次点より1,500 costを超えて優勢」という条件を維持したまま、右文脈の`する`・`して`判定を`した`、`しない`、`します`、`しよう`、`しつつ`、`しながら`へ広げる。単独の`し`は直後が文末、空白、句読点の場合だけ連用形とみなす。

受動・使役を含む全活用へ広げる案も比較したが、`される`でGSD testの正解`視`がtop-10から落ちたため採用しなかった。`せず`も`嫌な顔せず`のように一般名詞へ続くため対象外とした。候補生成、昇格量、曖昧なサ変名詞を動かさない条件は変更しない。

| dataset | 変更前 acc@1 | 活用拡張後 acc@1 | 候補配列の変化 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.7098 | **0.7113** | 18件 | 3 / 0 |
| GSD dev (331) | 0.8671 | 0.8671 | 6件 | 0 / 0 |
| GSD test (323) | 0.8824 | 0.8824 | 3件 | 0 / 0 |
| AJIMEE (200) | 0.5300 | 0.5300 | 0件 | 0 / 0 |
| JWTD dev (400) | 0.2950 | 0.2950 | 0件 | 0 / 0 |
| PUD phrase (446) | 0.6547 | 0.6547 | 0件 | 0 / 0 |

GSD trainでは`全身し → 前進し`、`即しない → 即死しない`、`大尉した → 退位した`の3件を修正した。dev/testでcostが変わった候補はすべて元から正解top-1のサ変名詞で、正解順位は変わらない。AJIMEE、JWTD、PUDは候補表層、cost、順序まで完全一致した。Apple M3、Release、50,000回を旧新交互に3組測定すると、`した`対象経路の中央値は173.443→174.235 µs/op（+0.792 µs）、非該当の`しか`経路は116.762→116.857 µs/op（+0.095 µs）だった。

#### 「こと・もの・ため・ので・よう」への右品詞接続

[ATOKの変換エンジン説明](https://atok.com/info/features/engine.html)は、一般的な文法と単語同士のつながりを辞書へ取り入れ、自然な文章を優先すると説明している。[azooKeyの辞書形式](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Docs/dicdata_format.md)も各entryに左右接続IDを保持し、[通常変換のViterbi更新](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Core/FullInputProcessing.swift)で前件の`rcid`と後件の`lcid`の接続重みを加える。Slimeでは右文脈先頭が`こと`、`もの`、`ため`、`ので`、`よう`の場合だけ、[Mozcの`id.def`](https://github.com/google/mozc/blob/master/src/data/dictionary_oss/id.def)にある非自立名詞・接続助詞などの左POSと、現在候補の右POSとの接続costを比較する。

語彙costは再加算せず、最良接続との差だけを最大1,100 costの補正へ変換する。同じ活用型の表層には同じ補正がかかるため、`負う`と`追う`のような意味差を品詞規則だけで逆転しない。候補は生成せず、一語exact候補だけを対象とする。完全辞書句による右文脈補正も存在する場合は大きい方だけを使い、証拠を二重加算しない。

丁寧助動詞も同時に再評価した。漢字を含む活用形への一律補正は500から1,000へ、一意な後続接続がある場合は`ます`系に限り1,500から2,000へ広げた。汎用の一意接続補正を2,500へ広げる案は`診察です → 新札です`を起こしたため棄却した。

| dataset | 変更前 acc@1 / MRR@10 | 右品詞接続後 acc@1 / MRR@10 | 候補配列の変化 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.7113 / 0.8072 | **0.7134 / 0.8083** | 54件 | 4 / 0 |
| GSD dev (331) | 0.8671 / 0.9104 | 0.8671 / 0.9104 | 4件 | 0 / 0 |
| GSD test (323) | 0.8824 / 0.9230 | **0.8854 / 0.9246** | 9件 | 1 / 0 |
| AJIMEE (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0件 | 0 / 0 |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0件 | 0 / 0 |
| PUD phrase (446) | 0.6547 / 0.7390 | 0.6547 / 0.7390 | 0件 | 0 / 0 |

trainでは`夜こと → 寄ること`、`得こと → 解くこと`、`近います → 誓います`、`秋ません → 飽きません`を修正した。testでは`火曜こと → 通うこと`を修正した。`王もの → 追うもの`は名詞から動詞へ移るが、正解の`負う`との意味差は残る。AJIMEE、JWTD、PUDは候補表層、cost、順序まで完全一致した。

Apple M3、Release、50,000回、warmup 1,000回を旧新交互に5組測定した。対象経路`right_function_word_candidates`の中央値は106.383→110.333 µs/op（+3.950 µs）、非該当経路`right_function_word_nonmatch`は109.050→108.894 µs/op（-0.156 µs、測定誤差内）だった。文法語のPOSは変換ごとの辞書検索ではなく、同梱Mozc接続行列と同じ名前付きIDスライスから取得する。

#### 活用接続の互換候補グループ

[ATOKの変換エンジン説明](https://atok.com/info/features/engine.html)は一般的な文法と単語のつながりを変換へ使い、[ATOK 2026の機能説明](https://atok.com/features/)も助詞・助動詞を含む文の変換改善を挙げている。[azooKeyの辞書形式](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Docs/dicdata_format.md)は各entryに`lcid`と`rcid`を保持し、[通常変換のViterbi更新](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Core/FullInputProcessing.swift)で直前entryの`rcid`と次entryの`lcid`に対応する接続重みを加える。Slimeにも一意な右文法接続を昇格する規則はあったが、同じ活用型の候補が複数ある場合は証拠を捨てるため、`竹ている`、`訳て`、`今でる`、`新でしまった`のように名詞候補が活用語尾の直前へ残っていた。

右文脈が活用語尾または接続助詞`た・て`で始まる場合、現在の読みに完全一致する漢字候補の右POSから、Mozc接続行列上の後続POSへのcostを比較する。最良接続との差が1,000以内の候補を「文法的に互換」として同じ1,500 costだけ昇格し、それ以外は動かさない。生の接続costで互換候補同士の意味順位を決めず、既存の一意接続補正がある場合も大きい方だけを使うため、文法証拠の二重加算はしない。

`で`は格助詞・助動詞との曖昧性が大きいため、`でいる・でおく・でくる・でしまう・でみる・でもらう・であげる・でほしい・でる・でた・でます`に相当する後続だけを接続助詞として扱う。`機器での音楽`の格助詞、`海であった`などのコピュラ、既存の丁寧語規則、`待機させていた`で悪化した`させ`は対象外にした。昇格上限は1,000、1,500、2,000を比較し、1,000はtrainの改善が1件少なく、2,000は`差し`の正解順位を1位から4位へ落としたため、非回帰だった1,500を採用した。

| dataset | 変更前 acc@1 / MRR@10 | 互換接続後 acc@1 / MRR@10 | 候補配列の変化 | 正解順位改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.7191 / 0.8120 | **0.7273 / 0.8172** | 251件 | 27 / 0 |
| GSD dev (331) | 0.8671 / 0.9104 | 0.8671 / **0.9115** | 28件 | 3 / 0 |
| GSD test (323) | 0.8854 / 0.9246 | **0.8916 / 0.9292** | 35件 | 4 / 0 |
| AJIMEE (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0件 | 0 / 0 |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0件 | 0 / 0 |
| PUD phrase (446) | 0.6547 / 0.7390 | 0.6547 / 0.7390 | 2件 | 0 / 0 |

GSD trainではtop-1正解数が1,395→1,411、testでは286→288へ増えた。代表例は`竹ている → 長けている`、`訳て → 分けて`、`今でる → 混んでる`、`新でしまった → 死んでしまった`である。AJIMEEとJWTDは候補表層、cost、順序まで完全一致し、PUDの2件もtop-1と正解順位は変わらない。

Apple M3、Release、5,000回、warmup 1,000回を旧新交互に10組測定した。対象経路`compatible_right_grammar_candidates`の中央値は99.231→102.520 µs/op（+3.289 µs、+3.3%）、非該当の`compatible_right_grammar_nonmatch`は188.438→186.026 µs/op（-2.412 µs、測定誤差内）だった。

#### 強い左辞書句と一般境界の競合

[ATOKの変換エンジン説明](https://atok.com/info/features/engine.html)は文法だけでなく単語同士のつながりと文章の自然さを使う。[azooKeyの辞書形式](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Docs/dicdata_format.md)と[通常変換のViterbi更新](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Core/FullInputProcessing.swift)も、語彙costと左右接続costを同じ経路で評価する。Slimeは左表層と候補を連結した完全なMozc辞書句を候補別に昇格していたが、辞書句を持たない競合候補には一般境界補正が残るため、`劇団☆新感線`が`劇団☆新幹線`に、`犬猿の仲`が`犬猿の中`に戻されていた。

一般境界を全辞書句で止める案は、実在する`グループ展`、`代表権`、`研究員`を過信し、正解`店`、`兼`、`院`の順位を落とした。右辞書句まで同様に扱う案も`膜間腔`を`幕間腔`へ変えたため棄却した。採用条件は左辞書句だけとし、最大8文字の左prefixが助詞`の`で終わるか非英数字の明示境界を含み、完全辞書句の補正が上限3,500に達する場合、または`の`境界で2,000以上の場合に限定した。この強い証拠がある変換だけ、辞書句を持たない競合候補の一般境界補正を止める。

事前判定は同じ表層の複数POS entryをまとめ、最大3,500の補正後も最良候補へ届かない表層を辞書検索前に除外する。候補生成、N-best幅、辞書costは変えず、右辞書句と一般境界の既存関係も維持する。

| dataset | 変更前 acc@1 / MRR@10 | 強い左辞書句優先後 acc@1 / MRR@10 | 候補配列の変化 | 正解順位改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.7273 / 0.8172 | **0.7284 / 0.8177** | 30件 | 2 / 0 |
| GSD dev (331) | 0.8671 / 0.9115 | 0.8671 / 0.9115 | 11件 | 0 / 0 |
| GSD test (323) | 0.8916 / 0.9292 | 0.8916 / 0.9292 | 6件 | 0 / 0 |
| AJIMEE (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0件 | 0 / 0 |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0件 | 0 / 0 |
| PUD phrase (446) | 0.6547 / 0.7390 | 0.6547 / 0.7390 | 0件 | 0 / 0 |

GSD trainのtop-1正解数は1,411→1,413へ増え、正解順位が変わったのは`犬猿の中 → 犬猿の仲`と`劇団☆新幹線 → 劇団☆新感線`の2件だけだった。dev、testは候補costに変化があるものの正解順位は全件不変で、AJIMEE、JWTD、PUDは候補表層、cost、順序まで完全一致した。

Apple M3、Release、5,000回、warmup 1,000回を旧新交互に10組測定した。強い辞書句と一般境界が競合する`strong_left_phrase_conflict`の中央値は773.630→789.011 µs/op（+15.381 µs、+2.0%）、事前gateで辞書句探索を省く`weak_left_phrase_conflict`は90.470→90.257 µs/op（-0.213 µs、測定誤差内）だった。

#### 一意な左辞書句の追加昇格

[ATOKの変換エンジン説明](https://atok.com/info/features/engine.html)は、単語のつながりと統計的な文脈を組み合わせて文章として自然な候補を選ぶ。[ATOK Hyper Hybrid Engine 2](https://atok.com/features/hhe2/)は、学習済み候補より文脈上自然な候補を優先する場合があることも明示している。[azooKeyの辞書形式](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Docs/dicdata_format.md)と[通常変換のViterbi更新](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Core/FullInputProcessing.swift)でも、語彙costと左右接続costを同じ経路で扱う。

Slimeの通常の左辞書句補正は最大3,500で、`資金｜なん`、`三國｜し`、`オクタン｜か`、`遠野｜しき`のように完全な辞書句が一つだけ存在しても、孤立時に高頻度な同音候補へ届かない場合があった。そこで、現在の読みと左文脈を連結する完全辞書句の表層が一意で、右文脈に別の完全辞書句がない場合だけ、cost ceilingを11,500、補正上限を5,200へ広げた。`価値観`と`価値感`のように複数の完全辞書句が競合する場合は、元の辞書cost差と3,500上限を維持する。分割変換の表層、候補生成、N-best幅、word costは変更しない。

また、左文脈末尾の`御`を無条件に敬語接頭辞と扱っていたため、`防御｜かき`が辞書語`御垣`の証拠を誤って得ていた。直前文字が漢字なら`御`を語中とみなし、単独の`御`、`お`、`ご`だけを既存の敬語境界として残した。

| dataset | 変更前 acc@1 / MRR@10 | 一意句昇格後 acc@1 / MRR@10 | 候補表層順の変化 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.7309 / 0.8191 | **0.7330 / 0.8204** | 5件 | 4 / 0 |
| GSD dev (331) | 0.8671 / 0.9115 | 0.8671 / 0.9115 | 0件 | 0 / 0 |
| GSD test (323) | 0.8947 / 0.9331 | 0.8947 / 0.9331 | 0件 | 0 / 0 |
| AJIMEE (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0件 | 0 / 0 |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0件 | 0 / 0 |
| PUD phrase (446) | 0.6547 / 0.7390 | 0.6547 / 0.7390 | 0件 | 0 / 0 |

GSD trainのtop-1正解数は1,418→1,422へ増えた。5件目の候補表層変化は`防御｜かき`から不正な`垣`がtop-10外へ下がったもので、正解順位の悪化はない。Apple M3、Release、GSD train 1,940件を旧新交互に8組測定した中央値はp50 0.5353→0.5396 ms（+0.0044 ms、+0.8%）、p95 1.9767→1.9755 ms（-0.0012 ms、測定誤差内）だった。完全辞書句が見つかった場合だけ競合句を調べる遅延判定にし、非該当入力の先行走査を避けている。

#### 外来語幹に続く一文字の一般名詞

[ATOKの変換エンジン説明](https://atok.com/info/features/engine.html)は単語単体の頻度だけでなく、単語同士のつながりと文章の自然さを変換に利用する。[azooKeyの辞書形式](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Docs/dicdata_format.md)と[Viterbi更新](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Core/FullInputProcessing.swift)も語彙costと左右接続costを同じ経路で扱う。SlimeのMozc辞書には`リステリア菌`が完全語として存在するが、末尾の`菌`は生産的な名詞接尾語POSではなく一般名詞POSであるため、従来の文脈用逆引き索引から除外され、`米国でリステリア + きん`では孤立costの低い`金`が優先されていた。

対象をカタカナ2〜8文字と一文字の漢字からなる完全辞書語に限定し、完全語と末尾語がともに一般名詞POS、完全語の辞書costが7,500以下の場合だけ文脈用逆引き索引へ加えた。候補生成、N-best幅、既存word costは変更しない。cost上限を8,000まで広げる実験では追加改善がなかったため、より狭い7,500を採用した。

GSD trainではtop-1正解数が1,413→1,414、acc@1が0.7284→0.7289、MRR@10が0.8177→0.8180へ増えた。候補配列が変わったのは`米国でリステリア + きん`のtop-1が`金 → 菌`となった1件だけで、正解順位の悪化は0件だった。GSD dev/test、AJIMEE、JWTD、PUDは候補表層、cost、順序まで完全一致した。recall@10も0.9711で不変であり、候補追加ではなく既存候補の文脈順位改善である。

逆引きFST・block・reading資材の合計増加は3,763 bytesだった。Apple M3、Release、5,000回、warmup 1,000回を旧新交互に10組測定した。該当する`katakana_general_noun_phrase`の中央値は198.645→194.609 µs/op（-4.036 µs、測定誤差内）、非該当の`katakana_general_noun_phrase_miss`は203.677→204.074 µs/op（+0.398 µs、+0.2%、測定誤差内）だった。

#### 外来語幹と短い漢字語尾からなる複合語

一文字語尾だけでなく、`アマチュア天文家`のようにカタカナ語幹と2〜3文字の漢字語尾からなる完全辞書語も、従来の文脈用逆引き索引には含まれない場合があった。対象を全長4〜9文字、先頭のカタカナ幹2〜8文字、末尾の漢字2〜3文字、最終文字が生産的な名詞接尾辞、完全語の左右POSが一般名詞から一般名詞接尾語、word cost 8,400以下に限定した。該当する92 entryのうち、既存のreconversion cost帯を超える90 entryだけが実質的な追加対象である。候補やword costは追加せず、文脈用の逆引き索引だけを拡張する。

GSD testでは`アマチュア天文｜か`が`化 → 家`となり、top-1正解数は289→290、acc@1は0.8947→0.8978、MRR@10は0.9331→0.9347へ改善した。候補表層、cost、順序が変わったのはこの1件だけで、GSD train/dev、AJIMEE、JWTD、PUDは完全一致した。逆引きFST・block・reading資材の合計増加は3,105 bytesである。

Apple M3、Release、GSD train 1,940件を旧新交互に8組測定した中央値はp50 0.2158→0.2154 ms（-0.0004 ms）、p95 0.5195→0.5178 ms（-0.0017 ms）で、いずれも測定誤差内だった。

#### サ変名詞に続く一文字の一般名詞接尾語

[ATOKの変換エンジン説明](https://atok.com/info/features/engine.html)が重視する文法と単語のつながり、[azooKeyの辞書形式](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Docs/dicdata_format.md)と[Viterbi更新](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Core/FullInputProcessing.swift)が使う左右接続IDを参考に、Mozc辞書にある完全な三文字複合語を文脈証拠として使う範囲を広げた。対象は全て漢字、左POSがサ変名詞、右POSが一般名詞接尾語、辞書cost 7,550以下を同時に満たすentryだけである。候補やword costは追加せず、文脈用の逆引き索引だけを拡張する。

この境界により`大勢の信者が傍聴｜けん`を`権 → 券`へ修正した。根拠となる辞書entryは`ぼうちょうけん / 傍聴券 / 1841 / 1949 / 7502`である。一般名詞接尾語をcost 8,000まで広げる案はartifactを約193 KB増やし、三文字漢字・サ変名詞へ絞ってもcost 7,850ではheld-outの誤候補`監査員`を`監査院`に対して強めた。7,550なら`傍聴券`を残しつつ、`罵倒語`（7,820）と`監査員`（7,589）を索引へ入れないため、この最小帯で固定した。

助詞の右POS接続も先に比較した。`が・を・に・へ`などを広く使う案はGSD trainで10件改善・5件悪化、対象を狭めても6件改善・2件悪化し、`下記をご覧 → 書きをご覧`、`件について → 県について`、`追うが → 王が`などを起こした。助詞は文法的に接続できる意味候補が多く、品詞だけでは選べないため採用していない。

| dataset | 変更前 acc@1 / MRR@10 | 接尾語帯追加後 acc@1 / MRR@10 | 候補配列の変化 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.7134 / 0.8083 | **0.7139 / 0.8086** | 1件 | 1 / 0 |
| GSD dev (331) | 0.8671 / 0.9104 | 0.8671 / 0.9104 | 0件 | 0 / 0 |
| GSD test (323) | 0.8854 / 0.9246 | 0.8854 / 0.9246 | 0件 | 0 / 0 |
| AJIMEE (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0件 | 0 / 0 |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0件 | 0 / 0 |
| PUD phrase (446) | 0.6547 / 0.7390 | 0.6547 / 0.7390 | 0件 | 0 / 0 |

逆引きartifactは5,516 bytes増えた。Apple M3、Release、10,000回、warmup 2,000回を旧新の順序も反転して10組測定した中央値は、一致経路が169.158→170.139 µs/op（+0.981 µs、+0.58%）、非一致経路が162.978→164.388 µs/op（+1.410 µs、+0.87%）だった。変換品質を最優先する現在の方針では許容するが、1%未満でも継続して性能予算へ記録する。

#### 「第n代」と通常の「n台」の区別

[ATOKの数値入力支援](https://atok.com/other/support/howtouse/mac/ip/pgs/ip_num_assist.htm)と[azooKeyのSpecial Conversion](https://github.com/azooKey/AzooKeyKanaKanjiConverter/tree/main/Sources/KanaKanjiConverterModule/ConverterAPI/SpecialConversion)にならい、数値表記を一般の意味順位とは別の構造として扱う。従来は整数直後の`だい`を常に`台`へ寄せていたため、序数接頭辞を含む`第33｜だい`まで`台`になっていた。末尾の整数を1回だけ解析し、直前が`第`なら`代`、それ以外の整数なら従来どおり`台`とする。小数、負数、`第1.5`には適用しない。

[Mozcの品詞定義](https://github.com/google/mozc/blob/master/src/data/dictionary_oss/id.def)にある一般助数詞と`つ・周・回・時・月・次・階`の専用IDを全て数値へ接続する案も比較した。しかし、数値だけでは`1字`と`一時`、`一突き`と`一月`を区別できず、いずれも誤った専用助数詞を強めたため不採用とした。`二階`と`二回`のように両方が完全な辞書語である場合も、周辺の意味文脈なしでは決めない。

| dataset | 変更前 acc@1 / MRR@10 | 序数区別後 acc@1 / MRR@10 | 候補配列の変化 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.7139 / 0.8086 | **0.7144 / 0.8090** | 1件 | 1 / 0 |
| GSD dev (331) | 0.8671 / 0.9104 | 0.8671 / 0.9104 | 0件 | 0 / 0 |
| GSD test (323) | 0.8854 / 0.9246 | 0.8854 / 0.9246 | 0件 | 0 / 0 |
| AJIMEE (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0件 | 0 / 0 |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0件 | 0 / 0 |
| PUD phrase (446) | 0.6547 / 0.7390 | 0.6547 / 0.7390 | 0件 | 0 / 0 |

変化したのはGSD trainの`第33代`だけで、`台 → 代`へ修正した。artifactは増えない。Apple M3、Release、5,000回、warmup 1,000回を旧新の順序も反転して10組測定した中央値は、`第33代`経路が57.035→57.151 µs/op（+0.116 µs）、通常の`33台`経路が51.980→52.106 µs/op（+0.126 µs）で、いずれも約0.2%の測定差だった。

#### 割合直後の「増・減」

数値と記号を一般語から分離する同じ方針で、ASCIIまたは全角の`%`直後に入力した`ぞう`と`げん`を、それぞれ`増`と`減`へ寄せる。`%`の直前には整数、小数、全角数字、漢数字のいずれかが必要で、負数や数字のない`%`には適用しない。候補生成やword costは変更せず、既存候補の構造化表記補正だけを使う。

| dataset | 変更前 acc@1 / MRR@10 | 割合増減後 acc@1 / MRR@10 | 候補配列の変化 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.7144 / 0.8090 | **0.7149 / 0.8093** | 1件 | 1 / 0 |
| GSD dev (331) | 0.8671 / 0.9104 | 0.8671 / 0.9104 | 0件 | 0 / 0 |
| GSD test (323) | 0.8854 / 0.9246 | 0.8854 / 0.9246 | 0件 | 0 / 0 |
| AJIMEE (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0件 | 0 / 0 |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0件 | 0 / 0 |
| PUD phrase (446) | 0.6547 / 0.7390 | 0.6547 / 0.7390 | 0件 | 0 / 0 |

変化したのはGSD trainの`0.5%減`だけで、`源 → 減`へ修正した。artifactは増えない。Apple M3、Release、5,000回、warmup 1,000回を旧新の順序も反転して10組測定した中央値は、一致経路が41.036→41.838 µs/op（+0.802 µs）、負数による非一致経路が38.611→38.764 µs/op（+0.153 µs）だった。

#### 整数直後の「匹・対」

[ATOKの数値入力支援](https://atok.com/other/support/howtouse/mac/ip/pgs/ip_num_assist.htm)と[azooKeyのSpecial Conversion](https://github.com/azooKey/AzooKeyKanaKanjiConverter/tree/main/Sources/KanaKanjiConverterModule/ConverterAPI/SpecialConversion)と同様、数値を一般語とは別の構造として扱う。ASCII・全角・漢数字の整数直後だけ、`ひき`を`匹`、`つい`を`対`へ寄せる。小数、負数、単独変換には適用しない。`回・階`、`席・隻`、`か月・カ月・ヶ月`のように意味または表記方針が必要な助数表現は、数値だけでは決めない。

| dataset | 変更前 acc@1 / MRR@10 | 匹・対対応後 acc@1 / MRR@10 | 候補配列の変化 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.7149 / 0.8093 | **0.7160 / 0.8098** | 2件 | 2 / 0 |
| GSD dev (331) | 0.8671 / 0.9104 | 0.8671 / 0.9104 | 0件 | 0 / 0 |
| GSD test (323) | 0.8854 / 0.9246 | 0.8854 / 0.9246 | 0件 | 0 / 0 |
| AJIMEE (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0件 | 0 / 0 |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0件 | 0 / 0 |
| PUD phrase (446) | 0.6547 / 0.7390 | 0.6547 / 0.7390 | 0件 | 0 / 0 |

GSD trainの`42匹`を`引き → 匹`、`4対`を`付い → 対`へ修正した。他5データセットは候補表層、cost、順序まで完全一致し、artifactは増えない。Apple M3、Release、5,000回、warmup 1,000回を旧新の順序も反転して10組測定した中央値は、一致経路が96.676→96.257 µs/op（-0.419 µs、測定誤差内）、小数による非一致経路が97.795→97.889 µs/op（+0.094 µs）だった。

#### 前後構造で決めるスポーツ記録・数値単位

[ATOKの数値入力支援](https://atok.com/other/support/howtouse/mac/ip/pgs/ip_num_assist.htm)と[azooKeyのSpecial Conversion](https://github.com/azooKey/AzooKeyKanaKanjiConverter/tree/main/Sources/KanaKanjiConverterModule/ConverterAPI/SpecialConversion)を参考に、数値だけでは曖昧な助数表現を前後の構造が揃う場合だけ補正する。`n勝n｜はい`は`敗`、0〜2の`n｜し`に塁上表記が続く場合は`死`、整数直後の`げん`に弦楽器名が続く場合は`弦`、整数直後の`せき`に船舶名が続く場合は`隻`とする。スポーツ記録はASCII・全角・漢数字に対応する。

数値だけの`2はい`、3アウト後の塁上表記、`7げん件`、`3せきの座席`には適用しない。既存候補だけを最大4,000 cost昇格し、候補生成、word cost、辞書artifactは変更しない。

| dataset | 変更前 acc@1 / MRR@10 | 前後構造対応後 acc@1 / MRR@10 | 候補配列の変化 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.7160 / 0.8098 | **0.7180 / 0.8114** | 4件 | 4 / 0 |
| GSD dev (331) | 0.8671 / 0.9104 | 0.8671 / 0.9104 | 0件 | 0 / 0 |
| GSD test (323) | 0.8854 / 0.9246 | 0.8854 / 0.9246 | 0件 | 0 / 0 |
| AJIMEE (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0件 | 0 / 0 |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0件 | 0 / 0 |
| PUD phrase (446) | 0.6547 / 0.7390 | 0.6547 / 0.7390 | 0件 | 0 / 0 |

GSD trainの`1死一、二塁`、`2勝2敗`、`7弦ギター`、`3隻の客船`を修正した。他5データセットは候補表層、cost、順序まで完全一致した。Apple M3、Release、5,000回、warmup 1,000回を旧新の順序も反転して10組測定した中央値は、弦楽器一致経路が56.052→56.375 µs/op（+0.324 µs）、同じ読みで右文脈が非一致の経路が55.729→56.111 µs/op（+0.383 µs）だった。

#### 数字同士を結ぶ「対」

[ATOKの数値入力支援](https://atok.com/other/support/howtouse/mac/ip/pgs/ip_num_assist.htm)は数値表記を通常語とは別の入力支援として扱う。[azooKey](https://github.com/azooKey/AzooKeyKanaKanjiConverter/tree/main/Sources/KanaKanjiConverterModule/ConverterAPI/SpecialConversion)も桁区切り、日時、時刻などを独立したSpecial Conversionとして実装する。Slimeでも一般の`たい`候補costを変えず、左末尾と右先頭がともに整数である`1｜たい｜1`型だけを構造化表記として扱う。

ASCII・全角・漢数字に対応し、既存候補の`対`を最大3,000 cost昇格する。小数、片側だけが数値、右文脈がない通常入力、ライブ変換、private modeは対象外である。

| dataset | 変更前 acc@1 | 数字間の「対」対応後 acc@1 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: |
| GSD train 1,940件 | 0.7067 | **0.7072** | 1 / 0 |
| GSD dev 331件 | **0.8671** | **0.8671** | 0 / 0 |
| GSD test 323件 | 0.8762 | **0.8793** | 1 / 0 |
| AJIMEE 200件 | **0.5300** | **0.5300** | 0 / 0 |
| JWTD dev 400件 | **0.2950** | **0.2950** | 0 / 0 |
| PUD phrase 446件 | **0.6547** | **0.6547** | 0 / 0 |

変化したのはtrainとtestの`1対1`だけで、いずれも`体 → 対`へ修正した。devは候補配列まで不変、AJIMEE・JWTD・PUDも候補表層、cost、順序まで全件完全一致した。Apple M3、Release、50,000回を旧新交互に3組測定した`numeric_score_notation_candidates`は旧0.0909〜0.0917 ms/op、新0.0913〜0.0924 ms/opで、中央値差は約0.00028 ms/opだった。

#### 「第N期終了」の両側構造

序数の`第N`だけでは`第N期`と`第N機能`を区別できないため、左側の数値だけで`き`を補正しない。左文脈が`第`とASCII・全角・漢数字の整数で終わり、読みが`き`、右文脈が`終了`から始まる場合だけ、既存候補の`期`を構造化表記として昇格する。右文脈がない通常入力や、`機能`など別の右文脈では既存順位を維持する。

GSD testの`第1｜き｜終了後`だけが`機 → 期`へ改善し、acc@1は0.8978→0.9009、MRR@10は0.9347→0.9362となった。GSD train/dev、AJIMEE、JWTD、PUDは候補表層、cost、順序まで完全一致し、testも残り322件は完全一致した。Apple M3、Release、GSD train 1,940件を旧新交互に8組測定した中央値はp50 0.2149→0.2154 ms（+0.0005 ms、+0.2%）、p95 0.5181→0.5178 ms（-0.0002 ms）で測定誤差内だった。

#### 再変換語彙と文脈証拠の人名境界

明示的再変換の逆引き索引は、人名を含む表層を保持する。一方、同じ索引を左文脈の完全辞書語判定にも使っていたため、一般語の`諏訪湖`と人名の`諏訪子`を同種の競合phraseとして扱い、`諏訪｜こ`で`湖`を一意に強められなかった。

逆引きrecordのword cost上位1 bitへ「非人名POSの証拠あり」を格納し、byte数を増やさず二つの用途を分離した。再変換と人名phraseの通常promotionは従来どおり全recordを使う。候補が非人名phraseである場合の一意性判定だけ、姓・名POSしか持たない競合を数えない。これにより`諏訪湖`を修正しつつ、`藤本博史`、`渡邉恒雄`、`遠野志貴`など既存の人名順位と、`諏訪子`の再変換readingを保持する。

| dataset | 変更前 acc@1 / MRR@10 | 用途分離後 acc@1 / MRR@10 | 候補配列の変化 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train 1,940件 | 0.7330 / 0.8204 | **0.7335 / 0.8207** | 1件 | 1 / 0 |
| GSD dev 331件 | 0.8671 / 0.9115 | 0.8671 / 0.9115 | 0件 | 0 / 0 |
| GSD test 323件 | 0.9009 / 0.9362 | 0.9009 / 0.9362 | 0件 | 0 / 0 |
| AJIMEE 200件 | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0件 | 0 / 0 |
| JWTD dev 400件 | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0件 | 0 / 0 |
| PUD 446件 | 0.6547 / 0.7390 | 0.6547 / 0.7390 | 0件 | 0 / 0 |

逆引き3 artifactは旧新とも2,347,447 / 2,177,418 / 3,829,769 bytesで増分なし。GSD trainを旧新交互に8組測定した中央値はp50 0.2226→0.2241 ms（+0.0015 ms、+0.7%）、p95 0.5318→0.5327 ms（+0.0009 ms、+0.2%）で同じ測定帯だった。

#### 低頻度の一般名詞複合語

明示的な再変換に使う逆引き索引は辞書cost 6,500以下に絞っているため、Mozc辞書に存在しても少し低頻度な複合語を文脈証拠として参照できない場合があった。候補やword costは変更せず、左右POSがともに一般名詞、3〜8字の漢字列、cost 7,000以下をすべて満たす2,192 entryだけを文脈用逆引き索引へ加えた。

| dataset | 変更前 acc@1 | 一般名詞帯追加後 acc@1 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: |
| GSD train 1,940件 | **0.7072** | **0.7072** | 0 / 0 |
| GSD dev 331件 | **0.8671** | **0.8671** | 0 / 0 |
| GSD test 323件 | 0.8793 | **0.8824** | 1 / 0 |
| AJIMEE 200件 | **0.5300** | **0.5300** | 0 / 0 |
| JWTD dev 400件 | **0.2950** | **0.2950** | 0 / 0 |
| PUD phrase 446件 | **0.6547** | **0.6547** | 0 / 0 |

GSD testの`線形作用｜そ`だけが`祖 → 素`へ改善した。trainとdevのtop-1は不変で、AJIMEE・JWTD・PUDは候補表層、cost、順序まで全件完全一致した。逆引き3 artifactの合計は8,182,120 bytesから8,259,148 bytesへ77,028 bytes（約0.94%）増加した。Apple M3、Release、50,000回を旧新交互に3組測定した`general_noun_context_candidates`は旧25.961〜26.209 µs/op、新26.335〜26.496 µs/opで、中央値差は約0.252 µs/opだった。非一致文脈は中央値18.275→18.246 µs/opで実質不変だった。

残存誤りをtrain/devだけで再診断し、上限7,200と7,500を比較した。7,200では1,277 entryを追加し、左文脈の`三陸｜おき → 三陸沖`と右文脈の`叙正三位｜くん｜一等… → 勲`を修正した。7,500はさらに1,803 entryを加えてもtop-1改善が増えなかったため、最小境界の7,200に固定した。

| dataset | cost 7,000 acc@1 | cost 7,200 acc@1 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: |
| GSD train 1,940件 | 0.7072 | **0.7082** | 2 / 0 |
| GSD dev 331件 | **0.8671** | **0.8671** | 0 / 0 |
| GSD test 323件 | **0.8824** | **0.8824** | 0 / 0 |
| AJIMEE 200件 | **0.5300** | **0.5300** | 0 / 0 |
| JWTD dev 400件 | **0.2950** | **0.2950** | 0 / 0 |
| PUD phrase 446件 | **0.6547** | **0.6547** | 0 / 0 |

境界固定後、GSD test・AJIMEE・JWTD・PUDは候補表層、cost、順序まで全件完全一致した。逆引き3 artifactは8,259,148 bytesから8,307,446 bytesへ48,298 bytes（約0.58%）増加した。Apple M3、Release、50,000回を旧新交互に3組測定すると、`lower_frequency_general_noun_left_candidates`は中央値140.004→140.110 µs/op（+0.106 µs）、`lower_frequency_general_noun_right_candidates`は66.349→66.651 µs/op（+0.302 µs）だった。

#### 距離・所要時間に続く「圏内・圏外」

右側の短い辞書語だけを優先すると、低costな`県内`が`徒歩10分｜圏内`を上書きする。一般の右複合語閾値は広げず、左側が数値と距離・時間単位（`秒`・`分`・`時間`・`駅`・メートル/キロメートル表記）で終わり、読みが`けん`、右文脈が`内`または`外`で始まる場合だけ、到達範囲の`圏`を既存候補内で優先する。`福岡｜県内`のように計測表現がない行政区域は従来順位のままにする。

条件はGSD train・devで固定した。GSD devの`大阪駅から徒歩10分｜けん｜内`を`県`から`圏`へ修正し、trainでは候補順の変化がなかった。固定後に確認したGSD test、AJIMEE、JWTDも全指標不変だった。

| dataset | 従来 acc@1 / MRR@10 | 計測範囲対応 acc@1 / MRR@10 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: |
| GSD train (1,940) | 0.7036 / 0.8019 | 0.7036 / 0.8019 | 0 / 0 |
| GSD dev (331) | 0.8580 / 0.9059 | **0.8610 / 0.9074** | 1 / 0 |
| GSD test (323) | 0.8576 / 0.9058 | 0.8576 / 0.9058 | 0 / 0 |
| AJIMEE (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0 / 0 |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0 / 0 |

100,000回の同一プロセスマイクロベンチは右文脈あり181.3 µs/op、同じ読み・左文脈だけ182.3 µs/opで同じ測定帯だった。通常の文末入力とprivate modeでは右文脈が渡らないため、この規則を通らない。

#### 文脈と単発履歴の競合

[ATOKの変換強度](https://atok.com/info/features/engine.html)が示す「直前に選んだ語より自然な文章を優先する」境界を、Slimeの既存の定着/一時履歴へ追加した。周辺文脈による辞書順位と文脈なし順位の1位が異なり、5回未満で2回確認も済んでいない単発履歴が現在の辞書候補集合にも存在するときだけ、文脈1位を単発履歴より前へ置く。履歴候補自体は候補一覧に残す。

5回以上の定着履歴、2回確認済みの普段の表記、同じ直前文脈で2回以上選んだ履歴、ユーザー辞書、現在の辞書候補集合にない履歴は従来順位を維持する。これにより、誤選択を1回学習済みの状態でも`再帰的`、`真価が問われる`、`家宅捜索`、`石油化学`、`渡した`の5種の辞書文脈規則が働く一方、反復確認または5回の使用で定着した選択を維持する。

履歴を持たない標準評価では、変更直前のN-best exportと、GSD dev 331件・GSD test 323件・AJIMEE 200件・JWTD dev 400件の`expected_output`、`label_index`、全候補の表層・cost・順序を比較し、1,254件すべて完全一致した。したがってfresh profileのacc@1/MRR/recallと既存の文脈改善は不変である。

500件の履歴を読み込んだRelease benchmarkを同じプロセスで4回測ると、文脈変換は履歴OFFで0.136–0.137 ms/op、競合する単発履歴ONで0.186–0.188 ms/opだった。追加の辞書1位比較は単発履歴と文脈が同時に存在するときだけ行い、差は0.049–0.051 ms/opで5 ms予算内だった。

#### 希望の助動詞「たい」の右接続

文章途中の編集では、候補直後の助動詞から活用形を絞れる。たとえば`かい｜たい`では、通常候補の低costな名詞`回`ではなく、動詞連用形`買い`が必要になる。ただし候補表層だけを一律に昇格すると、同じ読みの`書い`など接続できない活用形まで上がる。

そこで右文脈が`たい`で始まる場合だけ、Mozcの希望の助動詞POS（ID 152）へ接続できる一語候補を再評価する。同じ表層に名詞と動詞の両方がある場合も、候補生成時に残った安い名詞経路ではなく、全exact entryから助動詞へ接続する経路を探す。接続後costを候補間で正規化し、最大1,500 costのboundedな昇格に限定する。`た`まで広げる試作は`模試`を正しい`模し`ではなく別の動詞`燃し`へ寄せ、意味差を解けなかったため採用しなかった。

条件はGSD train・devで固定し、held-outは各1回だけ確認した。

| dataset | 従来 acc@1 / MRR@10 | `たい`接続 acc@1 / MRR@10 | recall@10 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.7031 / 0.8016 | 0.7031 / 0.8016 | 0.9701（不変） | 0 / 0 |
| GSD dev (331) | 0.8489 / 0.9003 | **0.8520 / 0.9029** | 0.9909（不変） | 1 / 0 |
| GSD test held-out (323) | 0.8545 / 0.9042 | 0.8545 / 0.9042 | 0.9907（不変） | 0 / 0 |
| AJIMEE held-out (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0.8350（不変） | 0 / 0 |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0.6725（不変） | 0 / 0 |

GSD devの`丁寧に案内してもらい、買いたい物`を`回 → 買い`へ修正した。100,000回の同一プロセスマイクロベンチは右文脈あり414.0 µs/op、同じ読み・左文脈だけ392.1 µs/opで、追加は約21.9 µs/opだった。

#### 一意な後続文法接続

[ATOKのAI用例](https://atok.com/other/support/howtouse/mac/shrd/shrd_yougo.htm)は、前後の語のつながりから適切な変換候補を提示する機能を説明している。[azooKeyの通常変換](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Core/FullInputProcessing.swift)も、辞書entryの左右品詞IDと接続costをラティスの評価に使う。Slimeは既存Mozc辞書の接続行列を使い、文章途中の右文脈が`た`・`ない`・`ます`・`て`・`で`・受身・使役の文法形から始まる場合だけ、同じ読みの全exact entryから後続POSへの接続costを比較する。

文法的に接続できるだけでは、`模し`と`燃し`のような意味の違いを解けない。そのため、最良接続costから1,000以内に入る漢字表層が1種類だけの場合に限り、その一語候補を最大1,500 cost昇格する。500のmarginはGSD train/devで`混んで → 込んで`、`人力である → 神力である`を壊したため不採用とした。条件はtrain・devで固定し、held-outは各1回だけ確認した。

| dataset | 従来 acc@1 / MRR@10 | 一意接続 acc@1 / MRR@10 | recall@10 | top-1改善 / 悪化 |
| --- | ---: | ---: | ---: | ---: |
| GSD train (1,940) | 0.7031 / 0.8016 | **0.7036 / 0.8019** | 0.9701（不変） | 1 / 0 |
| GSD dev (331) | 0.8520 / 0.9029 | **0.8550 / 0.9044** | 0.9909（不変） | 1 / 0 |
| GSD test held-out (323) | 0.8545 / 0.9042 | **0.8576 / 0.9058** | 0.9907（不変） | 1 / 0 |
| AJIMEE held-out (200) | 0.5300 / 0.6236 | 0.5300 / 0.6236 | 0.8350（不変） | 0 / 0 |
| JWTD dev (400) | 0.2950 / 0.4314 | 0.2950 / 0.4314 | 0.6725（不変） | 0 / 0 |

GSD trainの`私た → 渡した`、devの`個られています → 来られています`、testの`個ないね → 来ないね`を修正した。AJIMEEとJWTDはtop-1候補を含めて差分0だった。100,000回の同一プロセスマイクロベンチは右文脈あり110.6 µs/op、同じ読み・左文脈だけ100.5 µs/opで、追加は約10.1 µs/opだった。右文脈がない通常入力、ライブ変換、private modeの順位は変えない。

#### 組織名・地名recallの品詞別監査

組織・地域専用の探索を追加する前に、Mozcの組織POS（1929）と地域POS（1924〜1928）を品詞別に測った。辞書内の該当14万2,957 entryから約4,900件を均等抽出すると、全件が初回10候補に入っており、専用N-best経路を増やす余地はなかった。UD Japanese GSD trainの地名長単位語462件では396件を既存経路で回収し、残る66件は主に辞書にない海外表記、記号を含む地名、UniDicとMozcの読み差だった。候補探索を広げても語彙不在は解消しないため、製品コードへ地名・組織名専用beamは追加しない。次に扱う場合は、ライセンスと頻度を確認したoptional語彙packとして、独立したheld-out被覆とtop-1非回帰を要求する。

### 2026-07-20 baseline

N-bestを10件、ユーザー履歴なし、追加辞書なしで測定した。辞書の初期化時間はlatencyから除外している。

| subset | items | acc@1 | acc@10 | MRR@10 | MinCER@1 | MinCER@10 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 左文脈なし | 100 | 0.350 | 0.580 | 0.418 | 0.100 | 0.059 |
| 左文脈あり（文脈未使用） | 100 | 0.320 | 0.580 | 0.405 | 0.135 | 0.068 |
| 全体 | 200 | 0.335 | 0.580 | 0.412 | 0.117 | 0.064 |

全体の候補生成latencyはp50 6.76 ms、p95 27.03 ms、p99 34.73 ms、最大44.63 msだった。AJIMEEは入力長が最大117文字の難例を含む。通常候補表示の20 ms予算はp95で超えているため、品質改善と並行して長文N-bestの継続的な最適化が必要である。

この初回計測で、未変換のひらがな候補へ固定コストを与えると長文ほど第1候補へ上がる問題が見つかった。未変換候補を変換済み候補より後ろへ固定した結果、左文脈なしの`acc@1`は0.140から0.350へ改善した。

### 2026-07-20 改善後

失敗84件の分析から、(1) 辞書の語彙不足（矩形・荼毘・錠前などがcost閾値5500で除外）、(2) かな数詞の未対応（ジュウジカン→従事感）、(3) カタカナ未知語の断片化（ガミラス→紙ラス）が主因と判明した。次の4施策を適用した。

1. 辞書抽出閾値を5500から8500へ引き上げ（170,229語→1,085,464語）。8500で精度が飽和し、全辞書（1,223,906語）でも同一スコアだった。
2. 接続行列の量子化（解像度64の1 byte）を廃止し、正確な16 bit costで格納（`UCN2`、2.7 MB→3.6 MB）。単体では`acc@1`不変、`mincer@1`のみ微改善。
3. かな数詞列を解析して半角・全角・漢数字の合成候補をラティスへ追加（単体で`acc@1` +1.5pt、`acc@10` +3pt）。
4. 辞書に無い読みへカタカナ連続の未知語ノードを追加（base 1000 + 4000/文字。大辞書下ではベンチ中立で、真の辞書外語への保険として機能）。

| subset | items | acc@1 | acc@10 | MRR@10 | MinCER@1 | MinCER@10 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 左文脈なし | 100 | 0.560 | 0.860 | 0.650 | 0.054 | 0.015 |
| 左文脈あり（文脈未使用） | 100 | 0.510 | 0.810 | 0.603 | 0.082 | 0.024 |
| 全体 | 200 | 0.535 | 0.835 | 0.627 | 0.068 | 0.019 |

全体のlatencyはp50 13.1 ms、p95 50.2 ms、p99 74.2 msで、辞書拡大によりbaseline比でおよそ2倍になった。残る失敗33件は同音異義語の文脈選択（複合姓/複合性、皇帝/工程、高官/交換）と固有名詞（棟方志功、鈴木尚典）が中心で、辞書拡充では解消しない。次の改善は`CandidateRanker`への統計言語モデル（文脈スコア）導入が本命となる。

### 2026-07-20 ニューラルN-best rescoring（Phase 2 Step 1 フィージビリティ）

[phase2-context-model-survey.md](phase2-context-model-survey.md)の第一候補「小型ニューラルLMによるN-best rescoring」を、学習なしの公開モデル**zenz-v3.1-xsmall**（GPT-2系22.5Mパラメータ、Q5_K_M量子化20,970,272 bytes、CC BY-SA 4.0）で検証した。当時の`just fetch-neural-model`はこのartifactを取得していたが、現在は後述のv3.2-smallを取得する。v3.1-xsmallを保存済みの環境では次の評価コマンドで再現できる。

```sh
just evaluate-dev --neural-model target/evaluation/models/zenz-v3.1-xsmall-Q5_K_M-fixed.gguf            # λスイープ
just evaluate-ajimee --neural-model target/evaluation/models/zenz-v3.1-xsmall-Q5_K_M-fixed.gguf --lambda 0.8
```

`slime-tools`の`neural` feature（`llama-cpp-2`、要cmake）でビルドされる。評価スクリプトは`--neural-model`指定時に自動でfeatureを有効にする。

手法: 既存エンジンのN-best 10候補を、zenz-v3形式プロンプト`<左文脈40字><カタカナ読み><候補>`の対数尤度`log P(候補, EOS | 文脈, 読み)`で再順位付けする。最終スコアは`(1−λ)·(−cost/500) + λ·loglik`の対数線形補間。生成はせずprefillのみで、共有プレフィックスを全シーケンスに割り当て、候補ごとに独立シーケンスを継続することで、1アイテムを原則1回のdecode呼び出しで評価する（Metalのカーネル起動オーバーヘッドが支配的なため）。

devset 400件でのλスイープ: λ=0（rescoringなし）のacc@1 0.293に対し、λ∈[0.5, 0.9]で広いプラトーがあり、最良のλ=0.8で**acc@1 0.423（+13.0pt）**。held-outのAJIMEE-Benchをλ=0.8で1回だけ評価した:

| subset | items | acc@1 | acc@10 | MRR@10 | MinCER@1 | MinCER@10 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 左文脈なし | 100 | 0.690 | 0.860 | 0.761 | 0.034 | 0.015 |
| 左文脈あり（文脈使用） | 100 | 0.620 | 0.810 | 0.703 | 0.055 | 0.024 |
| 全体 | 200 | 0.655 | 0.835 | 0.732 | 0.045 | 0.019 |

改善後（辞書施策のみ）比で全体acc@1は**0.535 → 0.655（+12.0pt）**、MRR@10は0.627 → 0.732。左文脈ありサブセットも+11ptで、エンジンとして初めて左文脈を順位付けに使った（`context_used_by_engine: true`）。acc@10はrescoringでは変わらない（候補集合は不変）。サーベイの推定（+10〜25pt）の下限側を、生成方式のZenzai xsmall（同系ベンチでAcc@1 66.5）とほぼ同水準の65.5で裏付けた。

latency（M3、Metal）: 全体でp50 24.7 ms、p95 86.3 ms、p99 116.0 ms。rescoringの追加分はp50でおよそ+11 ms（バッチ投入約1.5 ms + GPU計算と同期・スコアリング約8.5 ms）。実装上の知見: (1) Metalのdecodeは非同期で、コストはlogits取得時の同期に現れる。(2) `llama_get_logits_ith`は呼び出しごとに同期するため、出力バッファ先頭を1回だけ取得して行を直接インデックスする。(3) softmax正規化はベクトル化可能な多項式exp近似で計算する（スカラーlibm expはdecodeと同程度の時間を消費していた）。

判断材料: 品質面はGo（+12pt、残存誤りの型に直効）。latencyはp95 20 ms予算を超過しており、製品組み込みには (a) 候補間コスト差が大きい場合のスキップ、(b) 文脈KVキャッシュの会話内再利用、(c) より小さい量子化・蒸留モデル、のいずれかが必要。CC BY-SA 4.0は商用利用を許すが、配布時は帰属・変更表示・ShareAlike等の履行と、アプリ本体から分離する境界の法務確認が必要なため、現状は評価専用に留めている。継続死条件・次段はサーベイの実装ステップ2以降（自前学習）を参照。

### 2026-08-10 Q4_K_M量子化の製品gate

配布サイズを下げるため、[zenz-v3.1-xsmallの公式BF16 checkpoint](https://huggingface.co/Miwa-Keita/zenz-v3.1-xsmall)からllama.cpp `5f55650a78f92aff4d48d671423e888fac0469ff`でQ4_K_Mを直接生成した。Q5からの再量子化は行っていない。Q5は20,970,272 bytes（19.999 MiB）、Q4は16,539,904 bytes（15.774 MiB）で、4,430,368 bytes、21.13%縮小した。比較したSHA-256はQ5 `aca732ea1a5f49a3f2ce677053bb7a977249aed0d4ed92ad37271fd1d2b9e8c9`、Q4 `6428ad81b8396f1efcaff092ace7cd55885f83a75f65428592ac0db6e98c001f`。

探索幅16、短文5候補、9文字以上16候補、base上位2件のcost差1,000、候補cost差1,500、長文2,500、lambda 0.7、EOS除外を固定した。GSD/PUDは右文脈がある製品条件としてmin margin 0.1、右文脈のないAJIMEE/JWTDは0を使用した。

| dataset | Q5_K_M acc@1 / MRR | Q4_K_M acc@1 / MRR | top-1差 |
| --- | ---: | ---: | ---: |
| GSD dev 331件 | **0.8580 / 0.9092** | 0.8489 / 0.9044 | -3件 |
| GSD test 323件 | **0.8638 / 0.9197** | 0.8607 / 0.9160 | -1件 |
| AJIMEE 200件 | 0.6950 / 0.7600 | **0.6950 / 0.7611** | 0件 |
| JWTD dev 400件 | 0.4475 / 0.5500 | **0.4475 / 0.5516** | 0件 |
| PUD 446件 | **0.7130 / 0.7803** | 0.7018 / 0.7739 | -5件 |

Q4は5コーパス合計でQ5より9件top-1を落とし、独立domainのPUDでも-1.12ptだった。AJIMEE/JWTDのMRR微増では複数domain非悪化のgateを満たせない。速度もGSDではQ4が短い一方、長文中心のAJIMEE/JWTDではQ4が一貫して速くならなかった。変換精度を優先してQ4は不採用とし、artifactを製品bundleやrepositoryへ追加しない。

### 2026-08-10 Apache-2.0 smallモデルの高精度profile

[azooKey Desktop](https://github.com/azooKey/azooKey-Desktop)がsmallモデルを製品資源として使い、v0.1.4でzenz-v3.2へ更新したことを踏まえ、[zenz-v3.2-small-gguf](https://huggingface.co/Miwa-Keita/zenz-v3.2-small-gguf)を現行xsmallと比較した。公式配布commit `c67e03e07d215c869f591b274c1631170d3e11fe`はApache-2.0を宣言し、Q5_K_Mを95.1M parameter、73.9 MBと表示している。source SHA-256は`29c223d4c23327b80fd13ebb5ab2555057a46317997d5da391584ffbef0db673`、pre-tokenizer metadata修正後は`b660082fcbe8e538c4ccc1044f79c2c881364a25f8c9277a8b8f1dcf680e5b84`である。

xsmallの製品profile（lambda 0.7、右文脈なしmargin 0、右文脈あり0.1）に対し、smallはJWTD/GSD devだけでlambda 0.8を固定した。候補overrideのmarginはGSD devで0 / 0.1 / 0.25 / 0.5 / 1を比較し、9文字未満ではtop-1同率内でMinCERが最良だった0.5を固定した。9文字以上では同じ既存の長文境界を使い、JWTD devのtop-1が202件から204件、MRRが0.5877から0.5922へ改善する0を固定した。右文脈ありは入力長によらず0.5を維持する。まず候補探索幅16、base cost gate、短文5候補、候補cost窓、EOS除外を同一にしてモデルとmarginを比較し、その後にhigh-accuracyだけ長文候補幅を32へ拡張した。

| dataset | xsmall acc@1 / MRR | small acc@1 / MRR | top-1差 | small p95 |
| --- | ---: | ---: | ---: | ---: |
| JWTD dev 400件 | 0.4550 / 0.5574 | **0.5250 / 0.6140** | +28件 | 130.0 ms |
| GSD dev 331件 | 0.8761 / 0.9198 | **0.8943 / 0.9260** | +6件 | 15.1 ms |
| AJIMEE 200件 | 0.7300 / 0.7804 | **0.8100 / 0.8352** | +16件 | 180.7 ms |
| GSD test 323件 | 0.8854 / 0.9246 | **0.9009 / 0.9310** | +5件 | 15.5 ms |
| PUD 446件 | 0.7444 / 0.7951 | **0.7668 / 0.8106** | +10件 | 26.6 ms |

smallは調整に使わなかった3 held-outを含む全domainでtop-1とMRRを改善した。修正後モデルは73,871,904 bytesで、xsmallの20,970,272 bytesより52,901,632 bytes大きい。長文候補32ではAJIMEEのp95が180.7 msになるため、軽量profileを置換せず、遅延との交換で精度を選ぶ明示的な`high-accuracy` profileとして採用する。

製品経路の右文脈なしmarginを一律0にした場合、GSD testは289/323（0.8947）だった。9文字未満へ0.5を適用すると、`住み`、`淳介`、`見積もり`、`主神`の4件を保護し、`感心`、`連隊`の2件が悪化して291/323（0.9009）となる。一方、9文字以上まで0.5にするとJWTDは204/400から202/400へ退行する。既存の9文字境界で長文だけ0へ戻すと、AJIMEEも160/200を維持しながらMRRが0.8211から0.8223へ改善する。右文脈を持つPUDは0.5のまま変更しない。

候補幅は、まず辞書oracleを16 / 32 / 64で診断した。正解回収率はJWTDで0.7250 / 0.7825 / 0.8300、AJIMEEで0.8600 / 0.8850 / 0.9000、PUDで0.8812 / 0.8924 / 0.9036だった。一方、候補生成p95はJWTDで27.1 / 75.0 / 210.4 ms、AJIMEEで33.7 / 115.4 / 339.7 ms、PUDで4.2 / 13.7 / 42.2 msとなった。64は回収増分に対して長文の生成時間が大きすぎるため除外した。

残る16 / 20 / 24 / 28 / 32候補をJWTD devで比較すると、top-1は204 / 206 / 206 / 208 / 210件、32候補のMRRは0.6140だったため、精度優先profileは32に固定した。同一条件のJWTD連続測定は16候補のp50 31.1 ms / p95 64.5 msから、32候補のp50 53.2 ms / p95 130.0 msへ増えた。凍結後のAJIMEEは160件から162件へ改善し、PUDはtop-1 342/446を維持してMRRが0.8091から0.8106へ改善した。GSD dev/testは全入力が9文字未満なので候補幅も結果も変わらない。

32と64の間も見落とさないため40 / 48候補を追加測定した。JWTD devは211 / 212件まで改善したが、この測定帯のp95は32候補215.4 msに対して40候補272.1 ms、48候補342.6 msだった。最小の追加改善を得る40候補を凍結してheld-outへ適用すると、AJIMEEは162/200のまま、PUDも342/446のままで、PUDのMRRは0.810595から0.810585へ僅かに悪化した。AJIMEEのp95も374.2 msまで増えたため、追加1件のdev改善に対して遅延と独立指標が見合わず、40 / 48候補は不採用とした。

lambdaは0.8 / 0.825 / 0.85 / 0.875 / 0.9をJWTD/GSD devで比較した。0.875はJWTDで+2件、GSD devで+1件だったが、凍結後のGSD testで0.8比-1件、0.9は-2件だったため不採用とした。単一係数を強めず、held-out非退行の0.8を維持する。

32候補化後にもlambdaを再確認した。JWTD devでは0.9が0.8比で210件から211件へ改善したが、凍結後のAJIMEEは162件から161件へ悪化し、その1件は45文字の長文だった。短文だけ0.8を維持する入力長境界でも回避できないため、0.9は不採用とし0.8を維持する。

[azooKeyKanaKanjiConverterの候補評価](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/8e3a6eb89e088efd868aa28dadb74c697df4e6fb/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Zenzai/Zenz/ZenzCandidateEvaluator.swift)に近い診断として、候補tokenごとの`候補logit - 最大logit`を合計する方式と、最悪の1 tokenだけを使う方式もJWTD devで比較した。lambda 0.7では既存のEOS除外尤度 / token差合計 / 最悪tokenがacc@1 0.5300 / 0.5300 / 0.5300、MRR 0.6161 / 0.6160 / 0.6152だった。lambda 0.8では0.5250 / 0.5225 / 0.5225となり、既存方式を上回らないため製品コードへ残さない。

既存のEOS除外尤度についてlambda 0.64〜0.80をJWTD devだけで再探索すると、0.74と0.75が214/400（0.5350）、MRR 0.6192で同率だったため、モデル依存を弱める0.74を仮固定した。GSD devは製品margin 0.5で0.74 / 0.75 / 0.8がすべて301/331（0.9094）だった。一律0.74はAJIMEEのtop-1とMRRを維持した一方、PUDを342件から341件へ1件悪化させたため不採用とした。

変更したtop-1を入力長で分けると、JWTDの改善4件はすべて28文字以上、AJIMEEの改善1件は20文字以上で、AJIMEEの悪化1件は10文字だった。そこで右文脈なし・20文字以上だけ0.74、それ以外は0.8を維持する境界を固定した。20文字以上の部分集合はJWTD 295件で151件から155件、MRR 0.5994から0.6064、MinCER 0.0465から0.0462へ改善し、AJIMEE 108件で82件から83件、MRR 0.7813から0.7859、MinCER 0.0248から0.0245へ改善した。全体ではJWTDが210件から214件、AJIMEEが162件から163件となる。GSDは最大5文字、PUDは最大15文字で境界を通らず、右文脈ありも入力長にかかわらず0.8を維持するため、両held-outの順位は現行と同一である。

長文の候補回収をさらに調べるため、通常N-bestと最良パスの文節境界を保った表記variantを同じ32枠で比較した。JWTDのoracleはN-best 32で313/400、完全な和集合で315/400、通常30件と固定文節2件では314/400だった。しかし追加回収した`異存ありません`はsmallモデルで2位までしか上がらず、全体top-1も変わらなかった。追加候補生成だけを製品へ残す根拠がないため、この方式は不採用とした。

一方、base上位2件のcost差が1,000を超えるだけでニューラル評価を省略していた長文を、`high-accuracy`かつ右文脈なしの場合だけ評価すると、候補集合を変えずに両コーパスで改善した。

| dataset | 従来 acc@1 / MRR | 長文gate解除 acc@1 / MRR | top-1差 | 変更内訳 |
| --- | ---: | ---: | ---: | ---: |
| JWTD dev 400件 | 0.5350 / 0.6192 | **0.5525 / 0.6317** | +7件 | 7改善 / 0悪化 |
| AJIMEE held-out 200件 | 0.8150 / 0.8377 | **0.8250 / 0.8435** | +2件 | 2改善 / 0悪化 |

JWTDでは`自身がある → 自信がある`、`行動を借りて → 講堂を借りて`、`資料が残されて → 史料が残されて`、AJIMEEでは`種類販売業免許 → 酒類販売業免許`、`音声を取り戻し → 温情を取り戻し`を修正した。MinCERもJWTD 0.0565→0.0550、AJIMEE 0.0258→0.0242へ改善した。9件の正解候補はすべて8位以内だったため、従来gateで評価済みの曖昧長文は32候補を維持し、新たにgateを解除した入力だけ8候補に制限した。JWTDのニューラル評価対象は328→392件、評価候補は5,171→5,525件、AJIMEEは159→189件・2,407→2,551件となった。実製品FFIで新規改善例`ラーメンには自信があるが`を20回warm測定するとp50 81.3 ms / p95 89.1 ms / max 133.3 msで、32候補をそのまま使う試作のp50 263.2 ms / p95 482.0 msから大幅に抑えた。精度と引き換えの追加推論は明示的な`high-accuracy`だけに限定し、`balanced`、9文字未満、右文脈あり、履歴・ユーザー辞書・規則・入力ミス訂正の候補は従来gateを維持する。GSD dev/testとPUDは全件が右文脈付きで、この条件を通らない。

製品FFIは既存の`balanced`（lambda 0.7 / 長文16候補 / marginは短文0・長文0・右文脈0.1）をABI互換で維持し、`high-accuracy`（通常lambda 0.8、右文脈なし20文字以上だけ0.74 / 長文32候補 / 右文脈なし長文はbase confidence gateを解除し、その場合だけ8候補 / marginは短文0.5・長文0・右文脈0.5）を明示選択できる。短文は両profileとも5候補、右文脈marginは入力長より優先し、KVセル数1024の上限は変更しない。通常buildはモデルを取得も同梱もしない。`just fetch-neural-model`はcommitとsource/fixed checksumを固定してsmallを評価cacheへ取得し、同梱buildにはApache-2.0全文、出典、metadata変更を記録した`crates/slime-neural/data/ZENZ_V3_2_SMALL_LICENSE.txt`と`SLIME_NEURAL_PROFILE=high-accuracy`を要求する。profileを省略した既存buildは互換性のため`balanced`になる。

v3.1のShareAlike問題はこのv3.2-small artifactには当てはまらない。一方、公式model cardはlicense metadata以外が空で、学習元の説明はない。Apache-2.0表示だけから第三者権利まで断定せず、実際の有償配布前には由来確認と法務レビューを残す。

### 2026-08-11 モデル指示prefixによる局所再探索

[azooKeyKanaKanjiConverterの現行`ZenzCandidateEvaluator`](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/93766c46e31fa6a18b7ced49dab31337780f6f45/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Zenzai/Zenz/ZenzCandidateEvaluator.swift#L286-L311)は、候補tokenがモデルの最尤tokenと食い違った最初の位置で`fixRequired(prefixConstraint:)`を返し、固定N-bestの外側を再探索する。Slimeでも同じ信号を評価した。候補ごとの全文尤度は従来どおり一度のdecodeで計算し、明示的な`high-accuracy` profileだけ、trie nodeごとの最尤tokenと最初の不一致prefixを追加取得する。復号不能な特殊tokenはその候補の診断だけを捨て、既存の尤度scoreを失敗させない。

無制限にprefix制約を採用すると、モデルが局所的に好む表記へ長文の後半まで書き換える誤発火があった。このためJWTD devだけで、prefix 4文字以上、代替tokenとのlogit差2.0以上、制約付きlattice最大8件、元候補と同じ文字数、変更は連続2文字以内に当初固定した。ASCII英数字を含む変更は禁止する。これによりPUDで観測した`紀元前511 → 紀元前後11`を構造的に除外する。制約候補は元のニューラルwinnerを削除せず、その直前へ挿入する。`balanced`、履歴、ユーザー辞書、規則、入力ミス訂正の保護境界は変更しない。後述の再評価後、保持データ非回帰を確認してlogit差だけ1.5へ更新した。

`neural_prefix_probe`で製品と同じhigh-accuracy候補数、cost窓、lambda、minimum marginを再現した。JWTDで閾値を固定した後、AJIMEE、PUD、GSDへ同じ条件を適用した。

| dataset | 現行top-1 | 局所再探索top-1 | 改善 / exact悪化 |
| --- | ---: | ---: | ---: |
| JWTD dev 400件 | 221 / 400（0.5525） | **223 / 400（0.5575）** | 3 / 1 |
| AJIMEE held-out 200件 | 165 / 200（0.8250） | **168 / 200（0.8400）** | 3 / 0 |
| PUD held-out 446件 | 342 / 446（0.7668） | **346 / 446（0.7758）** | 4 / 0 |
| GSD dev 331件 | 296 / 331（0.8943） | 296 / 331（0.8943） | 0 / 0 |
| GSD test 323件 | 291 / 323（0.9009） | 291 / 323（0.9009） | 0 / 0 |

JWTDのexact悪化1件は`マンネルヘイム十字勲章受賞 → マンネルヘイム十字勲章受章`である。評価goldと異なるため悪化として数え、表記の妥当性とは分けて人手確認対象に残す。改善例は`奨学の紛争 → 少額の紛争`、`補給してしまう → 捕球してしまう`、`地球参加 → 地球讃歌`、`検討をつけた → 見当をつけた`である。

実製品FFIの20回warm測定では`ラーメンには自信があるが`がp50 38.8 ms / p95 53.7 ms / max 65.1 msだった。同じ16候補を10回ずつ交互に測った通常score / prefix診断scoreのp50は27.6 / 26.8 msで、この条件では追加の全語彙走査は測定ノイズを超える増加を示さなかった。初回の局所再探索自体は二回目のmodel decodeを必要としない。JWTDで制約が閾値を通った24件の制約付きlatticeはp50 3.0 ms / p95 6.4 msで、通常変換の候補beamを消費しない。

### 2026-08-11 学習強度とニューラル順位付けの境界

局所再探索後にも誤ったままの項目を、同じ辞書で64候補まで再生成して分類した。JWTDは残り177件のうち103件が1〜32位、7件が33〜64位、67件が64位までに存在しなかった。103件を製品と同じ候補数・cost窓・confidence gateで再分解すると、正解が実際のニューラル採点対象内にあったのは80件、1〜32位にはあるが採点対象外だったのは23件だった。AJIMEEは32件中11 / 3 / 18件、PUDは100件中51 / 6 / 43件だった。長文beamをさらに広げるより、まず80件の順位付けを優先し、23件は候補投入境界の問題として分離する。`neural_prefix_probe INPUT.json MODEL.gguf --failures N`は、最終誤りごとに実際の採点候補数と64候補内順位を出し、集計でも`remaining_within_scored`と`remaining_outside_scored_within_32`を分ける。

順位付け側では、同じJWTD devで次の一般案を比較した。候補token数への定数補正は現行221件を上回らず、正の補正ほど悪化した。モデル上位2 / 3候補それぞれの不一致prefixまで制約付き探索へ広げると、局所再探索後の223件から215 / 203件へ悪化した。最尤tokenに次ぐ2・3位tokenを同じ不一致位置で試す方式も223件のままだった。v3.2-smallとApache-2.0の[公式v3.2-xsmall](https://huggingface.co/Miwa-Keita/zenz-v3.2-xsmall-gguf/tree/4f5423f0fad41a73b1242eb96fe5c12ae4fdca83)の尤度ensembleは、small単独221件に対してxsmall重み0.05から220件へ低下し、その後も重みを増やすほど悪化したため、二重モデルruntimeへ進めなかった。

最尤prefixで得た格子8候補をsmallで二段再評価する方式は、JWTD devを223件から225件へ改善し、GSD dev 301件を維持した。しかし条件固定後のAJIMEEは168件から166件へ悪化し、`軍 → 郡`、`要因 → 要員`を誤選択したため不採用とした。短文候補を5から8へ増やす、短文cost窓を1,500から2,500へ広げる、短文のbase confidence gateを解除する案も、採点対象を最大2件増やしただけでJWTDの223件を改善しなかった。held-outを見た後の語別・表記別guardは追加しない。

[ATOKハイパーハイブリッドエンジン2](https://www.atok.com/features/2025/)は、直前に一度確定した表記を常に最優先にするのではなく、継続的な入力傾向を「変換強度」として学習し、自然な日本語を優先できるとしている。azooKeyの現行Zenz評価も、[学習候補tokenへ限定したpriority](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/93766c46e31fa6a18b7ced49dab31337780f6f45/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Zenzai/Zenz/ZenzCandidateEvaluator.swift#L171-L184)を加える一方、モデルとの制約衝突が続く場合は[メモリとユーザー辞書を外したretry](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/93766c46e31fa6a18b7ced49dab31337780f6f45/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Zenzai/zenzai.swift#L501-L523)へ進む。いずれも、学習候補を無条件に固定する方式ではない。

Slimeは従来、履歴候補が1件でもあれば外部モデルへの候補要求全体を止めていた。このため通常辞書にもある表記を一度選んだだけで、その後に自然な文脈が来てもニューラル順位付けを利用できなかった。変更後は、使用5回未満で反復確認も済んでいない一時履歴が通常辞書候補にも存在する場合だけモデルとの比較を許可する。モデルは辞書候補の位置だけを並べ替え、一時履歴の表記も候補内へ残す。辞書外の独自履歴は採点対象に入らないため先頭を維持する。5回以上の確立済み履歴、2回確認済み表記、文脈学習、明示的なユーザー辞書、インストール済みcontext rule、英語逆引き、入力ミス訂正は従来どおりモデルより優先する。

実製品FFIとv3.2-smallで、`ラーメンには自身があるが`を同じ読みの一時履歴（1回）として置くと、モデルが`ラーメンには自信があるが`を先頭へ戻した。一方、同じ誤表記を確立済み履歴（5回）にすると、モデル要求を作らず履歴を先頭に維持した。モデル無効時と履歴のない評価コーパスの候補生成・順位は変えない。追加推論は、ニューラル機能が有効で一時履歴が通常辞書候補と重なる変換に限られる。

#### 2回確認による適応的な表記切替

5回未満を一時履歴とする境界は、1回の誤選択による上書きを防ぐが、意図的な表記変更にも5回を要求していた。そこで実使用回数とは別に、現在の定着候補へ対する反復選択だけを確認する。最初の競合選択では現在の候補を`history_preferences.tsv`へ固定し、同じ代替を文脈なしで再選択した場合、または異なる文脈で再選択した場合にだけ確認済み表記を切り替える。同じ文脈での反復は全体へ昇格せず、文脈履歴へ留める。

この分離により、`漢字`100回に対する`感じ`1回は従来どおり`漢字`が先頭、2回確認後は再起動後も`感じ`が先頭になる。切替後に`漢字`を1回選んでも戻らず、2回目で戻る。`history.tsv`の`感じ`は実回数2のままなので、5回必要なprefix補完には現れない。`日本の解答`を同一文脈で2回選んでも、文脈なしの`かいとう`は`回答`を維持する。履歴なしprofileではsidecar探索も候補変更も発生しない。

sidecarは履歴本体と同じ楽観的atomic replace、最大500件、壊れた既存ファイルを上書きしない境界を持つ。履歴に対応する表記がなければ順位へ使わない。macOS設定の個別削除・整理・全消去では対応するsidecar行も同時に絞り、sidecarが別プロセスで変更された場合はその変更を上書きしない。

Releaseの同一プロセスで、履歴500件・外部文脈ありの変換を各5,000回測定した。sidecarなしは0.1862 ms/op、上限のsidecar 500件ありは0.1872 ms/opで、追加は約0.001 msだった。`engine/contextual_conversion_history_on_500_entries_500_preferences`を恒常benchmarkとして残し、既存の履歴ON/OFF差5 ms未満という製品予算を維持する。

### 2026-08-11 局所prefix修正の1回再レビュー

azooKeyのZenz評価は候補とmodelの不一致をprefix制約へ変換し、必要なら複数回レビューする。Slimeの初回prefix修正が同じ安全条件を満たして実際に候補へ入る場合だけ、その修正後表層1件をv3.2-smallへもう一度渡し、最初の強い不一致prefixを1回だけ格子へ戻す方式を評価した。各段階は独立に「同じ文字数、連続2文字以内、ASCII英数字を変更しない」を満たす必要がある。2回目のmodel採点・復号・制約探索のいずれかが失敗した場合は、従来の1回目修正をそのまま返す。

| dataset | 1回prefix修正 | 2回目レビュー | 改善 / exact悪化 |
| --- | ---: | ---: | ---: |
| JWTD dev 400件 | 223 | **224** | 4 / 1 |
| GSD dev 331件 | 301 | 301 | 0 / 0 |
| AJIMEE held-out 200件 | 168 | 168 | 3 / 0 |
| PUD held-out 446件 | 346 | 346 | 4 / 0 |
| GSD test 323件 | 300 | 300 | 0 / 0 |

JWTDでは`推力偏光ノズル → 推力偏向ノズル`を追加で回収した。3回目のレビューは全5 datasetでtop-1を追加改善しなかったため、製品上限は初回を含む2回とする。2回目が発火したJWTD項目の追加model処理はReleaseでp50 49.5 ms / p95 56.9 msだった。全入力へ常時追加せず、初回の強いprefix信号と安全な格子候補が揃った場合だけ精度優先`high-accuracy` profileで実行する。

### 2026-08-11 prefix確信差の再評価

局所修正と1回再レビューを組み合わせた状態で、代替tokenの最小logit差をJWTD devだけで2.0から1.5へ下げた。安全条件、候補数、候補cost窓、ニューラル順位付けは変更しない。JWTDでは制約対象が24件から33件、実際の局所修正が8件から12件へ増え、top-1は224件から225件へ改善した。追加で`シリアスな間 → シリアスな曲`と`信託 → 神託`を回収し、`不 → ふ`を1件悪化させたため差し引き1件の改善である。

閾値を1.5に固定した後、AJIMEEは168 / 200、PUDは346 / 446、GSD devは301 / 331、GSD testは300 / 323を維持し、各datasetのexact悪化数も増えなかった。1.5未満へは広げず、prefix 4文字以上、連続2文字以内、同じ文字数、ASCII英数字不変という構造的な安全条件を維持する。

### 2026-08-11 辞書制約付き生成による候補集合外の回収

64候補にも正解がないJWTD残存68件は、55件が正解まで2領域以上の変更を必要とした。N-best幅、固定segment候補、prefixの3文字化、文脈長80、追加語彙の単純投入はいずれも5 dataset非悪化を満たさなかったため、`high-accuracy`だけでzenz-v3.2-smallのgreedy変換を1件生成し、完全な辞書lattice pathとして検証できる表層だけを既存の再採点poolへ加える方式を評価した。生成文字列は直接候補へ出さない。

候補化には、読み6〜32文字、EOSまで完了、元の辞書第1候補と同じ文字数、ASCII英数字不変、離れた変更領域が2〜4個、各領域2文字以内、通常の長短別cost窓内という条件をすべて要求する。追加候補にはmodel supplementalの追加margin 1.5を課し、最大pool 32件を維持する。1領域だけの生成候補は既存の局所prefix処理へ任せる。32文字を超えて無制限に生成すると、AJIMEEで`要因 → 要員`のexact悪化が出たため採用しない。

| dataset | 2回prefixまで | 辞書制約付き生成後 | 改善 / exact悪化 |
| --- | ---: | ---: | ---: |
| JWTD dev 400件 | 225 | **227** | 2 / 0 |
| AJIMEE held-out 200件 | 168 | 168 | 0 / 0 |
| PUD held-out 446件 | 346 | **347** | 1 / 0 |
| GSD dev 331件 | 301 | 301 | 0 / 0 |
| GSD test 323件 | 300 | 300 | 0 / 0 |

JWTDでは`世界歴史体系 → 世界歴史大系`と`事故に合い → 事故に遭い`、PUDでは右文脈付きの`タイの歌人 → タイの華人`を追加回収した。実際にpoolへ入った生成候補はJWTD 4件、PUD 1件、AJIMEE/GSDは0件だった。モデルが生成した表層のうち辞書latticeで完全一致したものはJWTD 384 / 400、AJIMEE 188 / 200、PUD 436 / 446だったが、構造・cost・長さ・marginを通ったものだけを採用する。

Releaseの生成単体は、製品対象長6〜32文字でJWTD p50 63.7 ms / p95 106.9 ms、PUD p50 31.5 ms / p95 43.3 msだった。これはSpace変換時だけ発生し、モデルなし・`balanced`・5文字以下・33文字以上では実行しない。精度優先profileでも無視できる負荷ではないため、候補生成と再採点のcontext共有は今後の性能課題として残す。実製品FFIでは通常prefixだけでは残る`『世界歴史体系・イギリス史`を`『世界歴史大系・イギリス史`へ回収する境界テストを通した。

### 2026-08-11 辞書内生成一致による近接順位の解決

greedy生成は候補集合外の回収だけでなく、通常の再採点poolに既にある表層とも一致する。そこで追加推論を行わず、読み6〜32文字、EOSまで完了、生成表層が通常辞書候補と完全一致し、元の第1候補から同じ文字数の連続2文字以内だけが変わり、ASCII英数字を変更しない場合に限り、その候補を生成一致として記録した。通常のcost・モデル補間winnerに対する候補尤度差が0.1〜0.2の狭い範囲にあるときだけ生成一致候補を優先する。0.1未満は生成側の優位が弱く、0.2を超える出力には`会えて → 逢えて`のようなモデルの表記選好が含まれたため、JWTD devで境界を固定してから他datasetへ適用した。候補を追加せず、履歴・ユーザー辞書・入力ミス訂正などニューラル採点を通らない保護経路も変更しない。

| dataset | 辞書制約付き生成後 | 生成一致後 | 改善 / exact悪化 |
| --- | ---: | ---: | ---: |
| JWTD dev 400件 | 227 | **228** | 1 / 0 |
| AJIMEE held-out 200件 | 168 | 168 | 0 / 0 |
| PUD held-out 446件 | 347 | **348** | 1 / 0 |
| GSD dev 331件 | 301 | 301 | 0 / 0 |
| GSD test 323件 | 300 | 300 | 0 / 0 |

JWTDでは`制作は文芸プロダクションに → 製作は文芸プロダクションに`、PUDでは右文脈付きの`任務につく → 任務に就く`を修正した。GSD dev/testは読みが最大5文字なので構造的に対象外である。実製品FFIでも前者を確認し、候補配列へ同じ表層を重複追加しないcoreテストと、尤度差0.09 / 0.10 / 0.20 / 0.21の境界テストを追加した。

生成beam幅2も先に比較した。JWTDの辞書裏付け付き生成oracleは219件から286件へ増えたが、大半は既存pool内の順位問題であり、安全条件を通ってpoolへ入った候補は4件から6件へ増えただけだった。最終top-1は幅1と同じ227件、改善2 / 悪化0で追加改善がなかった一方、対象長の生成p50 / p95は約64 / 114 msから90 / 151 msへ増えた。このためbeam実装は製品・診断コードへ残さず、既に行っているgreedy生成の一致信号だけを再利用する。

### 2026-08-11 文脈対比による一般頻度の分離

通常の候補尤度には、候補表層そのものの一般的な出やすさと、左右文脈による後押しの両方が含まれる。候補集合内に残る順位誤りに対し、同じ読み・同じ候補を文脈なしでも一度採点し、`文脈あり + weight × (文脈あり - 文脈なし)`で文脈固有の寄与だけを増幅する方式を評価した。候補生成、辞書cost、prefix診断、学習候補の保護境界は変更しない。

JWTD devとGSD devだけでweight 0 / 0.1 / 0.25 / 0.5 / 1.0を比較した。0.25以上はJWTDの正解を少なくとも1件落としたため除外し、JWTDを維持しながらGSD devを1件改善した最小の0.1へ固定した。その後、AJIMEE、PUD、GSD testへ同じ値を一度だけ適用した。

| dataset | 生成一致後 | 文脈対比後 | 改善 / exact悪化 |
| --- | ---: | ---: | ---: |
| JWTD dev 400件 | 228 | 228 | 0 / 0 |
| GSD dev 331件 | 301 | **302** | 1 / 0 |
| AJIMEE held-out 200件 | 168 | 168 | 0 / 0 |
| PUD held-out 446件 | 348 | **349** | 1 / 0 |
| GSD test 323件 | 300 | **301** | 1 / 0 |

GSD devでは`乗せ → 載せ`、GSD testでは`付か → 突か`、PUDでは`仮フォルニヤの小島 → カリフォルニヤの小島`を修正した。全5 datasetで従来正解からのexact悪化は0件だった。実製品FFIでも左右文脈付きの`のせ`が`載せ`になることをv3.2-smallで確認した。

追加採点は明示的な`high-accuracy`かつ左右どちらかの文脈がある場合だけ行う。`balanced`と文脈なし入力は従来どおり1回だけ採点する。通常採点と文脈なし採点は同じllama contextを再利用する。実製品と同じ5候補を各10回交互測定した2回のmodel処理p50は、通常の文脈採点14.165〜14.524 ms、文脈対比24.815〜25.641 ms、追加10.650〜11.117 msだった。精度優先profileでも常時二重採点にするのではなく、文脈が実在する候補再採点だけへ限定する。

### 2026-08-11 局所修正での漢字表記の保持

prefix局所修正は強いmodel不一致をN-best外へ戻すため、通常候補の選択とは異なり、ユーザーが既に得ていた漢字をひらがなへ戻す必要はない。実際にJWTD devでは、1回目の修正で`使える → 仕える`を回収した後、2回目レビューが文末の`不 → ふ`まで変更して正解を失っていた。そこで局所修正の変更位置に「漢字からひらがな」が1文字でも含まれる場合だけ、その修正を採用しない。通常N-bestにあるひらがな候補の選択、漢字同士の修正、`セウ知る → セウシル`のようなカタカナ固有表記は変更しない。

文脈対比まで含む最終パイプラインでJWTD devは228→229となり、GSD dev 302、AJIMEE 168、PUD 349、GSD test 301をすべて維持した。追加のmodel処理や辞書探索はない。実製品FFIでも`どの時期に織田信長に仕えることになったのかも不`を確認し、文字種境界はcore単体testで固定した。

### 2026-08-11 辞書内複数局所生成一致

辞書内生成一致を連続2文字以内だけに限定すると、greedy生成と通常辞書候補が正解で一致しても、離れた複数箇所を同時に直す候補を順位信号として使えない。そこで既存の読み6〜32文字・EOS完了・同じ文字数・ASCII英数字不変を維持し、生成表層が通常の再採点poolに既に存在して、元の第1候補から2〜4個の離れた領域だけが変わり、各領域が2文字以内の場合を複数局所一致として区別した。新しい候補は追加せず、辞書lattice検証済みの既存候補だけを選ぶ。

通常の連続局所一致は尤度差0.1〜0.2を維持する。複数局所一致だけ、JWTD devで上限0.20 / 0.25 / 0.30 / 0.50 / 1.00を比較し、改善を得る最小の0.25へ固定した。0.25以上はJWTDで同じ1件を改善し、GSD devは全条件で不変だった。設定を固定した後、AJIMEE、PUD、GSD testへ同じ値を一度だけ適用した。

| dataset | 漢字保持後 | 複数局所一致後 | 改善 / exact悪化 |
| --- | ---: | ---: | ---: |
| JWTD dev 400件 | 229 | **230** | 1 / 0 |
| GSD dev 331件 | 302 | 302 | 0 / 0 |
| AJIMEE held-out 200件 | 168 | 168 | 0 / 0 |
| PUD held-out 446件 | 349 | 349 | 0 / 0 |
| GSD test 323件 | 301 | 301 | 0 / 0 |

回収したのは`丹沢湖での統計開始依頼の局地を更新した → 丹沢湖での統計開始以来の極値を更新した`である。正解は通常辞書候補8位にあり、greedy生成も同じ表層を出していたが、従来の生成一致では2箇所の変更を扱えなかった。生成はhigh-accuracyで既に実行している1回を再利用するため、追加のmodel処理や辞書探索はない。coreでは局所・複数局所の種類と尤度境界を別々に固定し、実製品FFIでは文字入力から候補先頭まで同じ例をv3.2-smallで確認した。

### 2026-08-11 辞書制約付き表層圧縮

辞書制約付き生成候補は元の第1候補と同じ表層文字数を要求していたため、完全な辞書lattice pathでも`いろいろ → 色々`のように仮名列を漢字へ圧縮する生成結果を候補へ加えられなかった。そこで長さが異なる場合だけLevenshtein整列を行い、生成表層が全体で1〜2文字短く、変更領域が2〜4個、各領域の変更が元・生成側とも4文字以内、ASCII英数字不変の場合を追加の安全境界とした。読み6〜32文字、EOS完了、通常cost窓、完全な辞書lattice path、model supplemental追加margin 1.5は変更しない。同じ候補が既存poolにある場合はこの境界を順位一致へ流用せず、通常の再採点に任せる。

最初に長さの増減を両方許す案を回帰setへ適用すると、PUDでtop-1は変わらないものの`浮きの先駆け → 雨季のさきがけ`という余分な候補がpoolへ1件加わった。この候補汚染を根拠に長さを増やす方向は棄却し、仮名から漢字への圧縮を含む短縮方向だけへ固定した。最終条件ではPUDの追加候補は消えた。

| dataset | 複数局所一致後 | 表層圧縮後 | 改善 / exact悪化 |
| --- | ---: | ---: | ---: |
| JWTD dev 400件 | 230 | **231** | 1 / 0 |
| GSD dev 331件 | 302 | 302 | 0 / 0 |
| AJIMEE regression 200件 | 168 | 168 | 0 / 0 |
| PUD regression 446件 | 349 | 349 | 0 / 0 |
| GSD test regression 323件 | 301 | 301 | 0 / 0 |

JWTDでは`そしてエンジェル帯に復讐渡渉していろいろなちょっかい → そしてエンジェル隊に復讐と称して色々なちょっかい`を回収した。通常pool外だった生成候補1件だけが追加され、既存の再採点と追加marginを通ってtop-1になった。他4 datasetの候補追加数とtop-1は最終条件で不変だった。実製品FFIでは文字入力から同じ表層を確認し、coreでは短縮方向、長さ差、領域数、各側文字数、ASCII変更、辞書裏付けを個別に固定した。

### 2026-08-11 azooKey rich候補の評価と不採用

[azooKeyKanaKanjiConverterの`requestRichCandidates`](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/93766c46e31fa6a18b7ced49dab31337780f6f45/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Zenzai/Zenz/ZenzCandidateEvaluator.swift)は、候補がmodelの最尤token列に従っている位置でも上位の代替tokenを集める。全位置から確率比の高いprefixを最大5件保持し、[確率比0.25超を追加候補、0.5超を追加探索](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/93766c46e31fa6a18b7ced49dab31337780f6f45/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Zenzai/zenzai.swift#L2141-L2174)へ使う。Slimeで既に棄却した「最初の不一致位置の2・3位token」とは探索位置が異なるため、v3.2-smallで別に評価した。

JWTD 400件では、通常winnerが最尤token列に従う場合だけ各位置の上位3tokenを走査し、確率比0.25超のprefixを辞書latticeへ戻した。95件から418表層を得て、最終169誤りのうち24件で正解表層を含んだ。ただし厳格な「確率比0.65以上、同じ文字数、連続2文字以内、変更文字は漢字同士」まで固定すると、通常poolにない新規候補は2件だけだった。正解の`施設秘書 → 私設秘書`と誤候補の`鼻にかける → 花にかける`が1件ずつで、候補追加としても汚染を避けられなかった。

候補を直接選ぶ閾値はJWTDだけで比較した。漢字同士の条件では確率比0.25 / 0.35 / 0.50 / 0.65 / 0.75 / 0.85のtop-1が221 / 227 / 230 / **234** / 233 / 232となり、0.65は現行231件から3件改善、exact悪化0だった。条件を0.65へ固定してAJIMEEへ適用すると168→165、改善0・悪化3となったため不採用とした。AJIMEEを見た後の語別・表記別guardは追加していない。

直接選ばず、0.65以上の候補だけを同じv3.2-smallで全文再採点する二段方式も、追加margin 0 / 0.1 / 0.25 / 0.5 / 1.0 / 1.5の全条件でJWTD 231件から変化しなかった。全語彙top-3走査と追加lattice探索を製品へ残す精度根拠がないため、診断実装は削除した。

### 2026-08-11 prefix制約内の安全候補探索

局所prefix修正は、modelが強く支持したprefixで辞書latticeを再探索した後、従来は先頭の1表層だけを安全判定していた。先頭表層が離れた2領域を同時に変更するなどの理由で不採用になると、同じprefixを持つ後続の安全な局所修正も調べず、修正全体を破棄していた。

初回は従来どおり8候補だけを探索し、その中に安全な候補がなかった場合に限って最大32候補へ広げ、同じ文字数、連続2文字以内、ASCII英数字不変、漢字からひらがなへ戻さない、既存候補と重複しない、という既存条件を満たす最初の表層を選ぶようにした。通常N-bestや表示候補数は広げず、追加のmodel推論も行わない。上位8件がすべて遠隔領域も変更し、後続にだけ安全な表層がある合成fixtureで境界を固定した。

| dataset | 従来の最終top-1 | 安全候補探索後 | 改善 / exact悪化 |
| --- | ---: | ---: | ---: |
| JWTD dev 400件 | 231 | **234** | 3 / 0 |
| GSD dev 331件 | 302 | 302 | 0 / 0 |
| AJIMEE regression 200件 | 168 | 168 | 0 / 0 |
| PUD regression 446件 | 349 | 349 | 0 / 0 |
| GSD test regression 323件 | 301 | 301 | 0 / 0 |

JWTDでは`照査時 → 少佐時`、`認メル形で → 認める形で`、`性格で → 正確で`を追加回収した。全32候補を最初から作る方式との同一環境比較では、制約探索p50が7.743 msから3.602 msへ下がった。fallback対象を含むp95は31.212 msから34.702 msへ増えたが、探索自体が発火したのは400件中33件だけで、初回8候補で安全表層が見つかる入力は追加探索を避ける。実製品FFIでもv3.2-smallを使い、文字入力から`ラインハルトが少佐時に艦長を務めた`が先頭候補になることを確認した。

### 2026-08-11 複数局所生成一致の限定cost拡張

greedy生成と完全一致する辞書lattice pathでも、通常winnerとのcost差が長文上限2,500をわずかに超えると、構造的な安全条件を満たしていても候補化できなかった。cost上限だけを3,300まで診断すると、JWTDでは追加対象のうち`ただし、編目の中には中身を書くものがあり → ただし、篇目の中には中身を欠くものがあり`がcost差3,063で正解し、3,100が改善を回収する最小の100単位上限だった。このため3,100へ固定し、それ以上には広げない。

拡張対象は9〜32文字、EOS完了、生成表層と完全一致する辞書経路、元候補と同じ文字数、2〜4個の離れた変更領域、各2文字以内、ASCII英数字不変をすべて満たす場合だけとする。表層圧縮と通常N-bestには従来のcost窓を維持する。追加候補はmodelのgreedy出力そのものであるため、この限定経路では通常のsupplemental marginではなく生成一致を最終選択に使う。追加のmodel推論や表示候補探索はない。

| dataset | 安全候補探索後 | 限定cost拡張後 | 改善 / exact悪化 |
| --- | ---: | ---: | ---: |
| JWTD dev 400件 | 234 | **235** | 1 / 0 |
| GSD dev 331件 | 302 | 302 | 0 / 0 |
| AJIMEE regression 200件 | 168 | 168 | 0 / 0 |
| PUD regression 446件 | 349 | 349 | 0 / 0 |
| GSD test regression 323件 | 301 | 301 | 0 / 0 |

実製品FFIでもv3.2-smallを使い、文字入力から`ただし、篇目の中には中身を欠くものがあり`が先頭候補になることを確認した。coreでは通常上限より外、拡張上限以内、複数局所、model supplementalであることを同時に要求し、通常の既存候補consensusとは別の種類として固定した。

### 2026-08-11 信頼度内の完全格子生成候補

複数局所・表層圧縮という形だけに生成候補を限定すると、greedy全文が辞書latticeで完全に証明できても、差分形状が外れる候補を落としていた。そこで形状制限の外側は、6〜32文字、EOS完了、生成表層と完全一致する辞書経路、通常winnerとのcost差1,000以内をすべて満たす場合だけmodel supplemental候補へ加える。直接の生成一致昇格は行わず、既存の追加model margin 1.5をそのまま要求する。複数局所・表層圧縮には従来の1,500 / 2,500 / 3,100の窓を維持する。

形状制限を外した候補へ通常の長文cost窓まで許す診断では、PUDの`面から測ら → 面から計ら`をcost差1,481で1件悪化させた。これに対してcost差1,000へ固定するとこの候補を除外し、cost差937の`外在42 → 外在史に`だけを追加回収した。閾値固定後の全評価は次のとおり。

| dataset | 複数局所cost拡張後 | 完全格子候補後 | 改善 / exact悪化 |
| --- | ---: | ---: | ---: |
| JWTD dev 400件 | 235 | 235 | 0 / 0 |
| GSD dev 331件 | 302 | 302 | 0 / 0 |
| AJIMEE regression 200件 | 168 | 168 | 0 / 0 |
| PUD regression 446件 | 349 | **350** | 1 / 0 |
| GSD test regression 323件 | 301 | 301 | 0 / 0 |

実製品FFIでも左右文脈を含む文字入力から`外在史に`が先頭候補になることを確認した。候補は辞書格子で全文を再検証し、モデル生成だけでは追加しない。通常候補の幅、表示候補探索、model推論回数は増やさない。

### 2026-08-11 長文候補の二段階深掘り

[ATOKの高度な変換処理](https://atok.com/other/support/howtouse/android_pro/02set.html)は精度向上と端末性能による速度低下を明示的に分け、[azooKeyの通常変換](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Core/FullInputProcessing.swift)も辞書・接続costに基づく候補探索を後段評価の土台にする。Slimeでは初回表示の探索幅を増やさず、ユーザーが候補末尾まで明示的に進んだ場合だけ候補recallを段階的に広げる。

9文字以上の読みは、初回候補の末尾で従来どおりN-best 16とbounded compound・人名・固定文節候補を追加する。さらにその拡張候補の末尾まで進んだ場合だけ、二段目としてN-best 32を追加する。8文字以下は従来の一段N-best 32のまま、N-best 64は長文p95が大きいため追加しない。自動順位、ライブ変換、ニューラル再採点候補、最初の候補表示は変更しない。

`slime-recall`にも`deepened`段階を追加し、製品と同じく第一拡張の`expanded / compound / personal name / fixed segment`を先に評価してから、第二拡張で初めて到達する候補だけを`deepened`へ分類した。重複を除いた各評価セットの回収数は次のとおり。

| dataset | initial | expanded | compound | fixed segment | deepened | missing |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| JWTD dev 400件 | 269 | 21 | 0 | 4 | **21** | 85 |
| AJIMEE 200件 | 144 | 6 | 0 | 2 | **9** | 39 |
| PUD 446件 | 390 | 5 | 3 | 1 | **2** | 45 |
| GSD dev 331件 | 331 | 0 | 0 | 0 | 0 | 0 |
| GSD test 323件 | 323 | 0 | 0 | 0 | 0 | 0 |

第二段階はJWTD 21件、AJIMEE 9件、PUD 2件を新たに選択可能にした。GSD dev/testは全件が初回候補内にあり変化しない。C ABI回帰では長文の初回候補、第一拡張、第二拡張を順に通り、第二拡張で初めて追加された候補をindex選択して確定できることまで確認した。

Apple M3、Release、同じ50文字の長文を各1,000回測定すると、N-best 10は7.468 ms/op、16は14.823 ms/op、32は51.764 ms/opだった。32探索は初回入力や第一拡張では実行せず、第一拡張の末尾へさらに進んだ操作にだけ約52 msのcostを限定する。

### 2026-07-25 コスト上書きハックの除去とスコア尺度の一貫化

`いいかんじ`が`いい漢字`へ変換される不具合の調査で、must-passを通すための語別ハックが変換全体を歪めていたことが判明した。次の方針へ改めた。

1. **語別のコスト上書きを廃止する。** `build.rs`が`かんじ→漢字`の単語コストを4191から500へ書き換えていたため、格子内のあらゆる文脈で漢字が過剰に選ばれていた（`いい+漢字(500)`が辞書のフレーズエントリ`いい感じ(2264)`に勝つ）。辞書コストはMozc抽出値をそのまま使う。
2. **テスト文の丸写しエントリも廃止する。** `せいどをたかめる→精度を高める`等のgolden文をcost 500の合成エントリとして辞書へ注入していた。これらはベンチの体裁だけを整え、devset指標には寄与していなかった（除去前後でacc@1 0.2925、acc@10 0.6725、MRR 0.4298が完全一致）。
3. **全読み一致候補はBOS/EOS接続コスト込みで採点する。** 従来は候補窓で生の単語コストと接続コスト込み格子パスを混在ソートしており、`イイ感じ`が`いい感じ`より上に来る等の歪みがあった。
4. **現モデルで原理的に解けないケースは`#[ignore]`で追跡する。** 漢字/感じ、精度/制度、箸/橋は同じ名詞クラスで接続行列では区別できない。該当goldenは`context_dependent_golden_cases`（slime-core）と`semantically_ambiguous_noun_needs_context`（slime-converter）として赤のまま残し、文脈モデル導入で解消する。コスト上書きや辞書注入で緑にしない。

あわせて`evaluate-dev.sh`/`evaluate-ajimee.sh`がmacOS標準bash 3.2の`set -u`で空配列展開に失敗して実行不能だった問題を修正した。

### 2026-08-02 非ニューラル軽量rankerの比較

「文脈を使うためにLLMが必須か」を判断するため、候補集合を変えずに順位だけを変える軽量方式を比較した。すべて実験用実装であり、独立データで改善しなかった方式は製品コードへ残していない。

| 方式 | JWTD dev acc@1 | held-out AJIMEE acc@1 | 判断 |
| --- | ---: | ---: | --- |
| 現行cost | 0.2925 | 0.535 | baseline |
| 語/skip bigram（頻度加点） | 最大0.295 | 改善なし | 効果が小さすぎるため不採用 |
| 文字bigram/trigram境界素性 | 0.295 | 0.525 | held-outで-1.0ptのため不採用 |
| 疎な判別reranker（小規模学習） | 小規模注釈holdoutで+0.24pt | 0.530 | held-outで-0.5ptのため不採用 |

この結果は「LLMが必須」を意味しない。小規模な頻度表や数百例の判別学習では、残っている意味的な同音異義語を一般化できないことを示す。原著で改善したstructured SVMは約4万文、Mozcは大規模コーパスと約3,000クラスを使っており、今回の学習規模とは桁が異なる。

次の比較対象は、(1) ライセンスを確認できる数万文以上の形態素注釈データで学習したphrase/POS classモデル、(2) 既に+12ptを確認した22.5MパラメータのN-best専用ニューラルrankerの蒸留・高速化、とする。後者は文章生成やクラウド送信を行うLLM機能ではなく、候補10件を採点するローカルの小型言語モデルである。採用条件はAJIMEEだけでなく開発セットとbalanced corpusでも改善し、p95 latency、配布サイズ、ライセンスの全条件を満たすこととする。

実装面では、`CandidateRanker::ranking_cost_with_context`、注釈済み`surface/reading`コーパスを直接評価できる`annotated`形式、旧ranker APIとの互換性だけを残した。これにより次のモデルを同じN-best・指標・latency測定で比較できる。

`さがつく→さが付く`のような部分ひらがな経路を一般則だけで抑える案も検証した。接続格子から「表記=読み」の内容語nodeを除くと対象例は`差が付く`へ改善したが、JWTD-train dev 400件のacc@1が0.2925から0.090へ、acc@10が0.6725から0.220へ大幅悪化した。活用語尾・通常のひらがな表記まで候補集合から失うため不採用とし、語別cost上書きも行わない。この失敗型は、品詞大分類を保持したphrase/POS modelまたは文脈rankerで解く。

### 2026-08-02 ニューラルrankerのconfidence gate

22.5M rankerの全件実行を避ける第一段として、既存ラティスの第1候補と次点候補のcost差が閾値を超える場合はニューラル採点を省略する方式を、JWTD-train dev 400件で比較した。`--neural-max-cost-gap N`はこの評価専用gateであり、製品コードにはまだ組み込んでいない。M3、Q5_K_M、lambda 0.8の同一環境・同一条件で連続測定した結果:

| 最大cost差 | 採点件数 | 省略率 | acc@1 | MRR@10 | p50 | p95 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 250 | 148 | 63.0% | 0.3350 | 0.4617 | 11.3 ms | 44.8 ms |
| 500 | 219 | 45.3% | 0.3675 | 0.4828 | 17.7 ms | 50.2 ms |
| 750 | 283 | 29.3% | 0.4025 | 0.5033 | 22.0 ms | 48.8 ms |
| 1000 | 326 | 18.5% | 0.4225 | 0.5164 | 27.3 ms | 56.0 ms |
| 2000 | 387 | 3.3% | 0.4300 | 0.5232 | 33.4 ms | 62.2 ms |
| gateなし | 400 | 0% | 0.4225 | 0.5185 | 36.0 ms | 66.3 ms |

閾値1000はacc@1を維持して18.5%の推論を省き、p95を約15%削減した。一方、p95 56 msは20 ms予算を大きく超える。cost差だけでは意味的曖昧性の信頼度を十分に判定できず、gate単独は不採用とする。次段は、より小さい蒸留モデル、候補token数を減らす識別的head、またはCPUを含むruntime比較が必要である。閾値はdevでのみ調整し、held-out AJIMEEを繰り返し最適化には使わない。

### 2026-08-09 ニューラル順位変更の採用マージン

ニューラル再採点が元の第1候補とは別の候補を選んだ場合だけ、両者の補間score差に最小値を設ける案を比較した。`--neural-min-margin X`は評価専用で、同じニューラル採点結果から複数の値を一度に比較できる。zenz-v3.2-xsmall、先頭3候補、base cost差1000以下、lambda 0.7を固定し、held-outは使わずJWTD-train dev 400件とUD GSD dev 331件だけを測定した。

| 最小score差 | JWTD dev acc@1 | GSD dev acc@1 |
| ---: | ---: | ---: |
| 0 | **0.4075** | 0.8248 |
| 0.1 | 0.3950 | 0.8278 |
| 0.25 | 0.3950 | 0.8308 |
| 0.5 | 0.3950 | 0.8308 |
| 1 | 0.3850 | **0.8338** |
| 2 | 0.3550 | **0.8338** |
| 4 | 0.3200 | 0.8187 |

GSDでは曖昧な順位変更を抑える効果があった一方、JWTDでは最小の0.1から悪化した。単一のscore差はドメインをまたぐ信頼度になっていないため、全変換共通の採用条件には加えず、held-out評価も行わなかった。後述の右文脈対応では「右文脈が実在する編集時」という別境界で0.1をdev固定し、右文脈なしはmargin 0のまま維持する。今後はscore差だけでなく、入力長、文脈有無、base cost差を含む校正済みconfidenceを独立した学習・評価境界として扱う。

### 2026-08-09 EOSを除外した候補score

[azooKeyKanaKanjiConverterの現行候補評価](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/8e3a6eb89e088efd868aa28dadb74c697df4e6fb/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Zenzai/Zenz/ZenzCandidateEvaluator.swift)は、候補tokenごとにモデルの最尤tokenとの一致を確認し、候補末尾のEOSを順位scoreに使わない。Slimeは候補とEOSの合計対数尤度をN-bestへ補間していたため、`--neural-score-mode`を追加し、同じdecode結果からEOSの有無とtoken平均を比較した。公式実装は制約付き逐次探索であり、Slimeの上位3候補再ランキングと同一方式ではないが、EOSを候補表記の評価から分離する根拠として用いた。

JWTD devとGSD devだけで、EOS除外・lambda 0.85を凍結した。token平均は両devで悪化したため不採用とした。凍結後にAJIMEEとGSD testを各1回だけ現行方式と比較した。

| dataset | EOS込み / lambda 0.7 acc@1 | EOS除外 / lambda 0.85 acc@1 | MRR変化 | MinCER@1変化 |
| --- | ---: | ---: | ---: | ---: |
| JWTD-train dev 400件 | 0.4075 | **0.4200** | 0.4943 → 0.5010 | 0.0750 → 0.0742 |
| UD GSD dev 331件 | 0.8248 | **0.8580** | 0.8885 → 0.9062 | 0.1611 → 0.1354 |
| held-out AJIMEE 200件 | **0.6350** | **0.6350** | 0.6869 → 0.6869 | 0.0508 → 0.0506 |
| UD GSD held-out test 323件 | 0.8266 | **0.8638** | 0.8863 → 0.9074 | 0.1605 → 0.1202 |

製品FFIもEOS除外scoreとlambda 0.85へ切り替えた。xsmall実モデルによる50回の明示変換はp50 7.90 ms、p95 12.45 msで、候補数3・base cost差1000以下という既存の実行境界は維持する。AJIMEE非悪化と別domain held-out改善を同時に満たしたため採用する。

### 2026-08-09 costで絞ったニューラル候補プール拡張

[ATOKの公式変換エンジン説明](https://atok.com/info/features/engine.html)は、従来のかな漢字変換に深層学習で抽出した特徴を組み合わせ、直前に確定した単語だけでなく文脈上自然な文章を優先する方針を示している。azooKeyの現行zenz実装も固定3候補の事後比較ではなく、tokenごとの制約付き探索で候補を評価する。これらはSlimeの候補数を直接決める根拠ではないが、base上位3件だけにニューラル判断を閉じる必要はない。そこでEOS除外後のscoreを固定し、ニューラル対象の候補数と補間weightをJWTD/GSD devだけで再評価した。

| 設定 | JWTD dev acc@1 | GSD dev acc@1 | 判断 |
| --- | ---: | ---: | --- |
| 現行: 3候補、lambda 0.85 | **0.4200** | 0.8580 | 比較基準 |
| 5候補、lambda 0.7 | **0.4200** | **0.8671** | MRR/MinCERも改善 |
| 8候補、lambda 0.7 | 0.4400 | **0.8671** | 長文の推論量が増える |
| 10候補、lambda 0.7 | **0.4425** | **0.8671** | AJIMEE p95が228.8 msのため不採用 |

10候補はheld-out AJIMEEでacc@1を0.635から0.720へ上げたが、同じ連続測定でp50は8.7 msから52.6 ms、p95は24.5 msから228.8 msへ悪化した。精度だけを見れば最大だが、明示変換の停止時間として許容できないため製品へ入れない。

5候補について、第1候補とのbase cost差で遠い末尾候補を除く`--neural-max-candidate-cost-gap`を追加した。上位2候補の既存gateは1000のまま、候補末尾の上限だけをdevで比較した。

| 末尾候補の最大cost差 | JWTD dev acc@1 / 平均候補数 | GSD dev acc@1 / 平均候補数 |
| ---: | ---: | ---: |
| 500 | 0.3625 / 2.97 | 0.8399 / 2.43 |
| 1000 | 0.4125 / 3.52 | **0.8671** / 2.91 |
| 1500 | **0.4200** / 4.23 | 0.8640 / 3.61 |
| 上限なし | **0.4200** / 5.00 | **0.8671** / 4.99 |

JWTD top-1を維持できる最小の1500を凍結し、held-outを各1回だけ評価した。

| dataset | 現行 acc@1 | 5候補・cost差1500 acc@1 | MRR変化 | MinCER@1変化 |
| --- | ---: | ---: | ---: | ---: |
| held-out AJIMEE 200件 | 0.6350 | **0.6950** | 0.6869 → 0.7361 | 0.0506 → 0.0429 |
| UD GSD held-out test 323件 | 0.8638 | **0.8700** | 0.9074 → 0.9134 | 0.1202 → 0.1125 |

製品は最大5候補、末尾cost差1500、base上位2件のcost差1000、lambda 0.7へ変更した。履歴、ユーザー辞書、入力ミス訂正などの保護候補は従来どおり対象外である。runtimeの並列候補上限も3から5へ揃え、4・5番目だけの2回目decodeを防いだ。別プロセスがCPUを約386%使用している高負荷下の50回連続参考値では、現行3候補がp50 21.1 ms / p95 77.5 ms、5候補の単一decodeがp50 20.1 ms / p95 99.1 msだった。絶対値はアイドル時の性能値として扱わないが、中央値は同等で、tail増加を最大5候補とcost上限で制御したうえでheld-outの改善を採用する。

### 2026-08-09 候補prefix共有と8候補案の評価

JWTD devの辞書候補回収率はoracle@10 67.25%、@32 78.25%、@64 83.00%だったが、無条件の探索幅32はp95 101.8 ms、64は413.3 msであり、N-bestを広げるだけでは製品の初回変換へ入れられない。一方UD GSD devはoracle@10で99.09%に達しており、長文の近接候補をニューラル層へ渡す経路を優先した。

従来のニューラル実装は`context + reading`のpromptを全候補で共有していたが、候補表層は各sequenceへ全文を複製していた。長文候補は先頭tokenの大部分が一致するため、候補token列をtrie化し、分岐まで同じKV stateを複数sequenceで共有するよう変更した。100件・444候補の固定比較では、最終精度とMRRは完全一致したまま、別プロセスがCPUを約196%使用する高負荷下でp95が39.3 msから29.2 msへ短縮した。絶対値ではなく同一条件の差だけを採用根拠にする。

この実装で末尾cost差1500を維持したまま、最大候補数をdevで再比較した。

| 最大候補数 | JWTD dev acc@1 / MRR@10 / p95 | GSD dev acc@1 / MRR@10 / p95 |
| ---: | ---: | ---: |
| 5 | 0.4200 / 0.5085 / 34.5 ms | 0.8640 / 0.9130 / 14.0 ms |
| 8 | **0.4325** / **0.5217** / **33.4 ms** | **0.8640** / **0.9152** / **12.2 ms** |
| 10 | **0.4325** / 0.5210 / 37.3 ms | 未評価 |

10候補はJWTD top-1を追加改善せずtailだけ増えたため不採用とし、最大8候補をdev案として凍結した。base上位2件のcost差1000、各候補の先頭候補とのcost差1500、lambda 0.7、EOS除外、保護候補を再採点しない境界は変更しない。

凍結後のheld-outではAJIMEEが0.6950から0.7150へ改善した一方、GSD testは0.8700から0.8669へ1件悪化した。このため、この時点では入力長を区別しない固定8候補化を採用しなかった。その後、短文5候補・長文のみ拡張する境界で回帰を除き、上記のscorer-ready 5/16候補へ進んだ。prefix trieは候補scoreと順序を変えず、長文候補の重複計算を削減する。

### 2026-08-09 長文の第二段N-best回収

[ATOKの公式説明](https://atok.com/announce/2021/20211201-01.html)は同音異義語を文脈で変換し分ける方針を示し、[azooKeyの`ZenzCandidateEvaluator`](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/8e3a6eb89e088efd868aa28dadb74c697df4e6fb/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Zenzai/Zenz/ZenzCandidateEvaluator.swift)は固定候補の再採点だけでなく、token不一致からprefix制約を返して探索を修正する。Slimeはまだ固定N-best poolへ依存するため、初回候補を無条件に広げず、ユーザーが候補末尾へ進んだ場合の第二段回収を比較した。

JWTD dev 400件で表示上限10を固定し、内部探索幅だけ10/12/16/20/24/32へ広げた。top-1は全条件0.2925で不変、top-10回収率は幅10の0.6725から幅16で0.7075、幅24で0.7150まで上がった。一方、候補生成p95は幅10の8.61 ms、幅16の14.90 ms、幅20の21.40 ms、幅24の29.16 ms、幅32の48.70 msだった。20 ms予算内で最大の改善を得る最小幅16を長文の第二段探索として固定した。GSD dev 331件は幅10/16でtop-1 0.8127、top-10 0.9909のまま、p95は0.58→0.76 msだった。

製品経路と同じく初期10件を残して幅16の候補を後ろへ追加した和集合では、JWTD devの正解回収が269/400（0.6725）から290/400（0.7250）へ21件増え、GSD devは328/331（0.9909）から329/331（0.9940）へ1件増えた。最大5候補のニューラル再採点も幅10/16でJWTD top-1 0.4200、GSD top-1 0.8640と完全に同じで、JWTD 400件の先頭候補表層にも差がなかった。したがって初回候補は幅10のまま維持し、9文字以上では候補末尾に到達した後だけN-best 16を追加する。8文字以下の既存N-best 32、bounded compound、fixed-segment回収は維持する。

幅16をdevで固定した後、held-outを各1回評価した。初期10件との和集合回収率はAJIMEE 200件で167/200（0.835）から172/200（0.860）へ5件、GSD test 323件で320/323（0.9907）から322/323（0.9969）へ2件増えた。幅16単独のtop-1はAJIMEE 0.530、GSD test 0.8204で幅10と同じだった。候補生成p95はAJIMEEで10.96→20.28 ms、GSD testで0.49→0.70 msだが、初回表示ではなく候補末尾到達後の一度だけに限定する。

### 2026-08-09 人名の候補回収

UD GSDの`PROPN`だけを分類すると、dev 21件はtop-10で19件、top-16で20件、top-64で21件、held-out test 12件はtop-10で11件、top-16で12件を回収できた。devの残り1件は単独の名が既に候補33位に存在する順位問題であり、姓+名の生成不足とは分けて扱う。

一方、姓と名が辞書に別々に存在しても、各文節8候補の一般複合語探索では同じ読みの名が多いと正解表記を失う。そこで候補末尾到達後だけ、Mozcの姓POSと名POSを全読み境界で接続し、各部分64件、最終64件に制限した人名候補を追加する。合成fixtureでは一般複合語で欠落する表記を回収し、初回候補の先頭とライブ変換は変えない。

初回順位にも完全な姓名辞書entryを使う案を別途検証した。2文字姓と2文字名がそれぞれ人名POSで確認でき、完全名のcostが7,800以下の約1.4万表層だけを文脈用逆引き索引へ加えると、GSD trainは6件改善・1件悪化、testは3件改善・0件悪化したが、devで正解1件を別人の表記へ壊し、さらにtrainの4件を別の誤表記へ動かした。人名辞書は全人物を網羅しないため、競合entryがないことを「一意な姓名」と解釈できない。候補末尾でのrecall回収は維持するが、初回順位への姓名索引は採用しない。

Common Voice由来の複合語859件では`initial=849 / expanded=1 / compound=3 / personal_name=0 / missing=6`で、通常top-1は776件のまま変化しなかった。一般的な6文字の姓+名の読みをrelease buildで1,000回測定した人名探索は0.011 ms/op、27候補だった。評価用JSONには対象語の`target_upos`を保持し、`slime-recall`は人名専用回収を一般複合語と別段階で報告する。

地名の未回収を調べると、Common Voice注釈には、辞書上の都道府県語に続く市区町村を一般語の読みで分割した例が含まれていた。これを製品の候補不足として最適化すると誤変換を強化するため、複合語fixture生成時に、Mozcの地域POSで確認できる地名へ未確認の行政区画末尾が続くwindowを除外する。正しい地域POS同士の連結は残す。4,100行から21 window、重複除去後19候補が除外され、fixtureは859件から840件、未回収は6件から2件になった。残る2件は`内的応化`と`硬軟両様`で、地名専用処理の根拠にはしない。

### 2026-08-09 直前文脈末尾の語彙再利用

候補全体の完全一致だけでなく、長い変換候補の辞書segmentが直前文脈の末尾に既出なら、その候補を最大2,000 costだけ昇格する。対象は2文字以上かつ漢字を含む変換済みsegmentに限定し、閉じ括弧などの末尾記号は無視する。文書中の任意位置を`contains`で探す案は、AJIMEEで「冷却水」に含まれる「冷却」が誤った「冷却型」を昇格し、top-1を0.530から0.525へ悪化させたため棄却した。

末尾一致へ絞ると、JWTD dev 400件では「『日本書紀』」に続く「、そして書紀が引用する」を回収してtop-1が0.2925から0.2950、MRR@10が0.4298から0.4314へ改善した。GSD dev 331件はtop-1 0.8127、MRR@10 0.8795のまま、両devを合わせた先頭候補の悪化は0件だった。2,000/2,500/3,000を比較して追加改善がなかったため、最小の2,000に固定した。

固定後のheld-outでは、AJIMEE 200件のtop-1 0.530、MRR@10 0.6236、GSD test 323件のtop-1 0.8204、MRR@10 0.8822がすべて不変だった。製品設定のニューラル再採点でもAJIMEE top-1 0.695、GSD test top-1 0.8700は不変だった。候補表層配列はAJIMEE/GSD testの全件で同一で、初回探索幅や候補数も変更しない。

### 2026-08-02 大規模な非生成判別reranker

数百例では学習量が不足していた可能性を切り分けるため、JWTD v2 train全体から同じ読み信頼性フィルタを通過した48,393件を生成した。元JSONL行番号の`mod 10 == 0`を固定dev（4,788件）、残り43,605件を学習候補とし、`source_split + index`の複合identityで重複を除外する。AJIMEEはJWTD test由来なので引き続き最終held-outとした。

候補表層の文字unigram/bigram/trigramと、直前3文字の左文脈との交差素性を262,144次元へhashし、averaged perceptronでN-bestを再順位付けした。10,000学習例、3 epoch、1,048,576 byteの`f32`重みで、分離devのweight sweepは0.5が最良だった。

| dataset | baseline acc@1 | reranker acc@1 | MRR@10 | MinCER@1 | 判断 |
| --- | ---: | ---: | ---: | ---: | --- |
| JWTD-train固定dev 4,788件 | 0.3139 | 0.3926 | 0.5041 | 0.0769 | +7.87pt、weight選択に使用 |
| held-out AJIMEE 200件 | 0.5350 | 0.5250 | 0.6349 | 0.0641 | -1.0ptのため不採用 |

AJIMEEではMRRとMinCERはbaseline（0.627、0.068）より改善したが、主指標のtop-1完全一致が悪化した。分離devだけの大幅改善を根拠に製品へ入れると、Wikipedia修正窓の表記傾向へ過適合する。数万件級へ増やすだけでは、局所文字素性が意味的同音異義語を一般化できないという問題は解けない。

### 2026-08-02 22.5M teacherから1MB studentへの順位蒸留

正解表記ではなく、品質効果が確認済みのzenz-v3.1-xsmallがlambda 0.8で選んだN-best先頭候補をteacher labelとして、同じ1MB hashed perceptronへ蒸留した。teacherは学習時だけ使い、student推論は文字素性の加算だけである。

| teacher例 | 固定dev acc@1 | baseline比 | student採点p95 | model |
| ---: | ---: | ---: | ---: | ---: |
| 2,000 | 0.3480 | +3.41pt | 0.035 ms | 1,048,576 bytes |
| 10,000 | 0.3594 | +4.55pt | 0.020 ms | 1,048,576 bytes |

例数を5倍にしても改善は+1.15ptに留まり、teacher本体のdev `acc@1 0.423`級を再現できなかった。速度とサイズは十分だが、studentの局所hash表現に意味判断を保持する容量がない。dev段階でGo条件を満たさないためAJIMEEを追加評価せず、この蒸留方式は不採用とする。次にニューラルを比較するなら、文字n-gramへの蒸留ではなく、共有token embeddingと低rank/小hidden層を持つN-best専用studentを学習し、5–10M parameter以下で品質・p95・配布サイズを同時に測る。

### 2026-08-03 共有bi-encoder student

局所hash表現の容量不足を切り分けるため、MLXを使う評価専用の文字Transformer studentを実装した。左文脈末尾40文字と読みを1回だけencodeし、最大10件の候補表層は同じencoderで並列encodeする。両者のvector、要素積、絶対差を小さなMLPへ渡し、既存Viterbi costへ加算する。文章生成、辞書外候補、ネットワーク送信は行わない。64件をtrain/dev兼用にしたsanity checkではteacher順位を10 epochで100%再現し、loss/optimizer/model wiringが正常であることを先に確認した。

zenz-v3.1-xsmall（lambda 0.8）の先頭候補をteacher labelとした10,000件で、hidden 192、3層、6 head、1,737,985 parameterを3 epoch学習した。weightは固定devだけで選び、AJIMEEへ当てる前に0.5へ凍結した。

| dataset / epoch | baseline acc@1 | student acc@1 | MRR@10 | 備考 |
| --- | ---: | ---: | ---: | --- |
| JWTD-train固定dev 4,788件 / epoch 1 | 0.3139 | 0.3193 | 0.4499 | weight 2.0 |
| JWTD-train固定dev 4,788件 / epoch 2 | 0.3139 | 0.3244 | 0.4538 | weight 1.0 |
| JWTD-train固定dev 4,788件 / epoch 3 | 0.3139 | **0.3331** | **0.4585** | weight 0.5を凍結 |
| held-out AJIMEE 200件 | 0.5300 | **0.5200** | 0.6216 | baseline MRR 0.6236、-1.0pt |
| UD Japanese GSD外部dev 331件 | 0.6405 | **0.5921** | 0.7270 | 凍結weight 0.5、-4.84pt |

10候補を採点する単体latencyはApple GPUでp50 3.62 ms、p95 6.86 ms、重みは6,956,261 bytes（FP32。parameter数からのFP16概算3.48 MB）だった。速度とサイズは22.5M teacherより大幅に改善したが、held-outの主指標を悪化させたため製品へは組み込まない。候補生成自体も同じAJIMEEでp95約26 msを要するため、単純な常時追加は全体20 ms予算にも収まらない。

固定devで+1.92pt出てもheld-outで反転し、後述のnews/blog外部devではさらに大きく悪化した。同じJWTD修正窓10,000件へのteacher蒸留をモデル拡大だけで続ける根拠はない。AJIMEEはこの設定の最終報告に一度使用済みなので、以後の調整には戻さない。

### 2026-08-10 両側文脈cross-encoder student

[azooKey/Zenzai v3](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Docs/zenzai.md)は左文脈に加えて右文脈を独立したtagで扱う。bi-encoderで文脈と候補を別々に圧縮した際の情報損失を切り分けるため、`[CLS] 左文脈 [RIGHT] 右文脈 [INPUT] 読み [OUTPUT] 候補`を候補ごとにjoint encodeするcross-encoderを評価専用スクリプトへ追加した。RustのN-best exportも`right_context_text`を保持する。フィールドがない既存exportは空文字として読み込むため、従来artifactとの互換性は維持する。

JWTD train先頭10,000件とGSD train 1,940件を5回反復した19,700件（正解候補reachable 16,317件）を学習側に使用した。固定devはJWTD 400件とGSD dev 331件の合計731件。hidden 128、2層、4 head、793,217 parameterを3 epoch学習し、既存Viterbi scoreへのweight 0.5を比較した。

| dataset | baseline acc@1 / MRR@10 | cross-encoder acc@1 / MRR@10 | 6文字以上だけ適用 | 判断 |
| --- | ---: | ---: | ---: | --- |
| JWTD dev (400) | 0.2950 / 0.4314 | **0.3625 / 0.4784** | **0.3625 / 0.4784** | 長文domainでは改善 |
| GSD dev (331) | **0.8550 / 0.9044** | 0.8278 / 0.8905 | **0.8550 / 0.9044** | 全面適用は回帰 |
| AJIMEE (200) | 0.5300 / 0.6236 | 0.5350 / 0.6282 | 0.5300 / 0.6257 | 長さgateはtop-1不変 |

10候補の単体latencyはApple GPUでp50 2.36–4.97 ms、p95 2.85–9.65 msの範囲、重みは3,175,763 bytes（FP32、FP16概算1.59 MB）だった。6文字以上のgateなら短いGSDを構造的に変更せずJWTDの改善を残せるが、この境界はAJIMEEで複数値を見た後に選んでいる。AJIMEEをheld-outと呼び直すことはできないため、独立した新規held-outを得るまで製品採用はNo-Goとし、モデルや学習データはbundleしない。

学習スクリプトは最終epochを無条件に保存せず、devのacc@1、MRR、より小さいweightの順で最良checkpointを保存する。既存の非空outputも上書きしない。JWTD/AJIMEEは[CC BY-SA 3.0](https://creativecommons.org/licenses/by-sa/3.0/)、GSDは[CC BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/)のShareAlike条件を持つデータに由来する。いずれも商用利用自体は許すが、将来モデルを配布する場合は帰属・同一条件をアプリ本体とは分けて確認する。現時点のartifactは評価専用で、商用bundleには含めない。

#### 独立held-out: UD Japanese PUD

6文字以上のgateをAJIMEE確認後に選んだ汚染を解消するため、設定を変更する前に[UD Japanese PUD](https://universaldependencies.org/treebanks/ja_pud/index.html) r2.18（commit `4abd575c57bfa125dd4bc564f2ceb8973bbbf422`）を独立held-outとして固定した。PUDはGSDと別のnews/wiki 1,000文で構成され、公式に全体がtest split、ライセンスはCC BY-SA 3.0とされている。学習、epoch選択、weight・入力長gateの調整には使用しない。

```sh
just build-pud-heldout
just evaluate-pud-heldout --json
```

取得するCoNLL-UはSHA-256 `7ac51383189bdf5513102f9f8ac7ee1b967d5449f8c2dc1a9108fa21a8f1a688`へ固定する。単語単位では455件を抽出できたが、読みが全て5文字以下で6文字gateを一度も通らないため、cross-encoderの採否には使わない。代わりに各曖昧語を含み句読点をまたがない6〜20文字の連続句を決定的に切り出す。かな・記号は原文表層、辞書で一意に読める語は辞書読み、残りはUniDic表層発音を使い、入力長だけを満たすための不自然な読みを作らない。

長文held-outは446件すべてが6文字gate対象で、現行辞書baselineは`acc@1 0.6547`、`acc@10 0.8744`、`MRR@10 0.7390`だった。390件は正解候補がN-best内にあり、未知domainの長文順位付けと候補生成を分けて報告できる。データは`target/evaluation`だけに保存し、製品bundleへ含めない。

PUDを一度も参照せず、JWTD 10,000件とGSD train 1,940件の5回反復から作った19,700件でcross-encoderを再学習した。hidden 128、2層、4 head、927,873 parameter、3 epochの設定と、先に固定した6文字gate・weight 0.5を変更せずPUDへ適用した結果、`acc@1`は`0.6547 → 0.6390`（292件→285件、-1.57pt）、`MRR@10`は`0.7390 → 0.7308`へ悪化した。10候補の再採点latencyもp50 8.67 ms、p95 12.81 ms、FP32重み3,714,387 bytesだった。

参考としてweight 0.1では`acc@1 0.6592`まで上がるが、独立held-outを見た後のweight変更は採用判定に使わない。固定条件で主指標を悪化させたため、このcross-encoderはNo-Goとし、製品コードや商用bundleへ組み込まない。PUDはこの方式の最終判定に使用済みなので、同じモデル系列の追加調整では今後held-outとして再利用しない。

## UD Japanese GSD 外部ドメイン評価

[UD Japanese GSD](https://universaldependencies.org/treebanks/ja_gsd/index.html) r2.18（commit `33e7310b58308e85fd2b33a2fc3ef3e434f821c7`）を、CC BY-SA 4.0の評価専用キャッシュとして追加した。Wikipedia修正履歴ではなくnews/blogを原文とし、手動由来の短単位語境界、UniDic品詞、表層発音を持つ。製品bundleには含めない。

```sh
just build-balanced-devset
just evaluate-balanced-dev --json
```

生成器は各文から、同梱辞書で同じ読みを持つ漢字表層が2件以上ある内容語を最大1件選ぶ。入力はUniDic表層発音、期待値は原文表層、文脈は現在語より前の最大40文字である。句読点と空白も左文脈に保持する。train 7,050文から学習用注釈列と1,940件、dev 507文から331件、test 543文から323件を生成した。単一の表記だけを正解とするため絶対精度より差分を重視し、testは方式とweightをdevで凍結した後にだけ使う。

初回baselineはdev `acc@1 0.6405`、`acc@10 0.9758`、test `0.6966` / `0.9814`だった。正解のほぼ全てがN-best内にあり、文脈順位モデルを測る目的に適している。

### 直前語context bigram

UD trainの表層・読み列から、直前語表層→現在語（表層・読み）の81,266遷移を数えた。候補先頭segmentと左文脈末尾が一致する場合だけ、`weight × (floor(log2(count)) + 1)`を既存costから引く評価専用rankerである。devだけでweightを探索し、同率の大きな値より副作用の小さい1500に凍結した後、testと他データセットを各1回評価した。

| dataset | baseline acc@1 | context bigram acc@1 | MRR@10 | 判断 |
| --- | ---: | ---: | ---: | --- |
| UD GSD dev 331件 | 0.6405 | **0.7190** | 0.8139 | +7.85pt、weight選択に使用 |
| UD GSD held-out test 323件 | 0.6966 | **0.7647** | 0.8461 | +6.81pt、同一news/blog domainで再現 |
| JWTD-train dev 400件 | 0.2925 | **0.2900** | 0.4278 | -0.25pt |
| AJIMEE 200件 | 0.5300 | **0.5300** | 0.6199 | top-1差なし、MRR悪化 |

UD testの候補生成込みlatencyはbaseline p95 0.85 ms、rankerあり0.86 msだった。ただしこれは起動時のモデル読み込みを含まない。2.24 MBの注釈テキストでも、無効なword/skip表まで構築した初期実装は冷起動0.69秒・最大RSS 105.1 MBを要した。有効なcontext表だけを構築すると、同一コマンド3回の中央値は0.22秒・最大RSS 49.1 MBまで改善したが、モデルなしの0.09秒・23.8 MBと比べると約0.13秒・25.3 MBの追加である。採点hot pathは軽くても、現形式の起動コストは製品基準を満たさない。

81,266遷移のmatch率はdev 5.97%、test 6.55%だった。少数の高価値な共起だけで同一domainを+6〜8pt改善できるため、LLMや文章生成が必須という仮説は否定できる。一方、Wikipedia系では改善せず、7,050文のexact bigramを汎用モデルとして同梱するのはNo-Goである。次は再配布・商用条件が明確で、複数domainを含む十分な規模の形態素注釈データを確保できた場合に限り、class backoff、pruning、compact格納を評価する。

一般会話を増やす候補としてTatoeba/Tanakaも確認したが、[公式説明](https://www.edrdg.org/wiki/Tanaka_Corpus.html)自身が文章は自然・代表的でなく、統計分析には使うべきでないと警告しているため学習には使用しない。

より適合するBCCWJは書籍、雑誌、新聞、白書、ブログ、ネット掲示板、法律など約1億語に形態論情報を持つが、本文は有償契約対象で再配布不可である。[商業利用料金](https://clrd.ninjal.ac.jp/bccwj/fee.html)は2021年10月以降、2年・社内利用者10人以内で40万円（税別）からであり、成果物の商品化には契約が必要となる。公開UD-BCCWJもannotationがCC BY-NC-SA 4.0で、ライセンス問題により本文は含まれない。契約なしに製品モデル学習へ使わない。

### JWTD + GSD複数ドメイン固定合成

GSD単独のexact contextが他ドメインへ汎化しなかったため、JWTD v2 trainの訂正文から語彙bigram用の`surface/reading`列を追加生成した。元JSONL行番号の`mod 10 == 0`を固定devとして除外し、残る43,605窓を学習側に限定する。生成コマンドは次のとおり。

```sh
just build-jwtd-context-corpus
```

注釈列は7,386,118 bytes、SHA-256 `256b9b4b56f5e7c665934b38243cf6bbd5e431baff93b605adb76d39dc9f0256`である。GSD trainと合わせた固定モデルはword 315,156遷移、context 282,446遷移。word重みはJWTD分離devだけで250、context重みは先のGSD devだけで1500に凍結し、組合せ後に再調整していない。

| dataset | baseline acc@1 | word=250 | word=250 + context=1500 | 判断 |
| --- | ---: | ---: | ---: | --- |
| JWTD固定dev 4,788件 | 0.3139 | 0.3680 | **0.3682** | +5.43pt |
| GSD dev 331件 | 0.6405 | 0.5831 | **0.6828** | word単独は反転、合成は+4.23pt |
| GSD held-out test 323件 | 0.6966 | — | **0.7090** | +1.24pt |
| AJIMEE held-out 200件 | 0.5300 | — | **0.5000** | -3.0pt、No-Go |

AJIMEEの`acc@10`は0.835のままで、MRRは0.6236級から0.6169へ悪化した。GSD testでわずかに再現しても、別held-outの主指標を悪化させるため製品へは入れない。また、`差→が`はJWTD学習側に7文あったが、全体最良のword重み250でも`差がつく`は第4候補のままだった。該当語のために重みを750へ上げても第4候補で、JWTD devの改善幅は+3.86ptまで下がる。単例に合わせた再調整は行わない。

固定合成の候補生成込みp95はJWTD dev 18.4 ms、GSD test 1.41 ms、AJIMEE 48.2 ms。さらに3回の冷起動中央値は1.47秒、最大RSS 204.7 MBで、現在のテキスト読み込み形式は配布要件を大きく超える。これは品質gateを通った後にprune/compact化すべき実装課題であり、品質No-Goのモデルを圧縮する理由にはしない。

## JWTD-train開発セット

AJIMEE-BenchはJWTD v2のtest分割由来のため、コストや言語モデルの調整に使うと過適合する。調整はJWTD v2の**train分割**から自動生成した開発セットで行い、AJIMEE-Benchはheld-outの報告専用とする。

```sh
just build-devset      # 初回のみJWTD v2(約99 MB)を取得し、400件を生成
just evaluate-dev      # 開発セットで評価
```

生成方法（`slime-tools/src/devset.rs`）: 単一diffが`kanji-conversion_a`の文対から誤り周辺の変換窓を切り出し、同梱辞書の表層→読み逆引きで読みを推定する。誤変換前後の表層の読みが両方導出でき、かつ一致しない場合は読み推定を信頼せず破棄する。さらに、読みが一意な表層の最長一致と、Mozcの単語cost・接続costを使う独立した表層Viterbi解析が同一の読みを導いた場合だけ採用する。ページ偏りを避けるため等間隔サンプリングする。

### 2026-08-11 推定読みの二方式一致gate

従来の最長一致だけでは、`シリアスな曲が…`の`曲が`に、別の辞書entry由来の`まが`を割り当てるなど、本来の語境界を跨ぐ推定があった。これを正解として候補生成を調整すると実入力を壊すため、辞書表層の最長一致と、単語cost・接続costを使う表層Viterbiの読みが完全一致する例だけを残す。単体testで`曲が`の最長一致`まが`とViterbi`きょくが`の不一致を固定した。

同じJWTD train入力から信頼できる候補45,280件を受理し、400件を等間隔抽出した。再生成したJSONのSHA-256は`df6287214cb77db4a167386894b52d3745e2fa0234b6b0ce18e008bf5c237326`でバイト一致した。Releaseの生成は10.62秒、最大RSS 368,328,704 bytesだった。新しい400件の辞書baselineはacc@1 126/400（0.3150）、acc@10 279/400（0.6975）、MRR@10 0.4590、MinCER@1 0.0829である。

この400件は旧セットと別の等間隔サンプルである。したがって旧・新の絶対数値差を変換器の改善量とは扱わず、今後の変更はこの固定JSON内で前後比較する。

制約: 許容解は1変種のみ（AJIMEEのような人手の許容解列挙が無い）ため、絶対値はAJIMEEより体系的に低く出る（例: 「ヒトの進化」に対して「人の進化」は不正解扱い）。**相対比較（調整前後の差分）にのみ使う。** 初回計測（2026-07-20、400件）: acc@1 0.293、acc@10 0.673、MRR@10 0.430。

この開発セットの初回運用で、かな数詞パーサが「せんぜん（戦前）」を1000+1000=2000と解釈する誤りを検出し、位取り単位の降順制約（千→百→十）を追加した。

## 元の日本語Wikipedia入力誤りデータセット

AJIMEE-Benchは、日本語Wikipedia入力誤りデータセットv2のtestデータから漢字誤変換200件を抽出し、かな入力、変換範囲、複数の許容解を人手確認した評価用データである。

元データは約70万文対を含み、誤字、脱字、衍字、転字、漢字誤変換など複数の問題が混在する。そのままかな漢字変換評価へ使うのではなく、将来の統計モデルでは次のように扱う。

1. 学習には元データの`train`分割だけを使用する。
2. `kanji-conversion`を抽出し、読みと変換対象範囲を別途生成・検証する。
3. AJIMEE-Benchは元データのtest由来なので、学習や調整には使用しない。
4. must-pass、AJIMEE、balanced corpusの結果を別々に報告する。

## ライセンス

AJIMEE-Benchの評価データと元の日本語Wikipedia入力誤りデータセットはCC BY-SA 3.0。AJIMEE-Benchの`utils.py`と`test_utils.py`はCC0 1.0。UD Japanese GSDはCC BY-SA 4.0。評価データはダウンロードキャッシュとして分離し、利用時は各配布元のライセンスと帰属表示に従う。

## 2026-08-11 明示探索専用の大規模語彙

[SudachiDict](https://github.com/WorksApplications/SudachiDict)のFull辞書
v20260428を再配布せず評価専用に展開した。辞書全体は2,883,177 entry、
固有名詞POSは2,109,561 entryだった。JWTDで通常の64候補にも正解がなかった
84件の診断では、Sudachi tokenizerが期待表記と読みを完全に構成できたものが51件
あった。ただし複数語からなる文章も含むため、1語entryを追加すれば51件すべてを
回収できるという意味ではない。

固有名詞、source cost 5,000以下、ひらがな読み、表記2〜128文字へ絞り、重複を
除いた203,590 entry、7,860,601 bytesの一時パックを作った。語彙本体は公開
repositoryへ追加していない。通常候補へ混ぜず、候補末尾で完全読みだけを引く
`explicit-search-only`として評価した。

一時パックから200件おきに取った1,018件は全件を64候補内で回収した。6,400件おきの
32件を製品の候補操作経路で比較すると、初回回収は両条件15/32、通常の末尾探索は
16/32、明示探索パックは32/32だった。通常・パックありを5組交互に測った中央値は、
通常p50 3.895 ms / p95 13.392 ms、パックありp50 3.845 ms / p95 13.269 msで、
明示索引の追加時間は測定誤差内だった。

初期実装は明示探索語彙まで英単語逆変換索引へ渡し、同じパックの起動が3.32秒
かかった。通常候補へ参加するstandard packだけを逆変換対象にし、読み・表記を
inline小文字列で保持し、明示探索を専用の完全一致索引へ変えると、同一プロセスの
pack load中央値は114.944 msになった。別processの標準起動評価器では、1 entryの
baseline比でmedian +107.161 ms、p95 +106.748 ms、最大RSS +24,150,016 bytesだった。
これは任意の20万件パックを読み込む追加コストであり、通常buildには発生しない。
初回順位を維持しつつ固有名詞を回収する基盤として採用するが、実配布語彙は
ライセンス、由来、頻度、メモリを別途gateする。

## 2026-08-11 辞書制約付きwhole-result一致

[azooKeyの現行`ZenzCandidateEvaluator`](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Zenzai/Zenz/ZenzCandidateEvaluator.swift)は、生成がEOSへ到達した場合に`wholeResult`を返し、候補tokenとの不一致ではprefix制約を返す。Slimeでは生成表層を直接採用せず、既存の局所prefix修正と複数領域一致を適用した後の候補に対し、完全な辞書lattice pathとして検証済みのgreedy全文だけを最後の一致信号として評価した。

JWTDの信頼性gate後400件でcost差500 / 1,000 / 1,500 / 2,500 / 3,100 / 5,000を比較した。3,100は開発側で改善幅が大きかったが、PUDで既存正解を悪化させたため棄却した。PUD参照後に閾値を再調整せず、既存のbase confidence境界と同じ1,000へ戻した。さらに、漢字からひらがなへの表記崩れを禁止し、ASCII英数字列を不変とし、辞書で確定できる姓名を保護する条件を固定した。姓名保護は文字数が変わる全文候補にも拡張し、名前segment内部の削除・挿入を拒否する。

同じ条件でUD Japanese GSD r2.18のdev/test文から句読点をまたがない句を新規生成した。devは323件、testは320件で、testは条件固定後に一度だけ使用した。最終判定は診断器の近似値ではなく、旧`1d74db6`と変更後のRelease FFIを同じmodel・入力・文脈で直接比較した。

```sh
cargo run --release --quiet -p slime-tools --bin slime-balanced-devset -- \
  target/evaluation/ud-japanese-gsd/r2.18/ja_gsd-ud-dev.conllu \
  crates/slime-converter/data/mozc-basic.tsv /tmp/slime-gsd-phrase-dev.json \
  --source-split ud-japanese-gsd-r2.18-phrase-dev --phrase-windows
cargo run --release --quiet -p slime-tools --bin slime-balanced-devset -- \
  target/evaluation/ud-japanese-gsd/r2.18/ja_gsd-ud-test.conllu \
  crates/slime-converter/data/mozc-basic.tsv /tmp/slime-gsd-phrase-test.json \
  --source-split ud-japanese-gsd-r2.18-phrase-test --phrase-windows
```

| dataset | 変更前 | whole-result一致後 | 改善 / 悪化 |
| --- | ---: | ---: | ---: |
| 信頼性gate後JWTD 400件 | 232 | **234** | 2 / 0 |
| PUD phrase 446件 | 346 | **349** | 3 / 0 |
| GSD phrase dev 323件 | 220 | **222** | 2 / 0 |
| GSD phrase test 320件 | 245 | 245 | 0 / 0 |
| AJIMEE 200件 | 167 | 167 | 0 / 0 |

JWTDでは`性的衝動にかられて → 性的衝動に駆られて`と`魔道具 → 魔導具`、PUDでは`賛成だと話 → 賛成だと話し`、`シベリアの第 → シベリアの大`、`評判はよく → 評判は良く`を回収した。5 dataset合計で7件改善、既存正解の悪化は0件だった。通常のmodel補間では選ばれない場合も全文一致を優先するが、EOS完了、6〜32文字、完全な辞書経路、base cost差1,000以内、ASCII不変、漢字保持、姓名保持を一つでも満たさなければ従来結果を維持する。履歴、ユーザー辞書、入力ミス訂正、規則候補は外部再採点要求を作らない既存境界のままである。

## 2026-08-11 遅延生成による33〜40文字のwhole-result一致

32文字を超える入力でも全文の意味選択が必要になるため、GSD train/dev/testとPUDから、
句読点をまたがず、実際に出力した辞書補正後の読みが33〜40文字になる評価窓を生成した。
`--phrase-min-reading`と`--phrase-max-reading`を省略した既存の
`--phrase-windows`は従来どおり6〜20文字であり、長文評価だけ明示的に範囲を変える。
UniDicの生発音ではなく最終的に出力する読みで境界を検証するため、指定範囲外のfixtureを
混ぜない。

```sh
cargo run --release --quiet -p slime-tools --bin slime-balanced-devset -- \
  target/evaluation/ud-japanese-gsd/r2.18/ja_gsd-ud-train.conllu \
  crates/slime-converter/data/mozc-basic.tsv /tmp/slime-gsd-long-train.json \
  --source-split ud-japanese-gsd-r2.18-long-train --phrase-windows \
  --phrase-min-reading 33 --phrase-max-reading 40
```

生成件数はGSD train 462、dev 93、test 84、PUD 129件だった。最初に6〜32文字と
同じ条件で生成上限だけ40文字へ広げると、GSD trainは2件改善・3件悪化、PUDは
1件改善・1件悪化した。`よい → 良い`、`鶏ガラ → 鶏がら`、`事故 → 自己`、
`がっかり → ガッカリ`のように、辞書第1候補とcostが近すぎる表記を全文生成だけで
覆すため、この全面拡張は採用しない。48文字までの拡張もAJIMEEで悪化したため棄却した。

GSD trainだけでcost差の下限0 / 250 / 500 / 750 / 1,000を比較し、既存正解の悪化を
初めて0件にした500を固定した。その後、dev/test/PUDを同じ条件で確認した。

| dataset | 変更前 | cost差500〜1,000 | 改善 / 悪化 |
| --- | ---: | ---: | ---: |
| GSD long train 462件 | 142 | **144** | 2 / 0 |
| GSD long dev 93件 | 33 | 33 | 0 / 0 |
| GSD long test 84件 | 36 | 36 | 0 / 0 |
| PUD long 129件 | 57 | **58** | 1 / 0 |

回収した`おめー`、`問い質し`、`任期を終えた`はいずれも既存N-best内にあり、順位は
それぞれ4位、2位、2位だった。したがって33〜40文字ではモデルが生成した新しい表層を
候補へ追加せず、通常N-bestを先に一度採点する。モデル全体の首位がbase以外の既存候補で、
そのcost差が500〜1,000の場合だけgreedy生成を遅延実行し、EOSまでの全文がその候補と
完全一致した場合に限ってwhole-result一致を記録する。候補内最上位、baseより高score、
model全体の首位という3種類の採点だけによる昇格も比較したが、別datasetで悪化したため、
生成一致を最終確認から外さない。

Release FFIで現行mainと比較すると、信頼性gate後JWTDは234→235、AJIMEEは167のままで、
AJIMEE 200件の出力は全件同一だった。既知の改善・悪化候補とAJIMEEの長文を合わせた
重点21件では15→18、改善3件・悪化0だった。21件を旧・新Releaseで5回ずつ交互測定した
総時間中央値は3.6331秒→4.4744秒で、長文のhigh-accuracy入力では平均約40.1 ms/件の
追加となる。生成を採点前に行う試作の約103 ms/件、33〜40文字で常に行う試作の
約156 ms/件より小さい。6〜32文字の推論回数、`balanced`、モデルなし、履歴、
ユーザー辞書、入力ミス訂正、規則候補は変更しない。

## 2026-08-11 モデル優勢時だけ補う同文字数whole候補

6〜32文字ではgreedy生成が完全な辞書lattice pathに一致しても、baseとのcost差が
1,000を超える非局所候補は従来のwhole-result一致へ参加できなかった。この境界を
単純に3,100へ広げたり生成表層を直接先頭にすると、PUDで改善9件に対して悪化7件となる。
通常N-best内のmodel首位をconfidenceだけで選ぶ案もJWTDを悪化させたため採用しない。

生成は従来どおりEOS完了と完全な辞書経路を必須とし、既存の局所・複数領域・圧縮形に
該当しない同文字数候補だけを追加の採点候補にする。baseとのcost差は1,001〜1,400、
ASCII英数字列不変、漢字からひらがなへの退行なし、辞書確定済み姓名不変をすべて満たし、
さらに生のmodel log-likelihoodが他の全候補を1.5以上上回る場合だけ先頭へ昇格する。
生成表層そのものは採用せず、辞書が返した同一表層・cost・segmentsを使う。

最初に試した上限1,500はGSD devで2件改善した一方、PUDの既存正解
`面から測ら`を`面から計ら`へ変えた。PUDを閾値選択用validationとして上限1,400へ
狭めた後はこの回帰が消えた。条件固定後、GSD test、JWTD、AJIMEEを変更せず確認した。
最終判定は`fddc831`と変更後のRelease FFIを同じmodel・入力・左右文脈で比較した。

| dataset | `fddc831` | model検証型whole後 | 改善 / 悪化 |
| --- | ---: | ---: | ---: |
| GSD phrase dev 323件 | 222 | **224** | 2 / 0 |
| PUD phrase validation 446件 | 349 | **350** | 1 / 0 |
| GSD phrase test 320件 | 245 | 245 | 0 / 0 |
| 信頼性gate後JWTD 400件 | 235 | 235 | 0 / 0 |
| AJIMEE 200件 | 167 | 167 | 0 / 0 |

`専属意思権 → 専属医師兼`、`移行し機帯 → 移行し機体`、
`淡水庫で → 淡水湖で`を回収し、5 dataset合計で3件改善、既存正解の悪化は0件だった。
GSD test、JWTD、AJIMEEは候補先頭を含む製品出力が全件同一だった。6〜32文字で既に行う
生成推論の回数は増えず、条件に一致したときだけ最大32候補の末尾を1件入れ替えて採点する。
回収3例を20回ずつ反復した60件を旧・新Releaseで順序を反転しながら各10回測定すると、
総時間中央値は4.3011秒→3.9637秒、1件あたり71.7 ms→66.1 msだった。高速化を意味する
差とは扱わないが、追加候補によるlatency悪化はこの測定では観測されなかった。
これはazooKeyの生成結果評価から責務分離を参考にしたもので、ATOKやazooKeyの内部実装を
複製するものではない。

## 2026-08-11 一意な全漢字地名の表記保護

[ATOKの固有名詞優先](https://atok.com/other/support/howtouse/mac/tr/pgs/tr_conv_name.htm)は、
人名・地名・両方を優先する変換modeを明示的に分ける。Slimeでは地名候補を一律に優先せず、
通常の辞書第1候補に3文字以上の全漢字地名segmentがあり、同じ読みの地名POS表層が辞書内で
一意の場合だけ、high-accuracy modelによるひらがな表記への退行を拒否する。複数表記を持つ
`たちあらい → 大刀洗 / 太刀洗`、2文字以下、かな・カタカナ混在、漢字同士の意味選択は
従来どおりmodelへ委ねる。

最初の試作は地名を含む再採点をすべてbaseへ戻し、`枢軸 → 数字区`と
`太刀洗 → 大刀洗`の2回帰を起こしたため棄却した。次に一意表記へ絞っても、人名保護まで
通常再採点へ広げると不正解表層同士を2件変えたため、人名の既存境界とは分離した。最終形は
漢字からひらがなへ退行する候補だけを安価に先判定し、該当時だけ辞書segmentを検証する。

Release FFIで`daaebbf`と比較すると、信頼性gate後JWTDは235→236となり、
`五所川原警察署くるみだて駐在所 → 五所川原警察署胡桃舘駐在所`だけが変化した。
GSD dev 224、PUD 350、GSD test 245、AJIMEE 167は候補先頭を含む全出力が一致した。
非該当60件を旧・新各6回、順序を反転して測定した中央値は105.7→108.6 ms/件で、各runの
範囲は旧93.8〜129.6、新97.6〜122.7 ms/件と重なった。該当例20件の同条件中央値は
146.8→139.0 ms/件だった。高速化とは扱わないが、辞書検証を該当時へ遅延した最終形では
明確なlatency回帰は観測されなかった。

## 2026-08-11 辞書完全一致カタカナ語の混在表記分断を防止

ニューラル再採点が辞書第1候補の`アルゴル`を`あるゴル`へ変更する残存誤変換を分析した。
カタカナ語を一律に保護すると、利用者が意図する全ひらがな表記まで妨げる。そのため、4文字
以上の全カタカナsegmentが辞書に完全一致し、model候補の変更がそのsegment内だけに収まり、
同じ文字数のひらがな・カタカナ混在表記へ分断する場合だけ再採点結果を拒否する。全ひらがな
化、短い語、辞書にないカタカナ列、segment外も同時に修正する候補は従来どおり評価する。

最初の試作はsegment外も変更する候補を変換全体ごと拒否し、PUDで
`メルディン現 → 目ルディン源`という不正解表層同士の不要な変更を1件起こしたため棄却した。
最終形は変更範囲を1segment内へ限定し、辞書経路を調べる前に文字種と変更範囲を走査する。

Release FFIで`ec1ac44`と比較すると、JWTDは236→237となり、
`あるゴル太陽系の惑星間を移動するための宇宙船 → アルゴル太陽系の惑星間を移動するための宇宙船`
だけが変化した。GSD dev 224、PUD 350、GSD test 245、AJIMEE 167は全出力が一致した。
非該当60件を旧・新Releaseで順序を反転しながら各6回測定した総時間中央値は
4.390→4.215秒、1件あたり73.2→70.3 msだった。範囲は旧4.22〜5.78秒、
新4.18〜5.63秒と重なるため高速化とは扱わないが、明確なlatency回帰は観測されなかった。

## 2026-08-11 確定単位を越える左文脈

platform adapterは変換開始時にカーソル前後の文書をcoreへ渡していたが、従来の
`SessionHistory`は次の候補確定時に左文脈をその候補だけへ置き換えていた。また、候補を
選ばず確定したひらがな・記号・英字・live conversionは文脈全体を破棄していた。このため、
同じ文を細かく確定すると、文書全体を見られるはずのニューラル再採点が直前の変換単位しか
利用できない経路があった。

確定表層を変換単位にかかわらず一時的な左文脈へ連結し、末尾128文字へ制限する。モデルへ
渡すpromptは従来どおり末尾40文字である。候補選択を伴わない確定と句読点は予測文脈には
残すが、永続履歴の文脈edgeにはしない。外部右文脈は維持し、caret・document・input client
の境界ではplatform adapterが置き換える。文書内容は履歴fileへ保存せず、private modeでは
左右とも即座に破棄する。

文脈長だけの寄与を分離するため、同じv3.2-small Q5、候補集合、左右文脈、採点条件を固定し、
GSD phraseの左文脈を末尾8文字へ切った場合と、製品prompt上限の末尾40文字まで利用する場合を
比較した。

| dataset | 末尾8文字 | 末尾40文字 | 差分 |
| --- | ---: | ---: | ---: |
| GSD phrase dev 323件 | 224 | 224 | 0件 |
| GSD phrase test 320件 | 242 | **245** | 4改善 / 1悪化 |

testでは`打ち明けあっ → 打ち明け合っ`、`世界電動 → 世界伝道`、
`カルトくんと読ん → カルトくんと呼ん`、`その時期はご → その時期は5`を修正した一方、
`ホテルが建ち → ホテルが立ち`が1件悪化した。ほかに不正解同士の表層変化が2件あった。
したがって長い確定文脈は純改善だったが、文脈だけで常に正解するとは扱わず、候補・domainを
固定した回帰評価を継続する。この比較は文脈窓のablationであり、逐次確定処理そのものは
core testで「外部文脈 + 複数変換 + 句読点 + 次の変換」と「候補なし確定 + 次の変換」を固定した。

同じGSD phrase test 320件を短い文脈・長い文脈の順序を反転しながら各3回測定すると、
総時間中央値は末尾8文字14.015秒、末尾40文字15.631秒で、1件あたり43.8→48.8 ms
（約+5.0 ms）だった。各組の差は+2.6〜+9.8 ms/件で、長いpromptのcostは実在する。
追加の永続化・network通信・通常辞書変換の推論はなく、ローカルmodelを有効にした文脈採点の
精度とlatencyの交換として扱う。

## 2026-08-11 複数segment末尾の右文脈複合語

右文脈と候補を連結した完全なMozc辞書語による順位補正は、従来は変換全体が1segmentの
場合だけ適用していた。しかし製品のN-bestでは、文頭側がすでに複数segmentへ分かれ、末尾の
同音語だけが右文脈と複合語を作る場合がある。`当初バッテリふ｜具合`では`不具合`が辞書に
あっても、`当初バッテリ不`全体は1segmentでないため証拠を使えていなかった。

複数segment候補では末尾segmentだけを対象にし、その直前までの候補表層を左境界として、
既存の完全辞書語・品詞・cost条件を再利用する。一般の長い右prefixまで許すと
`事件の際｜セット → 事件の再｜セット`が発生したため棄却した。最終条件は、右suffixが2文字
以下、または既存の語境界検証を通る`AやB`・`AのB`に限定する。新しい候補は生成せず、N-best
にすでにある末尾表層だけを並べ替える。同じ末尾読みと左境界の辞書計算は変換回内で再利用し、
文書内容や学習状態として保持しない。

`bedbf2a`相当の製品Release FFIと同じmodel・左右文脈で比較した。

| dataset | 変更前 | 変更後 | 改善 / 悪化 |
| --- | ---: | ---: | ---: |
| GSD phrase dev 323件 | 224 | **225** | 1 / 0 |
| GSD phrase test 320件 | 245 | **248** | 3 / 0 |
| PUD phrase 446件 | 350 | **354** | 4 / 0 |
| 信頼性gate後JWTD 400件 | 237 | 237 | 0 / 0 |
| AJIMEE 200件 | 167 | 167 | 0 / 0 |

`作業者はし → 作業者は死`、`固まった方 → 固まった肩`、`たことが書い → たことが買い`、
`室もここがは → 室もここが歯`、`当初バッテリ付 → 当初バッテリ不`、`三者のか → 三者の過`、
`係としてたん → 係として短`、`二人の日 → 二人の非`を修正した。5 dataset合計は8改善・
既存正解の悪化0件で、JWTDとAJIMEEは全出力が同一だった。辞書計算cacheの導入前後も、
同一modelで5 datasetの製品出力は全件一致した。

モデルを無効にし、GSD dev/testとPUDの計1,089件を旧Releaseと新Releaseで順序を反転しながら
各6回測定した。中央値は0.8835→0.9350 ms/件で、追加は0.0516 ms/件（+5.8%）だった。
候補ごとに同じ辞書走査を行う試作の約+17%から縮小した。絶対値は小さいが入力中のhot path
なので、右文脈規則を広げる場合も同じ条件の性能gateを継続する。

## 2026-08-11 数字列に埋もれる「数字＋に＋サ変名詞」

`ぐれーどよんにいち｜し`は、正解`グレード4に位置｜し`がN-best 16位に存在する一方、
数詞生成が`よんにいち`全体を`421`として1segmentにまとめ、接続costで約6,000先行していた。
数詞生成自体を弱めると電話番号や型番を壊すため、32候補内に「数字surface」「助詞に」
「同じ読みで一意なサ変名詞」の3segmentがあり、右文脈が`する`の活用で始まる場合だけ、
その既存候補へ構文証拠を与える。候補生成・数詞の通常順位・初回10候補の探索幅は変更しない。

最初の試作はGSD devで`みなさんにお伝えし → みな3にお伝えし`を1件悪化させた。
`さん`は数字3だけでなく敬称・複数呼称の末尾になるため、単独segment読み`さん`を構文補強から
除外し、正解と回帰の両方をfixtureへ固定した。

| dataset | 変更 | 改善 / 悪化 |
| --- | ---: | ---: |
| GSD phrase dev 323件 | 0 | 0 / 0 |
| GSD phrase test 320件 | 0 | 0 / 0 |
| PUD phrase 446件 | `グレード421 → グレード4に位置` | 1 / 0 |
| 信頼性gate後JWTD 400件 | 0 | 0 / 0 |
| AJIMEE 200件 | 0 | 0 / 0 |

モデルなしのGSD dev/testとPUD計1,089件を旧・新Releaseで順序を反転しながら各6回測定した
中央値は0.9485→0.9446 ms/件だった。高速化とは扱わないが、追加のlatency回帰は観測されなかった。

## 2026-08-11 引用格と受け手をまたぐ発話動詞

`被疑者を攻撃した、と｜けいさつにいっ｜た`では、正解`警察に言っ`が通常候補2位にあるが、
局所的な接続costは`警察に行っ`を1,078先行させていた。単なる`に＋いっ＋た`では移動動詞が
自然なので、左文脈が読点または閉じ括弧の直後の引用格`と`で終わり、候補末尾が「受け手＋
格助詞に＋発話動詞」、右文脈がその活用接続である場合だけ、既存候補へ構文証拠を与える。
発話動詞は`言う・伝える・話す・述べる・答える`の活用表層に限定し、候補生成は行わない。

公式zenz-v3.2-small Q5の製品Release FFI比較では、PUD 446件が355→356となり、
`警察に行っ → 警察に言っ`だけを修正して悪化0だった。GSD dev 323件は225のまま全出力不変。
5 datasetの入力を境界条件で全走査すると、該当する左文脈はPUDの改善例とGSD devの無関係な
2件だけで、GSD test、JWTD、AJIMEEは発火条件が0件だった。反例`昨日、警察に行った`は
移動動詞のままになることをcore fixtureへ固定した。

モデルなしのGSD dev/testとPUD計1,089件を旧・新Releaseで順序を反転しながら各6回測定した
中央値は0.9431→0.9416 ms/件だった。1 runに外れ値はあったが、中央値で追加costは観測されなかった。

## 2026-08-12 十分長い局所節へ絞るニューラル左文脈

[ATOKハイパーハイブリッドエンジン2](https://atok.com/info/features/engine.html)は、直前の
確定表記だけでなく日本語として自然な文と前後の語のつながりを優先する。
[azooKeyのZenzai](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/main/Docs/zenzai.md)も
左文脈と右文脈を別tagでモデルへ渡す。Slimeは確定文の末尾40文字を左文脈としていたが、
`電話には、GoogleアシスタントやAppleのSiriのような人工知能の｜インターフェイスがかけ｜ている`
では、古い節を含む40文字が`かけ`を維持し、読点後の32文字だけなら`欠け`を選べた。

そこで末尾40文字内の最後の句読点を調べ、その後が28文字以上ある場合だけ、同一の長い局所節へ
左文脈を絞る。短い節では話題語を失うため従来の40文字を維持し、右文脈、候補集合、辞書cost、
モデルweightとmarginは変更しない。最初の12文字境界はPUDで`写っ→映っ`のexact悪化1件と
不正解同士の変化1件を起こしたため棄却した。28文字へ固定すると両方が消えた。

公式zenz-v3.2-small Q5の製品Release FFI比較では、PUD 446件が356→357となり、
`インターフェイスがかけ→インターフェイスが欠け`だけを修正した。GSD phrase dev 225/323、
GSD phrase test 248/320、信頼性gate後JWTD 237/400、AJIMEE 167/200は全出力不変だった。
発火は5データセット計1,689件中17件（GSD dev 4、GSD test 3、PUD 10、JWTD/AJIMEE 0）に限定された。

発火17件を旧・新Releaseで順序を反転しながら各6回測定した中央値は、76.9→70.7 ms/件だった。
測定幅が重なるため高速化とは扱わないが、文脈tokenを減らす変更で追加latency回帰は観測されなかった。

## 2026-08-12 発音表記の長音から辞書表記を探索

UniDicの発音や口語的なkana入力では、辞書が`う・い・お・え`で持つ長母音が`ー`になる。
従来のlatticeは入力readingとの完全一致だけを探索するため、`ちゅーごく → 中国`、
`しょとー → 諸島`、`ほのー → 炎`が候補集合へ入らなかった。これはmodelの順位問題ではなく
candidate recallの欠落なので、辞書costや特定単語を変更せず、元のreadingと発音表記から導いた
辞書表記readingを別経路として探索する。

直前kanaの母音に応じて`ー`を`あ・い・う`へ置き換え、e段は`い / え`、o段は`う / お`を
候補にする。元のreadingは常に先に探索し、別経路には長音1個あたり2,000のpenaltyを加える。
同一surfaceが元の経路にある場合は、そのcostとsegmentsを置換経路で上書きしない。
また、置換結果がカタカナ表記しか増やさない経路と、カタカナ列の内側だけを漢字化する経路は
除外する。これにより`ラーメン`、`パフォーマンス`、`コンピューター`、`グレード`を維持する。

探索は24文字以下、長音4個以下、reading variant 16個以下に限定する。各variantは上位6経路、
追加する変換全体はcost順の12件までとし、対象外readingは従来のexact経路をそのまま返す。
弱いpenalty 800はGSD devの`モーラが破壊 → 網羅が破壊`を悪化させた。1,200ではPUDの
`ウィントー自身`と`トランスコーカシア`を崩したため棄却し、2,000へ固定した。

`55ac99e`の製品Release FFIと、同じ公式zenz-v3.2-small Q5、左右文脈、profileで比較した。

| dataset | 変更前 | 変更後 | 改善 / 悪化 |
| --- | ---: | ---: | ---: |
| GSD phrase dev 323件 | 225 | **240** | 15 / 0 |
| GSD phrase test 320件 | 248 | **261** | 13 / 0 |
| PUD phrase 446件 | 357 | **378** | 21 / 0 |
| 信頼性gate後JWTD 400件 | 237 | 237 | 0 / 0 |
| AJIMEE 200件 | 167 | 167 | 0 / 0 |

GSD testでは`会場内`、`定義を厳しく`、`中国の反`、`平和`、`一定期間`、PUDでは
`10年間`、`性の高い`、`長時間`、`市場と並ん`、`言い方`、`成長`、`天皇`、`英国大使館`、
`音楽業界`、`流出`などを回収した。対象readingだけの旧・新Release総時間は、GSD dev
3.490→3.466秒、GSD test 3.272→2.863秒、PUD 5.734→5.721秒だった。高速化とは扱わないが、
候補上限を絞った最終形では追加latency回帰は観測されなかった。

## 2026-08-12 促音形「じっ」の数詞合成

数詞parserは`じゅう・じゅっ`を十として扱っていたが、口語・発音表記で使われる`じっ`を
認識しなかった。そのため`さんじっかい`、`ごじっこ`、`じっぷんけん`は、それぞれ
`30回`、`50個`、`10分圏`を候補集合へ生成できなかった。

`じっ`を十のtokenとして追加する一方、単独では危険なreadingとして扱う。前に別の数字が
ある`さんじっ・ごじっ・よんじっ`、または促音化した分のcounterが直後にある`じっぷん`
だけ数値nodeを公開する。これにより`じっけん → 実験`、`じっこう → 実行`、
`じっかい → 十戒`を`10件・10校・10回`へ誤って分割しない。特定の期待surfaceや文脈語は
条件に含めず、数詞の構造だけで候補を生成する。

条件をGSD phrase devで固定すると、公式zenz-v3.2-small Q5の製品Release FFIで
`30回`、`50個`、`10分圏`の3件を回収した。その後のGSD phrase testでは`40歳`を1件回収し、
PUD phrase、信頼性gate後JWTD、AJIMEEは先頭出力が全件同一だった。
モデル無効時も同じ4件だけが改善し、5 dataset計1,689件で既存正解の悪化は0件だった。

改善4件を旧・新Releaseの順序を反転しながら各3回測定した総時間中央値は、
旧0.590秒、新0.553秒だった。測定幅が重なるため高速化とは扱わないが、数詞token追加による
対象入力のlatency回帰は観測されなかった。

## 2026-08-12 辞書語の途中から始まる数詞経路を抑制

数詞候補は型番や数量を取りこぼさない一方、辞書語の途中から始まると
`よせんなない → よ1007位`、`とくにろくだい → と926代`、
`にはしぜんこ → には4000個`のように、複数のかなを大きな数へまとめて辞書語を分断する。
数詞全体のcostを上げると本物の数量を壊すため、漢字を含む低cost辞書語のreading内部に
数詞経路が重なる場合だけ、その数詞候補へ750のpenaltyを加える。候補自体は削除せず、
ニューラル再採点と文脈規則から引き続き選択できる。

対象はword cost 6,000以下の辞書語と、実際に数詞候補を生成した曖昧な開始reading
`に・し・ご・く・せん・ぜん`に限定する。`いっ・はっ・ろっ`の後に十・百・千が続く
強い促音数詞は保護対象外とし、`はっせん → 8000`を維持する。また辞書語末尾の`く`だけを
次の数詞が再利用する境界には直前辞書語から保護を持ち越さず、既存の
`なくいちにん → なく1人`を維持する。
hot pathでは全位置を再検査せず、数詞なし・未保護・保護済みの3状態を先に作り、未保護の
数詞開始位置を含む辞書entryだけ表層を確認する。

`c462f43`の製品Release FFIと、同じ公式zenz-v3.2-small Q5、左右文脈、profileで比較した。

| dataset | 変更前 | 変更後 | 改善 / 悪化 |
| --- | ---: | ---: | ---: |
| GSD phrase dev 323件 | 243 | **244** | 1 / 0 |
| GSD phrase test 320件 | 262 | 262 | 0 / 0 |
| PUD phrase 446件 | 378 | 378 | 0 / 0 |
| 信頼性gate後JWTD 400件 | 235 | **236** | 1 / 0 |
| AJIMEE 200件 | 167 | 167 | 0 / 0 |

`予選7位`と、長文中の`ショタ24箇所高み → ショタにしかショタ神`を正解へ戻した。
`と926代 → 特に6代`も正解readingの語境界まで回復したが、期待値`特に六代`との表記差が
残るためexact改善には数えていない。ほかの正解からの悪化は0件だった。

モデル無効時はGSD testが225→226、AJIMEEが106→107となり、
`マントを94 → マントを駆使`と`人文科学研究科は94 → 人文科学研究科博士`を修正した。
GSD dev 215、PUD 318、JWTD 118は正解数を維持し、5 datasetすべてでexact悪化は0件だった。
PUDの`外在42 → 外在しに`とJWTDの`44がなく → 獅子がなく`は不正解同士の改善方向の変化である。

改善例と促音数詞の4件を旧・新Releaseの順序を反転しながら各3回測定した総時間中央値は、
旧0.908秒、新0.908秒だった。測定幅は重なり、3状態化した最終形で追加latency回帰は
観測されなかった。

## 2026-08-12 文脈モデルが強く支持する候補集合外の全体変換

高精度profileのgreedy生成は、通常の再採点候補にない表層も辞書latticeで再検証できる。
従来は通常1位とのcost差1,400までに限定していたため、左右文脈では明確でも辞書costが
1,500台から1,700台の`公害車`、`京風の引き`、`いるため過激`、`海洋地殻`を候補集合へ
加えられなかった。cost上限を2,000へ広げる一方、モデル生scoreがほかの全候補を1.8以上
上回ることを必須にする。

最初の1.5境界はGSD devで`伸長率 → 伸張率`を悪化させた。評価probeの候補集合外採点だけが
製品と異なり文脈差分強調を適用していなかったため、製品と同じ
`contextual + 0.1 * (contextual - context-ablated)`へ揃えた。悪化例のモデル差は1.606、
改善する`海洋地殻`は1.922だったため、1.8を固定境界にした。

また、モデル差が境界未満でも、通常の辞書costとの補間順位から同じ追加候補が選ばれる抜け道が
あった。候補集合外の全体変換はモデル生scoreの条件を満たすこと自体が採用根拠なので、条件を
満たさない場合はその候補を除いて通常候補だけを再順位付けし、追加候補は選択可能な末尾候補に
残す。これにより`面から測ら → 面から計ら`も維持した。

引用符で囲まれた範囲はタイトル、製品名、固有名称である可能性が高い。左右文脈で最も近い
`「」・『』・“”・‘’・《》・〈〉`が対になる場合、通常の厳格なcost差1,000を越える
モデル専用の全体書き換えは行わない。これにより`“次世紀ファーム研究所”`を
`“磁性器ファーム研究所”`へ変える悪化を、単語例外なしで防いだ。

`5561650`の製品Release FFIと、同じ公式zenz-v3.2-small Q5、左右文脈、high-accuracy profileで
比較した。

| dataset | 変更前 | 変更後 | 改善 / 悪化 |
| --- | ---: | ---: | ---: |
| GSD phrase dev 323件 | 244 | **248** | 4 / 0 |
| GSD phrase test 320件 | 262 | **264** | 2 / 0 |
| PUD phrase 446件 | 378 | **381** | 3 / 0 |
| 信頼性gate後JWTD 400件 | 236 | 236 | 0 / 0 |
| AJIMEE 200件 | 167 | 167 | 0 / 0 |

改善した9件は`郊外者 → 公害車`、`強風の引き → 京風の引き`、
`国の国家 → 国の国歌`、`ペンギンが三和 → ペンギンが3羽`、
`は白眼し → は白眼視`、`いる為過激 → いるため過激`、`であった塩 → であった袁`、
`カ国の大志 → カ国の大使`、`海洋近く → 海洋地殻`である。既存正解の悪化は0件だった。

モデル無効時はGSD dev 215、GSD test 226、PUD 318、JWTD 118、AJIMEE 107で、
旧・新Releaseの先頭出力が5 dataset全1,689件で一致した。改善9件を旧・新の順序を反転しながら
各6回測定した総時間中央値は0.997→0.901秒だった。測定幅が重なるため高速化とは扱わないが、
候補を1件追加する最終形で対象入力のlatency回帰は観測されなかった。

## 2026-08-12 強いモデル証拠による深い辞書pathの回収

候補集合外の全体変換には1.8以上のモデル生score差、引用範囲の保護、ASCII・既存漢字・
人名・地域表記の維持を要求している。この安全条件を固定したまま、許容する辞書cost差を
2,000から、既存の複数領域探索と同じ3,100まで広げた。通常候補、モデルweight、score差、
候補数上限は変更しない。

`00f2e1d`の製品Release FFIと同じ公式zenz-v3.2-small Q5、左右文脈、high-accuracy profileで
比較すると、GSD phrase dev 323件は248→249となり`前率線 → 前立腺`、JWTD 400件は
236→237となり`非ネラーに生み出された人工生命体 → ヒネラーに生み出された人工生命体`を
修正した。GSD phrase test 264/320、PUD phrase 381/446、AJIMEE 167/200は全出力が一致し、
5 datasetで既存正解の悪化は0件だった。

モデル無効時は5 dataset全1,689件の先頭出力が一致した。改善2件を旧・新の順序を反転しながら
各12回測定した総時間中央値は0.284→0.292秒で、1件あたり約+3.9 msだった。測定範囲は重なるが、
候補を1件追加して採点するcostはあるため高速化とは扱わない。この追加costはニューラルmodelが
有効で、greedy生成が2,000を越える完全な辞書pathを返した入力に限られる。

## 2026-08-12 確定済みの並列表記による数値保護

カーソル左に`66・0%、`、右に`・6%`がある`39`のように、入力中の整数が同じ
パーセント列の次要素だと前後の確定文字列から証明できる。従来は辞書latticeが
`仙台39`を1位にしても、後段のニューラル再採点が`仙台サンキュー`へ戻していた。

そこで、左が数値パーセントと列区切り、右が小数部とパーセント記号で、
辞書1位の末尾が数字である場合だけ、その結果をモデル再採点から保護する。
ASCII・全角の数字と`.・．`、`%％`を扱うが、通常の`さんきゅう`変換には発火しない。

`6b0a003`の製品Release FFIと公式zenz-v3.2-small Q5、左右文脈、high-accuracy profileで
比較すると、GSD phrase dev 323件は249→250となり、`仙台サンキュー → 仙台39`だけを
修正した。GSD phrase test 264/320、PUD phrase 381/446、JWTD 237/400、AJIMEE 167/200は
全出力が一致し、5 datasetで既存正解の悪化は0件だった。モデル無効時も全1,689件が一致した。

対象1件を旧・新Releaseの順序を反転しながら各12回測定した中央値は142.3→3.2 msだった。
確定済みの表記構造が辞書結果を一意に保護できるため、この場合はモデル推論自体を
省略する。通常入力のlatency経路は変更しない。

## 2026-08-12 明示的な桁読みを数量読みから分離

[ATOK 2026の公式説明](https://atok.com/features/)は、確定済みの語に続いて助詞から細切れに
入力した場合も語のつながりを使って変換する。[azooKey/Zenzai v3.2](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/93766c46e31fa6a18b7ced49dab31337780f6f45/Docs/zenzai.md)
も左右文脈を独立tagでモデルへ渡し、
[候補評価器](https://github.com/azooKey/AzooKeyKanaKanjiConverter/blob/93766c46e31fa6a18b7ced49dab31337780f6f45/Sources/KanaKanjiConverterModule/ConversionAlgorithms/Zenzai/Zenz/ZenzCandidateEvaluator.swift)
は候補不一致時にprefix制約を返して探索を続ける。どちらの方式でも、確定文脈が正解を
支持していても候補生成がその表記を失っていれば選択できない。

Slimeの数量parserは、`ぜろぜろぜろ`を数値0へ畳み、`にぜろいちよねん`を
`201 + 余年`、`ななはっせん`を8,000系としていた。数量の値とは別に、話者が明示した
各桁を保持する候補を追加する。先頭0を含む2桁以上、2桁以上の後で年counterへ接続する
`よ`、別の桁に続いて銭counterへ接続する`はっ`だけを対象にする。単独の`よねん`と
`はっせん`は従来どおりで、一般の数量parser、辞書cost、候補数上限は変更しない。

また、通常候補1位のASCII数字を、ニューラル再採点が意味を変えず全角へ置き換える場合は
通常候補を維持する。全角数字は明示的な候補と文字種変換として残る。これにより
`2014年`がモデルによって`２０１４年`へ戻されない。

`5c63674`の製品Release FFIと同じ公式zenz-v3.2-small Q5、左右文脈、high-accuracy profileで
比較した。

| dataset | 変更前 | 変更後 | 改善 / 悪化 |
| --- | ---: | ---: | ---: |
| GSD phrase dev 323件 | 250 | 250 | 0 / 0 |
| GSD phrase test 320件 | 264 | **265** | 1 / 0 |
| PUD phrase 446件 | 381 | **383** | 2 / 0 |
| 信頼性gate後JWTD 400件 | 237 | 237 | 0 / 0 |
| AJIMEE 200件 | 167 | 167 | 0 / 0 |

`無無八千 → 78銭`、`２０１余年 → 2014年`、`0人 → 000人`を修正した。GSD dev、
JWTD、AJIMEEは全出力が一致し、5 datasetで既存正解の悪化は0件だった。モデルなしでは
PUDが318→320、ほかはGSD dev 215、GSD test 226、JWTD 118、AJIMEE 107を維持した。
GSD testの`無無8000 → 78千`は不正解同士だが、モデル有効時は確定済みの
`139円80銭…14銭高`という文脈から`78銭`を選べた。

モデルなし5 dataset計1,689件を旧・新Releaseの順序を反転しながら各6回測定した中央値は
2.3911→2.3936 ms/件（+0.0025 ms、+0.10%）だった。改善3件だけを各12回交互測定した
ニューラル経路は90.1→92.3 ms/件（約+2.3 ms）で、追加候補を採点するcostがある。
候補不足を解消する純改善との交換として採用し、通常入力の追加走査は0.10%に収まった。

## 2026-08-12 発音表記を保った複合候補の第二段回収

`こーてーけん`の`皇帝兼`、`じゅーそーだん`の`重奏団`のように、各構成語は辞書に
存在しても、長音記号を含む発音表記では通常N-bestと既存の複合探索の両方から漏れる
ケースがあった。既存の長音variantは通常latticeとprefix制約探索へだけ適用され、候補末尾で
実行するbounded compound探索には適用されていなかった。

候補末尾へ到達した後だけ、最大6個の長音variantを2〜6区間のbounded compound探索へ渡す。
候補は1variant最大20件、全体32件の既存上限を維持し、長音置換costも通常latticeと同じ値を
加える。置換区間が漢字・数字を含まない候補と、置換区間がカタカナ区間に両側から挟まれる
混在表記は除外する。初回候補、live変換、ニューラル再採点poolは変更しない。

`9fb6802`のReleaseと比較した候補回収は次のとおりだった。

| dataset | 変更前 missing | 変更後 missing | 新規回収 |
| --- | ---: | ---: | --- |
| GSD phrase test 320件 | 14 | **12** | `重奏団`、`9幹線` |
| GSD phrase dev 323件 | 22 | **21** | `新生闇` |
| PUD phrase 446件 | 16 | **15** | `皇帝兼` |
| JWTD 400件 | 84 | 84 | なし |
| AJIMEE 200件 | 39 | 39 | なし |

5 datasetとも初期候補数と辞書top-1は不変で、明示的な第二段候補だけが4件増えた。同じ
Release benchを旧・新の順で各4回、200 iterationずつ測定した`こーてーけん`の複合探索は、
中央値0.683→16.776 msだった。候補末尾操作に限定した20 ms予算内であり、初回変換の
latency経路には入らない。
