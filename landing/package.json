{
  "name": "slime-landing",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "deploy": "pnpm build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.114.0"
  }
}
